# Functional Specification Document
## BirdBox — WiFi Nest Box / Feeder Camera with AI Species Identification
**Version:** 2.37
**Author:** SEspe
**Date:** 2026-07-20
**Changelog:**
- v2.37 — **Nordic teardown: remove the on-device TFLite-Micro model entirely (firmware 0.74.0), rewrites §3.2.** The on-device nordic classifier is retired — iNaturalist online (with corroboration + geo-filter + best-of-crop) plus the optional cloud tier now do all classification, and nordic was hard to train well for this location. `classify.cpp` drops ~1000 lines of TFLM: model/label loading, `decode_to_input`, `infer_scores`/`run_locked`/`run_one`, `decide_from_scores`, the `aggregate_frames` best-of-N+pooling, `model_init`/`model_load_named`, and the periodic on-device iNat batch. The **`espressif/esp-tflite-micro` (+ `esp-nn`) dependency is removed** — binary **~1.55 MB → ~1.44 MB**, and the **~3 MB PSRAM tensor arena + ~3.5 MB model buffer are no longer allocated at runtime** (the SD `/sd/model` files can be deleted). The **public API is kept stable** so `web_server.c` is unchanged: `classify_run_sync` → an iNat round-trip; `classify_available` → `inat_cv_enabled() || cloud_enabled()`; the **relabel picker vocabulary** (`classify_label*`) → the curated **`target_species.h`** Norway list (every entry in-region); `classify_model_name` → "iNaturalist online". **Recheck (§3.4) repointed to iNat** (`inat_event` with the geo-filter, `source=inatcv`), so re-running a day re-classifies it with the current Norway allowlist. The live cascade simplifies to a two-tier **iNat → cloud**. Vestigial Settings controls (on-device model / region-model / TTA) and `docs/MODEL.md` are now inert; a UI/docs cleanup follows. `<unistd.h>` added (`unlink` was transitively via the removed TFLM headers).
- v2.36 — **Auto-refresh the iNaturalist JWT from a session cookie — end the daily paste (firmware 0.73.17), extends §3.2.3.** iNat's JWT expires ~24 h and has no permanent key; OAuth needs a registered app the operator can't use. Instead the device now mints a fresh JWT itself the way the browser does: a **`GET inaturalist.org/users/api_token`** authenticated by the **`_inaturalist_session` cookie** returns `{"api_token":"eyJ..."}`, and that cookie lasts **weeks**. New `settings.inat_session` (NVS `s_inatses`, present-only, export-omitted) + **`inat_refresh_jwt()`** (`inat.c`): the GET with a `Cookie: _inaturalist_session=…` header, parses `api_token`, stores it in `inat_key` + NVS, clears any cooldown/error. **Self-healing:** a **401** on a score_image call sets a `s_jwt_stale` flag; the next event refreshes the JWT from the cookie before classifying (one event lost at the moment of daily expiry, then it recovers), and a missing token also triggers a refresh. **UI:** a *Session cookie* field + **🔄 Refresh token now** button (new **`POST /api/inat-refresh`**, offloaded to the identify worker like the token test) in the iNaturalist Settings section, with instructions to copy `_inaturalist_session` from the browser; `isess_set` in `/api/settings` GET, `isess`/`isessclear` in POST (sanitised: accepts a bare value or a `name=value; …` copy). The JWT-token field now also accepts the whole `{"api_token":"…"}` JSON (v2.35). One route (52/56). So: paste the cookie once, and BirdBox keeps its own token fresh.
- v2.35 — **Robust iNaturalist token ingest — accept the api_token JSON, no reboot (firmware 0.73.16), fixes §3.2.3.** The daily JWT is copied from `inaturalist.org/users/api_token`, which serves `{"api_token":"eyJ..."}`. Pasting that whole blob was **silently rejected** — `form_field` URL-decodes, so the literal `"` tripped the settings handler's quote guard, the token was *not* stored, the old (expired) one stayed, and it looked like the new token "wouldn't take" until a reboot (which only coincided with a cleaner re-paste). New **`sanitize_inat_token`** pulls the value out of the `{"api_token":"…"}` JSON and strips stray whitespace/quotes a copy-paste drags along (a bare JWT passes untouched). And **`inat_token_changed()`** now clears any leftover 429 cooldown + stale error when a token is stored, so a fresh token is used on the **very next event with no reboot**. Purely the ingest path; no route/settings-shape change.
- v2.34 — **Lower the solo-accept bar 90 → 75 (firmware 0.73.15), tunes §3.2.3.** With v2.33 live, single-frame Dompaps kept landing 78–89% and being dropped (a 90% one classified, an 81% one right after did not) — obvious lone birds that didn't linger for a second frame. `INAT_SOLO_ACCEPT_PCT` drops to **75**: it accepts those confident lone frames while staying **above the 47–72% band** where iNat flip-flops between look-alikes (the *Pica pica*/*hudsonia* magpie case the corroboration guard exists for), so a weak lone hit still needs a second frame. One-line constant.
- v2.33 — **Solo-accept a very-confident single frame + lower the confidence floor (firmware 0.73.14), refines §3.2.3.** The per-frame log exposed a real gap: a **98%-confident Dompap** on a **single-frame** event (bird left after one frame) was logged *unclassified*, because the v2.26 corroboration guard requires **≥2 frames** to agree. Fix: `inat_event` now, when no species reaches 2 votes, accepts the most-confident lone frame **iff its confidence ≥ `INAT_SOLO_ACCEPT_PCT` (90%)** — so an obvious bird that didn't linger isn't dropped, while the guard still applies to everything below 90% (a 50–70% lone magpie — the false-positive class — still needs a second frame). Independently, the Settings **confidence-threshold input floor drops 30 → 15** (the server already accepted 0–100; only the UI restricted it), so the operator can lower it now that the **geo-filter (v2.28)** and **corroboration (v2.26)** backstop it — suggested **25%** (today's borderline real birds — Skjære 35–37%, a cropped Bokfink 34%, a tit 32% — sit just under the old 35%, while noise stays at 0–4%). No route/settings-shape change.
- v2.32 — **Live view: two species indicators — Current + Last identified (firmware 0.73.13), refines §3.3.** The single overlay (which showed the last classified species) is split into two. **Current** (bottom-left) reflects the **most recent event's outcome** — the identified species (with the Dompap avatar + confidence) or a greyed *"unclassified"* — and is **ephemeral**: it clears **3 min** after the event, or **the moment a new event starts** (a rising `evStart`, the motion trigger count, now in `/api/status`), so it never sits stale over the bird currently being processed. **Last identified** (bottom-right) is the **last real species ID**, **sticky** (the device's `species` field already persists it), kept as a reference regardless of the current event. Both share the `.livesp` style (`#livesp2` flips to the right edge) with small `.splbl` labels. Client-only inline JS/CSS plus one additive status field (`evStart`); no route or firmware-logic change.
- v2.31 — **Pixel-tight ROI + best-of whole-vs-crop on the iNat tier, live (firmware 0.73.12), refines §3.1/§3.2.3.** The v2.30 A/B proved a native-res crop lifts a small/off-centre bird a lot when the box is clean (Bokfink 17→34%, magpie 35→82%) but hurts on a loose/messy box. Two changes make it a live win. **(1) Pixel-tight ROI (`motion.c`):** instead of snapping `s_roi` to the 8×8 cell grid (12.5% quantisation), it now takes the tight bounding box of the actually-changed **pixels** inside the winning cluster's cells, from the 160×90 diff already computed — finer than even a 16×16 grid would give (which would starve each cell to ~10×5 px), at ~zero cost and without touching detection sensitivity/zone. **(2) Best-of whole-vs-crop (`classify.cpp` `score_frame_best`):** each frame is scored whole; **only when whole can't identify it** (empty latin) and `detect_zoom` is on does it also score a native-res crop centred on that tight box and keep the better real-species result (`result_better`). So confident birds cost nothing extra and only the hard frames spend a second (paced/cooled) iNat call — and whole is always tried, so it never scores below whole-only. `detect_zoom` is repurposed to this best-of-crop (its old on-device-model crop is moot with nordic off); Settings text updated. Reuses `classify_crop_jpeg` (v2.30). No route/settings-shape change.
- v2.30 — **Native-res ROI crop, A/B-testable via the debug endpoint (firmware 0.73.11), tests §3.1/§3.2.3.** A small/off-centre backlit bird under-scores on iNat because its CV is trained on subject-filling photos (a live Bokfink: correct species was iNat's top guess but only 17%, all frames sub-threshold → unclassified). The v2.22 ROI crop was reverted (v2.23) because it downscaled to **224²** and measured worse — but the fault was the *downscaling*, not the cropping. New **`classify_crop_jpeg`** decodes at the native (budget-capped, so HD → 640×360) resolution, crops a **square centred on the motion box, padded ~1.4× for context**, and re-encodes — subject-filling **and** sharp. Exposed **read-only** as **`GET /api/id-frame-debug?...&crop=1`** (`IDENT_INAT_FILE_DBG_CROP`): it looks up the frame's box from the per-day frame-ROI sidecar (`frame_roi_lookup`), crops, and re-scores via iNat **without saving**, so whole-frame vs crop can be A/B'd on the same stored frames before any live change. No change to the live cascade yet — this is the measurement tool to decide whether zooming helps. Re-adds `img_converters.h` (`fmt2jpg`) to `classify.cpp`; one enum kind, no new route.
- v2.29 — **True per-image iNat scores in the visit log + clear the stale live badge (firmware 0.73.10), extends §3.2/§3.4/§3.3.** Two operator asks. **(1) Per-frame scores:** the visit-log row gains a trailing **per-frame column** (col 11) written by `inat_event` — `"<binomial>=<pct>;..."` for each of the event's frames (a decided species, `<binomial>?=<pct>` when the frame was unsure/sub-threshold, or `err` on a failed call). It's on both classified and unclassified rows, so a table can show the true % per image and *why* corroboration decided as it did (the data only ever lived in the debug log before). Surfaced in **`/api/events`** as a `"pf"` field (parsed in `gal_build_labels`, held in an out-of-line PSRAM pool `gal_tab_t.pf[]` indexed by `gal_label_t.pfi`, so the 6000-entry label table doesn't grow by the string). Rows are now longer, so **every visit-log reader's line buffer widens 400 → 768** (writer caps at 720) — a split-row would otherwise mis-parse (§3.4 CSV rule); the new column is appended last so no existing fixed-column parse shifts. **(2) Stale badge:** the live-view species badge showed the last *classified* species and re-popped on every new event — so an unclassified visit (a bird that declined corroboration) left a stale label (e.g. a magpie) sitting over the current bird. New `classify_last_event_identified()` → **`spLive`** in `/api/status` is true only when the MOST RECENT event got a real species; the badge JS now shows only when `spLive`, so a new unclassified visit **clears** it. No route/settings-shape change; two additive fields.
- v2.28 — **iNaturalist geo-filter: reject out-of-region species, pick the best in-region one (firmware 0.73.9), fixes §3.2.1/§3.2.3.** Live-data diagnosis (via the v2.27 debug endpoint) found iNat cannot reliably separate *Pica pica* (Skjære) from *Pica hudsonia* (Svartnebbskjære, the **North-American** black-billed magpie) — it flip-flops between them frame-to-frame at low confidence (45–72%), sometimes rating the geographically-impossible American one **higher**; corroboration then just votes the coin-flip (three magpie visits logged correct, two wrong). Root cause: **the region filter only applied to the on-device model, never to iNat online results** — `inat.c` read `results[0]` and returned it, so a species impossible in Norway sailed through. **Fix:** `inat_classify_jpeg` now **walks `results[]`** (not just the top): it fills `top_label[0..2]` with the raw top-3 for the debug/visit-log view, and — when `region_filter` is on — **decides on the highest-scoring bird that passes the Norway allowlist** (`species_in_region`), skipping out-of-region hits (Pica hudsonia) for the best in-region candidate (Pica pica). Filter off ⇒ the top bird wins (unchanged). An all-out-of-region reply yields *Unidentified* (the out-of-region guess is itself the signal). `region_filter` is thus **repurposed** to govern the iNat path too (same NVS key/checkbox; Settings text updated). The **Norway allowlist is extended** (`species_i18n.c` `NO_NAMES`, ~111 → ~148) with more plausible species using iNat's **current** binomials (e.g. *Linaria flavirostris*, *Curruca curruca*) so it matches what `score_image` returns — a taxonomy-sync caveat: a few older entries (e.g. *Sylvia communis*) may lag iNat and would be filtered until updated. `INAT_RESP_MAX` 16 KB → 32 KB so more results fit for the walk. No route/settings-shape change.
- v2.27 — **Read-only iNaturalist reclassify for debugging (firmware 0.73.8), extends §3.4.** New **`GET /api/id-frame-debug?date=&f=`** re-runs the iNat online CV on a saved frame and returns the full result (species, confidence, top-3 candidates) **without saving anything** — no relabel, no confirm, no visit-log write, no state change. The existing 🔍 `id-primary` *saves* a confident result (`idfile_save`→`storage_relabel`, flipping the frame to human-confirmed ground truth), so it can't be used to safely re-score an event for inspection; this is the missing non-destructive path. Purpose: redo an *unclassified* or geo-wrong event (e.g. a magpie logged as *Pica hudsonia*, the American black-billed magpie, instead of *Pica pica*) and see what iNat returns per frame — including whether the correct species sits just behind the wrong one in the top-3 — to diagnose geo weighting without mutating the retrain data. Requires iNat online enabled (cloud costs money; the on-device model already has `/api/classify-file`). New `IDENT_INAT_FILE_DBG` worker kind + one route (51/56); shares the identify worker and `classify_send_json`, so `saved` is always false. No firmware-logic change to the live cascade.
- v2.26 — **iNaturalist scores every frame + a corroboration guard (firmware 0.73.7), hardens §3.2/§3.2.3.** The old path handed the classifier only the first **3** of an event's frames (`CLASSIFY_BEST_OF_N`) and, for the iNat tier, scored just **frame 0** — so one confidently-wrong view (a bird half-out of frame, an off-pose blur, a domain-shift misfire) became the logged ID with nothing to check it against. Two changes. **(1) All frames classified:** the frame-carry capacity (`CLASSIFY_BEST_OF_N`, the `s_frame_paths`/`cls_job_t`/recheck array dimension) goes **3 → 10** (= the max `capture_count`), so *every* saved frame flows to the classifier instead of being saved-but-ignored. The per-job struct grows (~464 B → ~1.25 kB; the 16-deep queue ~7.5 → ~20 kB internal RAM). **(2) Corroboration on the iNat tier:** `inat_event` now scores **all** of the event's frames (iNat's ~1 req/s pacing + 429 cooldown from v2.25 make the per-frame loop self-throttle, and a cooldown fails every frame → clean decline), tallies a **vote per distinct species** across frames, and requires the winner to be **seconded by ≥2 frames** — the model can be confidently wrong on one view but rarely on two independent ones. Among species clearing that bar the **most confident wins** ("best seconded species"), logged from its strongest frame; the candidate table lives in **PSRAM** (a TLS handshake sits deep on this task's stack). If **no** species is seconded it **declines** (returns false): with the on-device tier off (the reference unit's iNat-sole-source config, v2.24) that lands the event on **Unidentified** — its own row, or the cloud tier if enabled — i.e. the operator-chosen *"else Unidentified"* fallback; with nordic on it still gets its shot. **Consequence:** corroboration needs ≥2 frames of agreement, so a **1-frame event can never clear it** and always declines to Unidentified — acceptable given `capture_count` defaults to 5. The nordic tier is unchanged in logic (best-of-N + evidence pooling) but now sees all frames too; cloud stays whole-frame / manual. No new route or setting; `inat_event` gains a `scored_out` count for the visit-log "N/M frame(s)" line.
- v2.25 — **iNaturalist rate-limit handling: 429 cooldown + ~1 req/s pacing (firmware 0.73.6), hardens §3.2.3.** iNat's limits: **100 req/min** is a hard **429**, **~60/min (1 req/s)** is the recommended pace, **10 000/day** max. BirdBox is naturally gentle (events are classified serially and each call takes seconds), but a burst of manual identifies or a very busy feeder could trip it — and now that iNat is the sole classifier, a throttle matters. `inat.c` now: (1) **paces** to ~1 req/s (a min-gap `vTaskDelay` before each call); (2) on an **HTTP 429**, enters a **60 s cooldown** (`s_cooldown_until_us`) during which live events **skip iNat entirely** (fall through to the next tier / Unidentified) rather than hammering the throttle — which only extends the ban; (3) reports it: `inat_cooldown_s()` → `inatCooldown` in `/api/sysinfo`, shown in the Debug tab's System panel as a red "iNaturalist — rate-limited, cooling down Ns", and the last-error string names the 429. No new route; two additive getters/fields.
- v2.24 — **Toggle to disable the on-device (nordic) tier — run iNaturalist as the sole source (firmware 0.73.5), extends §3.2.3.** The operator's plan is to lean on iNaturalist and retire nordic (hard to train well for this location). New `settings.ondevice_enabled` (NVS `s_ondev`, default 1, Settings checkbox "Use the on-device model (nordic) as a fallback", in `/api/settings` GET/POST + export): when **off**, the live cascade skips the nordic tier — events go **iNat → cloud**, or stay *Unidentified* if the cloud is also off (i.e. iNaturalist as the sole classifier). `classify_task` gates the nordic `aggregate_frames` tier on it, and `classify_submit_event` treats the on-device tier as available only when the model is loaded **and** the toggle is on (so an event is still accepted when only iNat/cloud is active). The model stays on the SD card and the Gallery's 🔍 manual identify still uses it — this only removes nordic from **automatic** live classification, so it's fully reversible. Shipped with the toggle **off** on the reference unit per the operator's request (iNat-only).
- v2.23 — **Revert the iNat ROI crop — whole frame is better (firmware 0.73.4), reverts v2.22.** The v2.22 ROI crop measured **worse**, not better: a live event on a Bokfink scored **20% (unidentified) on the 224² ROI crop**, but the **same frame scored 93% Bokfink on the full frame** via the manual magnifier (both iNat, same geo — confirmed the crop was the only difference, on firmware that had the crop live). The nearest-neighbour 224² crop degrades quality, and the motion box isn't always a clean bird box, whereas iNat's CV does great on the full-resolution frame (feeder birds are prominent enough). So `inat_event` goes back to sending the **whole frame** (like the cloud tiers); `classify_roi_crop_jpeg` and the `esp_camera.h`/`img_converters.h` includes are removed. Lesson: don't down-res/crop images for the online iNat model — give it the cleanest full-res frame. Distant-bird misses (e.g. the earlier Lavskrike) are better addressed by the nordic retrain than by cropping.
- v2.22 — **Live iNaturalist tier sends the ROI crop, not the whole frame (firmware 0.73.3), refines §3.2.3.** iNat's CV is trained on subject-filling observation photos, so handing it a whole feeder scene with a small/distant bird classifies poorly (a Bokfink at the far side reads as background). On a live event with a motion ROI, `inat_event` (classify.cpp) now decodes the frame, crops to the square-expanded ROI (reusing `decode_to_input` → `CLS_INPUT_SIDE`, rotated upright), re-encodes with `fmt2jpg` (esp32-camera), and uploads that crop instead of the full JPEG — the bird fills the frame, which is what iNat wants. No ROI, or any decode/crop/encode failure, falls back to the whole frame, so nothing is ever dropped. The paid Claude/Gemini LLM tiers keep sending the whole frame (they reason about context). Scope: the **live** cascade only; the Gallery's manual 🔍 iNat identify still sends the whole frame (and old gallery frames often have no logged ROI anyway). New static `classify_roi_crop_jpeg`; `esp_camera.h`/`img_converters.h` added to classify.cpp.
- v2.21 — **iNaturalist geo hint: a capital-city location dropdown (firmware 0.73.2), fixes §3.2.3.** First live iNat CV test exposed the gap: with no location sent, `score_image` runs vision-only and returned *Haemorhous cassinii* (Cassin's Finch — North American) at 6% for a Nordic feeder bird. iNat's model is only strong **with geo context** (its `combined_score` blends vision with "how likely is this species here"). Added a **Location (nearest capital)** dropdown in the iNat Settings section — **104 world capitals** with coordinates (`main/cities_data.h`, generated; Nordic ones first) whose option value is `"lat,lng"`, stored in `settings.inat_loc` (NVS `s_iloc`, validated `[0-9.,-]`, carried in the export). `inat.c` now passes `lat`/`lng` as **URL query params** on the `score_image` call (what the geomodel reads; the multipart parts are kept as belt-and-suspenders) when a location is set, so the CV favours locally-plausible species and confidence rises above threshold. New `loc` field in `/api/settings` GET/POST + export. Pick the capital nearest the feeder and iNat's results should transform from geographically-absurd low-confidence guesses to real local IDs.
- v2.20 — **Gallery 🔍 identify now uses the PRIMARY classifier (iNat online when on) (firmware 0.73.1), extends §3.4.** The Gallery's 🔍 button was hard-wired to the on-device model. It now hits a new **`GET /api/id-primary`** that runs whatever leads the live cascade: **iNaturalist online CV when `inat_cv_enabled`, else the on-device model** (falls back gracefully if neither is available). So a manual "identify" uses the same engine as live events — press 🔍 on a Skjære/Bokfink frame and it goes to iNat (if configured), not the model that's been missing them. The ✨ button still targets Claude/Gemini. New `IDENT_INAT_FILE` worker kind + the route (50/56); `source` tag `inatcv` on a saved iNat label. **Copy-last already covered:** `idRun` seeds `g_lastSp` from any confident identify's `latin`/`common` (v2.17), and `IDENT_INAT_FILE` replies through `classify_send_json` (which emits both), so a successful 🔍 iNat ID updates the 📋 copy-last species with no extra code. Client + one endpoint; no firmware-logic change beyond the router.
- v2.19 — **iNaturalist online CV as the primary classifier tier — a 3-tier cascade (firmware 0.73.0), extends §3.2.3.** Adds iNaturalist's **online** Computer Vision API (`POST /v1/computervision/score_image`, the model that powers the iNat app — current, region-aware, ~100k taxa) as an opt-in **primary** classifier, so the cascade becomes **iNat online → nordic-v0.x (offline fallback) → Claude/Gemini (paid, last resort)**. iNat is free (no per-call fee), which is why it runs first. **NOT the weak on-device `inat-birds-v1.tflite`** — this is the strong online model. **New `inat.{c,h}`** (reuses `cloud_util`): the wire format differs from the Claude/Gemini tiers — score_image wants a **multipart/form-data** file upload, so the raw JPEG is streamed between a fixed preamble/trailer (no base64). Auth is `Authorization: Bearer <JWT>`; a confident **bird** species (rank species, iconic Aves, ≥ threshold) wins, else empty latin so the event falls through to nordic (which owns the "no bird" call). **Cascade rework** (`classify.cpp` `classify_task`): tier-1 `inat_event` (confident bird only) → tier-2 nordic `aggregate_frames` → tier-3 `cloud_event`; the submit gate accepts events when *any* tier is available; `source` gains `inatcv` (gallery badge `[V]`, src=5). **Settings/UI:** `settings.inat_cv_enabled` + `inat_key[800]` (NVS `s_inatcv`/`s_inatk`); a new **iNaturalist online ID (primary, free)** Settings section (toggle + JWT field + **Test token** → `/api/inat-test` which validates against `/v1/users/me` + Forget); `inatcv`/`ikey_set` in `/api/settings` GET, `inatcv`/`ikey`/`ikeyclear` in POST, `inatcv` in the export (token omitted — a secret). The settings POST body buffer 896→**2048 B** with a proper **recv loop** (an ~800-char JWT lands last and spans TCP segments — the v1.89 partial-read class of bug). **Hard caveat — auth:** iNat has no permanent API key; the JWT is grabbed from `inaturalist.org/users/api_token` and **expires ~24 h**, so for now it needs re-pasting daily (an OAuth auto-refresh is a possible follow-up). One route added (`/api/inat-test`; 49/56).
- v2.18 — **Gallery multiselect: shift-click range select (firmware 0.72.6), extends §3.4.** Selecting a whole burst/region of tiles was one-checkbox-at-a-time. Now **shift-clicking a tile's checkbox selects every visible tile between the last-clicked one (anchor) and this one**, set to the clicked box's new state — the standard range-select gesture, so a run of same-species frames can be selected in two clicks and then bulk-confirmed / relabelled / deleted. Implemented client-side: each `.gchk` gains `onclick="gChkClick(event,this)"` (the click event carries `shiftKey` and the already-toggled `.checked`, which `change` doesn't) alongside the existing `onchange`; `gChkClick` walks `gVisChecks()` (filtered/visible order, so it respects the active Show filter) between `g_selAnchor` and the clicked box and toggles the range, then `gSelSync()` updates the count. The anchor updates on every click; a stale anchor after a day reload just yields index -1 (no-op), so nothing breaks. Checkbox tooltip notes the gesture. No route/API/firmware-logic change.
- v2.17 — **A confident cloud/model identify seeds the 📋 copy-last species (firmware 0.72.5), extends §3.4.** The Gallery's copy-last fast-path (📋 — apply the last species to another tile in one click) was seeded only by a manual ✎ relabel. Now a **confident identify** — the ✨ Gemini/Claude button, or the 🔍 on-device model — that returns a real species also seeds it, so a strong cloud label on one frame of a visit can be one-click copied onto its sibling frames instead of re-typing. **Wiring:** `classify_send_json` (the shared identify/`/api/cloud-file` reply) now emits `latin` + `common` (the English canonical) alongside the localized `species` display name — `/api/relabel` needs `l`/`c`, which the display name can't supply. `idRun` sets `g_lastSp={c,l,d}` when the reply carries a `latin` (empty latin ⇒ no-bird/Unidentified, which isn't a species to copy), and a new `refreshCopyLast()` enables + retitles the already-rendered 📋 buttons in place (no full day reload). Triggers on any confidently-named identify, not literally 100% confidence (cloud results are typically 95–100%). Client-side JS + two additive JSON fields; `classify_send_json` buffer 800→912 B. No route change.
- v2.16 — **Live species overlay: auto-hide after 3 min + Dompap avatar (firmware 0.72.4), refines §3.3/v2.12.** Two operator tweaks to the v2.12 live-view species badge. **(1) It no longer lingers forever** — the badge now **auto-hides ~3 minutes after the event that raised it**, so a stale "last bird" label doesn't sit on the live view indefinitely. Timing is keyed to the `/api/status` `events` count (not the species string), so two consecutive events of the *same* species still re-show and re-pop the badge; it hides once no new event arrives for the TTL. **(2) The generic 🐦 emoji → the Dompap logo** — the badge is prefixed with the same circular Dompap avatar used in the header (`BIRD_LOGO`, via a `.splogo` CSS background so the data URI isn't duplicated into the poll JS). Pure client-side (inline JS/CSS in `web_server.c`); no route, API, or firmware-logic change.
- v2.15 — **Header logo: the bird emoji → a Dompap photo (firmware 0.72.3), cosmetic.** The 🐦 emoji in the page header (dashboard `<h1>` and the WiFi-setup `<h2>`) is replaced with a small circular photo of a male Dompap (Eurasian Bullfinch — the box's signature species). An operator-supplied 864×1214 image was cropped to the head+breast, resized to 96×96, JPEG q85 (~2.2 KB), and embedded as a base64 data URI in `main/logo_data.h` (`BIRD_LOGO`) — small enough to inline, no new route, no SD dependency. New `.logo` CSS (circular, sized to the heading) on both pages. Pure cosmetic; no behaviour change.
- v2.14 — **SD write-error auto-recovery + a can't-miss alert (firmware 0.72.2), fixes §3.1/§7.** Reported live: motion fired all morning (77 triggers, boxes visible in the live view) but **0 events saved** — the SD card had wedged into a write-failing state (`sdWriteOk:false`, mounts + reads fine, 59 GB free) that survived until a reboot remounted it. The box silently stopped capturing for hours with no self-recovery and no warning. **Two fixes. (1) Auto-recovery (`storage.c`):** the mount logic is factored into `sd_mount()`, and on a failed write both writers (`storage_save_jpeg`, `storage_append_visit_log`) call **`sd_recover()`** — an unmount+remount that clears a transient FATFS/driver error — then **retry the write once**. `sd_recover()` is **cooldown-limited to ≤1 attempt / 20 s** so a genuinely dead/read-only card can't thrash the driver, and it counts successes (`storage_remount_count`). So a transient wedge now self-heals in-place instead of dropping captures until a human notices. **(2) Alert + diagnostics:** `/api/status` gains `sdWriteOk`; the **Live view shows a full-width red banner** "SD WRITE FAILING — captures are not being saved" whenever it's false (driven by the existing 2 s poll). `/api/sysinfo` gains `sdRemounts`; the Debug tab's SD panel shows **"Auto-recoveries: N — card may be wearing"** — a climbing count is the early warning that the card should be replaced (the underlying transient is a card-wear symptom; recovery buys time, not immortality). A concurrent gallery read can glitch during the remount — accepted, far better than silent capture loss. Additive: two status/sysinfo fields, two storage getters; no route change.
- v2.13 — **Fix creeping web-UI sluggishness: bigger httpd socket pool + shorter timeouts, with a socket-usage diagnostic (firmware 0.72.1), fixes §6.** Reported twice: the web UI (stream reconnects, Gallery, every endpoint) grows sluggish over uptime and a **reboot fully cures it** — the tell that something accumulates in connection state, not CPU/memory. Profiling confirmed: warm requests are ~20 ms (incl. the cached SD free-space query), memory is flat over uptime (no leak: 2.12 MB at 50 min vs 2.14 MB fresh), camera/WiFi healthy — but the **first request after an idle gap stalls ~5 s**, and 5 s is exactly esp_http_server's default socket timeout. Root cause: the default socket pool is small (`max_open_sockets` 7, capped by `CONFIG_LWIP_MAX_SOCKETS`=10), and a long-lived browser UI (keep-alive polls + the jerky HD stream reconnecting) leaves most slots in slow-to-reclaim states; once the pool is saturated a fresh request must wait ~`recv_wait_timeout` (5 s) to purge a stale socket. **Fix:** `CONFIG_LWIP_MAX_SOCKETS` 10→**16** (sdkconfig.defaults; needs a clean rebuild) so the httpd pool can grow, `max_open_sockets` 7→**12** (headroom so a fresh request finds a free slot instead of purging), and `recv_wait_timeout`/`send_wait_timeout` 5→**3 s** (a stale socket reclaims faster; still ample for a continuous OTA/model upload). **Diagnostic (added first, to confirm the cause with data):** `/api/sysinfo` now reports `httpdSock`/`httpdSockMax` (live count via `httpd_get_client_list`), shown in the Debug tab's System panel as "HTTP sockets N / 12" (flagged red near the cap) — if it climbs toward 12 over hours, the pool is congesting; healthy is a handful. The server handle moved to a file-scope `s_httpd` so the sysinfo handler can read the count. No route/API-shape change beyond the two added sysinfo fields.
- v2.12 — **Live view shows the last classified species as an on-video overlay (firmware 0.72.0), extends §3.3/§3.4.** The Live tab already flashed a motion border and highlighted the trigger cells, but the *result* of a detection — what species the model decided — was only in the small status text line under the video. Now a badge in the lower-left of the live frame shows the last event's species + confidence (e.g. "🐦 Dompap 92%"), so a glance at the live view tells you what was just identified. Data comes from the existing `/api/status` poll (2 s): `classify.cpp` already tracked the last event's species/latin for the status line, so this adds one field — `s_last_conf` (`classify_last_confidence()`) — surfaced as `spConf` in `/api/status`. The overlay (`#livesp`) updates in the live `tick()`; it holds the value in `dataset.v` and only re-runs its pop animation when the decision actually changes, so a persistent last-species badge doesn't flicker every poll. Shows whatever the last event resolved to (a species, "Unidentified bird", or "no bird" — the same value the status line appends), localized by the device. Pure-additive: one new int getter + one JSON field + CSS/markup/JS in the live pane; no route change, and the status buffer grew 720→768 B for the field.
- v2.11 — **Gemini model is now an operator-set Settings field, default gemini-flash-lite-latest (firmware 0.71.9), extends §3.2.3.** Chasing Gemini model availability in firmware proved a losing game — three distinct failures in one session, each needing a reflash: `gemini-2.5-flash` 404s "not available to new users" (an **account-tier** gate that enabling **billing did NOT lift**), `gemini-flash-latest` hit sustained 503 "high demand," and `gemini-2.0-flash` 404s "no longer available" as the 2.0 generation retires. So the model id moved to **`settings.gemini_model`** (NVS `s_gmdl`, default blank → `GEMINI_MODEL_DEFAULT` "gemini-2.5-flash" in `gemini.c`), exposed as a **Gemini model** text box in Settings → Cloud Identification with a plain-language hint: if a call 404s, press **Test Gemini** (which lists the flash model ids the key can actually call) and paste a working one — no reflash. `gemini.c` builds the generateContent URL from the setting at call time; the value is validated to `[a-z0-9.-]` on save (URL-safe and safe to echo unescaped into `/api/settings` JSON) and is carried in the settings export (it's not a secret, unlike the key). The default (`GEMINI_MODEL_DEFAULT`) is **`gemini-flash-lite-latest`** — the id that live-probing found actually works on this account, returning a correct ID (Nøtteskrike/*Garrulus glandarius* @ 95%) in ~4 s. The alternatives all failed: `gemini-2.5-flash` is new-user gated even with billing; the whole 2.0 generation (`gemini-2.0-flash`/`-001`/`-lite`) 404s "no longer available" despite still appearing in ListModels; and the plain `gemini-flash-latest` alias resolves but returns sustained 503 "high demand" (the lite alias is the less-contended, cheaper sibling). Since the usable flash models are all **thinking** models, the body keeps `thinkingConfig.thinkingBudget=0` (disable thinking: a single-image ID gains nothing and it otherwise adds seconds of latency — an interim build that dropped it made flash-latest hang ~60 s per call); a non-thinking model would 400 on that, but that generation is retired. New `gmodel` field in `/api/settings` GET, `gmdl=` in POST (present-only; blank resets to default) + export + NVS. No route-count change.
- v2.10 — **Gemini reliability pass: model → gemini-2.0-flash, drop thinkingConfig, exponential backoff on 429/5xx (firmware 0.71.5), fixes §3.2.3.** Live testing on a new free-tier Gemini key surfaced three coupled realities. **(1) Model availability:** `gemini-2.5-flash` 404s "no longer available to new users" (even though it appears in that key's ListModels — v2.08), and the `gemini-flash-latest` alias (v2.08's fix for the 404) resolves but returns **sustained HTTP 503 "model is currently experiencing high demand."** Switched `GEMINI_MODEL` to **`gemini-2.0-flash`** (GA, available to every key, much better free-tier availability, vision + JSON `responseSchema`, cheap). Because 2.0-flash is a **non-thinking** model, the body's `thinkingConfig` was **removed** — sending it to a non-thinking model is a 400. **(2) Quota, not a bug:** the key then returned **429 "You exceeded your current quota"** — Google refusing at the account level (free tier has tiny limits; billing raises them). Every failed call still *reached* Google, so nothing was ever misclassified — it just never got an answer. **(3) 429 is a rate limit, retried with proper backoff:** the free tier is **10 RPM**, so the 429 is a per-minute window that clears within ~60 s — the fix is Google's recommended **exponential backoff**, not fail-fast (an interim build that dropped 429 from the retry set was wrong for a rate limit). `cu_retry_backoff` is now **2 s → 4 s → 8 s** (was a flat ~0.8 s × n) and `*_MAX_RETRIES` is **3** (4 attempts), for both 429 and 5xx; a 4xx other than 429 (bad key/model) still fails fast. A genuine *daily* quota exhaustion still 429s after all retries and degrades to the on-device model (§3.2). Not added: a proactive "≤1 request / 6 s" throttle — it would cap the paid tier the operator is moving to; revisit only if staying free-tier. The Test-button model listing stays (so `GEMINI_MODEL` can be re-pointed if 2.0-flash is ever deprecated). **Also observed (not fixed here):** a TLS handshake can fail with `ESP_ERR_HTTP_CONNECT` + mbedtls 0 + certflags 0 when internal-DRAM's largest free block is near the ~40-60 KB handshake peak (seen at ~44 KB under camera+classify load) — an out-of-memory masquerading as a connect error; watch `heapIntBig` (§5/§7). No API/route/UI change.
- v2.09 — **Cloud classifier auto-retries transient provider errors (firmware 0.71.2), hardens §3.2.3.** Both cloud tiers now ride through a provider's momentary overload instead of failing the call. Gemini returns **HTTP 503 "model is currently experiencing high demand"** (and 429 on rate limit) during traffic spikes, and Anthropic's edge can 429/5xx the same way; observed live within minutes of enabling Gemini. Each provider's request was split into a single-attempt `*_post_once()` (open → stream body → read reply) wrapped in a retry loop: on a **transient** status (`cu_status_transient`: 429/500/502/503/504) it backs off (`cu_retry_backoff`: ~0.8 s × attempt) and retries, up to **2 retries (3 attempts total)**; a deterministic 4xx (bad key, no credit, bad model) is **not** retried and is surfaced verbatim as before, and a transport failure (can't reach the host) also fails fast so an offline box degrades to the on-device model without stalling. The retry runs in the classifier / identify-worker task (never httpd or motion), so it can block briefly without affecting the UI or capture. Live-path semantics are unchanged where it matters: a call that still fails after retries degrades to the nordic model — never a dropped event (§3.2). New shared helpers in `cloud_util`; no API/route/UI change.
- v2.08 — **Google Gemini as a second, coequal cloud classifier — one active at a time (firmware 0.71.1), extends §3.2.3.** The v1.94/v2.05 cloud tier was Anthropic-only. Gemini now joins it as an interchangeable **secondary** classifier: both can hold a stored key at once, but exactly **one** identifies new events, chosen by a new provider selector. **Architecture.** The HTTP/JSON/TLS plumbing both providers share — the four-field JSON reader, the CSV/JSON metacharacter `scrub`, the `classify_result_t` decision (`to_result`), the `tls_detail` verdict, the DNS/TCP probes, and the streamed-base64 body writer — was lifted out of `claude.c` into a new **`cloud_util.{c,h}`** so the two request paths can't drift (a comma that shifts the fixed-column visit log, or a TLS code printed as garbage, are bugs this code already paid for once). New **`gemini.{c,h}`** mirrors `claude.{c,h}` against Gemini's `generateContent` endpoint (`generativelanguage.googleapis.com`, `x-goog-api-key`, `inline_data` image part, `responseSchema`/`responseMimeType` structured output, `candidates[].content.parts[].text` unwrap) on the **`gemini-flash-latest`** rolling alias — a pinned id (`gemini-2.5-flash`) 404s with "no longer available to new users" once Google closes that version to new keys, so firmware tracks the maintained latest-Flash alias instead (fast and cheap, the right trade for a per-event ID where the bill adds up); `thinkingConfig.thinkingBudget=0` disables Flash's default thinking (which otherwise eats the output-token budget and can return an empty candidate). The **Test Gemini** verdict lists the Flash model ids the key can actually use (parsed from ListModels), so a model-availability 404 is diagnosable without a serial console and `GEMINI_MODEL` can be overridden if the alias ever mis-resolves. A thin **`cloud.{c,h}`** dispatcher resolves the active provider and routes the cascade + UI, so `classify.cpp` and `web_server.c` never branch on which one is picked. **Settings.** `claude_enabled` (a bool) is replaced by **`cloud_provider`** (0 off / 1 Claude / 2 Gemini — matches `cloud_provider_t`), and a second key `gemini_key[128]` is added; both keys stay out of the settings export (billable secrets) and forgetting the *active* provider's key turns the selector off. NVS migrates the old `s_cld` boolean → `s_cprov` on load (a stored `s_cld=1` reads as Claude); new keys `s_cprov`/`s_gkey`. **The cascade is unchanged in shape** (`classify.cpp`): nordic primary → cloud secondary on "Unidentified bird", now via `cloud_active()`/`cloud_classify_file()`, tagging the row `source=` `claude` **or** `gemini`; the gallery provenance badge gains **`[G]`** (src=4). **UI/endpoints.** Settings' *Cloud Identification* section becomes provider-neutral: an **Active provider** dropdown plus a key block (key field + Test + Forget) for each of Claude and Gemini. The two Claude-specific routes are generalised to **`/api/cloud-file`** (manual ✨ identify — uses the active provider, or whichever has a key if none is selected) and **`/api/cloud-test?p=<1|2>`** (per-provider key/reachability probe); route count is unchanged (still 2). The Gallery ✨ button and the identify worker (`ident_job_t` gains a `provider` field) call whichever provider applies. No new firmware dependency; `main/CMakeLists.txt` gains `cloud_util.c`/`gemini.c`/`cloud.c`. **Why Gemini:** a second independent strong labeller both cross-checks Claude on hard birds and lets the operator pick on cost/availability, without changing the never-drop-work contract — a failed cloud call still degrades to the on-device answer (§3.2).
- v2.07 — **Visit log split monthly → per-day, + a "Today" Stats view (firmware 0.70.13), fixes §3.4/§6, resolves the v1.98 deferral.** The visit log was one **monthly** `visits-YYYY-MM.csv`, so every gallery load read the whole month (~2.2 s SD floor) and every relabel/confirm/set-ROI rewrote it (~6.5 s), and the v2.06 iNat batch scanned it too. Now each day is its own **`visits-YYYY-MM-DD.csv`**, so those operations touch only that day's rows. **(1) One path source of truth.** New `storage_visit_log_path(date, buf, sz)` builds `/log/visits-<date>.csv` (full `YYYY-MM-DD`, or `no-date`); the writer keys off `tm_now` incl. `mday`, and the ~10 former `%.7s` (YYYY-MM) sites across `storage.c` (relabel/relabel_batch/set_roi/reset_stats_day), `web_server.c` (gallery, roi-todo) and `classify.cpp` (recheck, iNat batch) all call it. The file enumerators (stats, `/api/labels/confirmed`, reset) already glob `visits-*`+`.csv`, so they pick up per-day files automatically. **(2) One-time crash-safe migration** (`storage_migrate_perday`, run once at `storage_init`): for each legacy `visits-YYYY-MM.csv` it splits rows **verbatim** into per-day files (ROI column untouched), verifies the row count matches, then renames the monthly to `visits-YYYY-MM.bak` (same-dir extension swap — never a cross-dir move, per the FATFS `rename()` gotcha; `.bak` has no `.csv` substring so all readers ignore it). Idempotent and safe: it deletes a month's per-day files before re-splitting (undoes a crashed run), and never removes the monthly until the split verifies — a mismatch keeps the `.csv` and retries next boot. **Captures are never touched; ROIs/labels are preserved 100%** (the operator's hard constraint). Stats-history parity is explicitly *not* required, so `STATS_MAX_LOGFILES` 12→**400** (now days, ~13 months; older all-time history beyond it is dropped, acceptable) and the file-name scratch is heap-allocated (400×40 too big for the httpd stack). **(3) Two Stats views.** `stats_collect_scoped(out, date)` scopes aggregation to one per-day file; `/api/stats/{daily,species,hourly}` accept `?scope=day` (clock-guarded: unsynced → all-time, never 1970); the Stats tab gains a **Today / All-time** toggle (default Today, one cheap file read). Additive — the untoggled path is the prior all-time behaviour. `.bak` backups linger until manually removed (a delete-backups action is a future add).
- v2.06 — **Optional periodic iNat re-scan: an off-by-default third classification tier via runtime model-swap (firmware 0.70.12), extends §3.2.3.** Phase B of the cascade. A new opt-in booster (`settings.inat_periodic_enabled`, default off; interval `inat_periodic_interval_min`, default 60, min 5) that periodically re-runs the **iNaturalist** model on the frames the nordic model + Claude left **unclassified/Unidentified**, restricted to the operator's 30 target species, and tags the result `source=inat`. **Why periodic, not live:** the device holds only one model in PSRAM (3 MB arena + ~3.5 MB model buffer; ~600-700 KB free heap), so a second model needs a **runtime swap** — reloading a model off the 1-bit SD costs ~10-13 s, which per-event would thrash while nordic knows only 4 species (nearly every non-target bird would fall back). Running it as a **batch** pays the swap once per cycle across many frames. **Why off by default:** iNat under-performs on this camera's domain (documented GBIF/zoom/backbone A/Bs), and a pass holds the classifier's run mutex for its duration, pausing live ID — so it's strictly opt-in. **Mechanics.** `classify.cpp`'s `model_init` was split into reusable **`model_load_named()` / `model_unload()`** that **reuse the 3 MB `s_arena`** (only the model buffer + labels + score scratch are freed/reloaded; the arena is never realloced, avoiding fragmentation), plus a shared op-resolver (`get_resolver`). A new low-priority **`inat_batch_task`** wakes every interval and, when enabled: skips if a manual recheck is running or live events are queued (yields the model), verifies `inat-birds-v1.tflite` is on the card, guards the pre-SNTP ~1970 clock, collects up to **25** of today's still-unclassified/un-corrected rows, then under the run mutex swaps nordic→iNat, scores each frame **whole-frame** (`detect_zoom` irrelevant — iNat isn't ROI-trained) keeping only the best class in the 30-set at/above the confidence threshold (`inat_score_target`), rewrites the confident rows via the existing `rc_rewrite_row` (tagged `source=inat`, ROI preserved), and **always swaps back to nordic** before releasing — a failed restore is logged loudly. Frames beyond the per-cycle cap, or events that arrive mid-batch, are picked up next cycle (self-healing; nothing is dropped). New Settings-tab control (checkbox + interval) with a plain-language warning; `inat`/`inatv` added to `/api/settings` GET/POST, the settings export, and NVS (`s_inat`/`s_inatv`). The Claude section's help text was corrected to describe the v2.05 **secondary** role (was still worded "instead of the on-device model"). Also adds **Garrulus glandarius (Nøtteskrike)** to the shared target list (`target_species.c` + the operator's Artsliste.txt, 30→31): it's in the iNat model and a plausible feeder visitor, so the Claude + iNat tiers and the relabel picker now cover it (the nordic model still needs retrain data before it can emit it). No route-count change. **Also fixes a latent settings bug surfaced by this work:** `h_settings_post` overwrote `g_settings.region` (the active-model selector) even when the POST carried no `region=` field — unlike `mode`/`tz`/`ntp`, which are guarded — so any *partial* settings POST silently blanked the model choice, and the device then auto-picked the first `.tflite` on the card (alphabetically `inat-birds-v1`) on the next boot. Added a `has_field()` presence check so `region` is applied only when the field is actually sent; an empty-but-present value (the Settings dropdown's "auto" option) is still honoured.
- v2.05 — **Classification cascade: nordic primary → Claude secondary, with per-visit engine provenance (firmware 0.70.11), extends §3.2.3/§3.4.** Reworks how an event is labelled. Previously (`classify.cpp` `classify_task`) Claude, when enabled, *replaced* the on-device model — Claude ran first and the local model was only its fallback. Now the **on-device nordic model is primary** and Claude is the **secondary**, consulted only when nordic can't identify the bird (its result is "Unidentified bird", i.e. below the confidence threshold) and only when Claude is enabled; a confident nordic species — or a confident "no bird" — is kept as-is, so hard cases escalate to the cloud but easy/empty frames don't spend an API call. A failed/declined Claude call leaves the nordic result in place, and if the local model is unavailable Claude alone is tried, so the row never drops work (§3.2). **The Claude tier is now constrained to the operator's 30 target species** (Artsliste): `build_prefix` (`claude.c`) lists them in the system prompt when the region filter is on, so the cloud can't return a geographically-absurd or off-target guess. The 30-species list is factored into a single shared source **`main/target_species.{h,c}`** (`TARGET_SPECIES[]` + `species_in_target()`), consumed by the relabel picker (`/api/labels`, replacing the inline `EXTRA_LABELS`), the Claude prompt, and — in a later phase — the iNat mask, so the three can't drift. **Provenance:** the visit log gains a 10th column **`source`** ∈ {`nordic`,`claude`,`inat`,``} recording which engine produced each label (`classify_task` + `recheck_task` line formats; appended so `stats.c` and the fixed-column scanners stay valid — human labels remain distinguished by the existing `corrected` column). `/api/events` emits it as `src` (0/1/2/3) and the Gallery shows a small `[N]`/`[C]`/`[I]` marker per tile. No route/UI-structure change beyond the marker; the optional periodic iNat third tier is a separate follow-up (deferred, off by default). This is **Phase A** of the three-tier design; the on-device iNat batch (model-swap) is Phase B.
- v2.04 — **Relabel picker: de-duplicate Lavskrike and offer the full target species list (firmware 0.70.10), fixes §3.2.1/§3.4.** Two reported bugs in the Gallery's "Set species" / relabel picker, both in `GET /api/labels` (the picker vocabulary). **(1) Lavskrike appeared twice.** The handler emits the model's in-region classes and then appended a hardcoded `EXTRA_LABELS` list of off-model hand-label targets. That list carried `Perisoreus infaustus` from when the v1 iNat model couldn't emit it — but **nordic-v0.5 now has Siberian Jay as a real class**, so it came out of both loops → a duplicate entry in the datalist. Fixed by **de-duplicating on the Latin binomial**: an `EXTRA_LABELS` entry is skipped when the model already emitted that binomial as an in-region class. **(2) The picker didn't match the operator's target list.** With nordic-v0.5 having only 4 classes, the picker offered just those 4 + Lavskrike, but the operator hand-labels a much wider set as retrain ground truth. `EXTRA_LABELS` is now the operator's full curated list — the 30 species of `Artsliste.txt` (Latin-keyed, in that file's order) — so every target species is a one-click pick whether or not the current model can auto-classify it; the dedup above means the 4 model species (Dompap/Bokfink/Skjære/Lavskrike) still show once. Four of the 30 (Heipiplerke *Anthus pratensis*, Heilo *Pluvialis apricaria*, Fjellerke *Eremophila alpestris*, Blåstrupe *Luscinia svecica*) were missing Norwegian names — added to `species_i18n.c`'s `NO_NAMES`, so they render localized in the picker and, being in the region set, are inference-ready if a future model emits them. Server-side only; no route, API-shape, or UI-markup change (the datalist JS is untouched).
- v2.03 — **Per-frame ROI logging: confirming a follow-up frame now recovers its own motion box, no manual backfill (firmware 0.70.9), extends §3.2.1/§3.4.** A visit is a burst of frames, but only the event's winning frame records an ROI in the visit log; a human who confirms a **follow-up** frame (very common in multiselect) got a new row with an **empty** roi, so those frames couldn't be ROI-crop training data without hand-backfilling. Yet the boxes already existed and were being thrown away — `capture_event()` re-runs `detect_once()` before every follow-up frame (motion.c) precisely so the zoom tracks a moving bird, then discards all but the classified first-`CLASSIFY_BEST_OF_N`=3. Fix: **persist every saved frame's box.** `capture_event_frame` now appends `file,roi` for each frame (all `capture_count`, not just the classified 3) to a small per-day sidecar **`/log/frameroi-YYYY-MM-DD.csv`** (`storage_log_frame_roi`). When `storage_relabel`/`storage_relabel_batch` hit the **no-row** case, they load that sidecar (`frameroi_load` into a PSRAM buffer, taken **before** the write lock so there's no self-deadlock; `frameroi_find` by filename) and write the frame's **own** box into the new row's roi column instead of leaving it empty. So confirming any follow-up now yields a training-ready, serve-matching crop automatically. Graceful for pre-0.70.9 captures (no sidecar → empty roi, exactly as before — the click-to-place backfill still covers those). Append-only, ~24 B/frame; the sidecar is **not** yet pruned when old captures age out (small, deferred). No API/UI/route change; the visit log's column layout is unchanged (the roi column is simply filled where it used to be blank).
- v2.02 — **Statistics reset is now non-destructive: decoupled from the gallery/training data (firmware 0.70.8), fixes §3.4.** "Reset statistics" **deleted every `visits-YYYY-MM.csv`** (`storage_reset_stats`), which is the single source of truth for **both** the Stats charts *and* every human-confirmed species label + motion ROI the gallery shows and the retrain pipeline exports — so a reset silently destroyed all training ground truth (the operator flagged this before ever pressing it: stats should be "activity since the last reset," not a wipe of the record). Fixed by a **reset epoch** instead of a delete: new setting `stats_reset_ts` (NVS `s_statrst`, "YYYY-MM-DDTHH:MM:SS"); `POST /api/stats/reset` now just stamps it with `now` and **deletes nothing**, and `stats.c ingest_line` skips rows older than it (ISO timestamps compare lexicographically, so it's a one-line `strcmp` filter). The visit log, `/api/events` (gallery), and `/api/labels/confirmed` (export) all keep reading the **full** history — so labels/ROIs are now permanent and only the counters reset. **Guard:** the reset requires an NTP-synced clock (`tm_year+1900 >= 2020`), else `now` would be ~1970 and filter nothing — it replies `{"ok":false,"error":...}` and the button shows why. The Stats tab now reads a `since` field on `/api/stats/species` and shows "**N bird visit(s) since &lt;date&gt;**", and the reset confirmation was rewritten from "permanently deletes … cannot be undone" to "start a new window … nothing is deleted." `storage_reset_stats()` is left defined but unused (a future explicit "delete all data" action). No change to per-day **Wipe day** (that still intentionally removes a day's rows). Settings struct grew one field; NVS namespace gained one key.
- v2.01 — **Gallery: follow-up frames of a visit inherit the event's label for display (firmware 0.70.7), extends §3.4.** A motion event classifies only its **trigger frame** (best-of-N writes one visit-log row); every later frame in the same burst is saved but lands **unclassified**, so a single bird visit shows as one labelled tile followed by a wall of grey "unclassified" tiles — which read as clutter / apparent misses (reported after the nordic-v0.4 ROI model went live: a confirmed Dompap burst where only frame 1 carried the species). Fix is **display-only**, in the gallery's inline JS: after loading a day, walk the frames in time order and carry the current visit's label forward onto consecutive unclassified frames within **15 s** (`GPGAP`), tagging each with `o.prop`. Such a tile renders the inherited name in a distinct dimmed/italic badge (`.gprop`, prefixed "≈") titled "same visit (inherited, not independently classified)", and the Show filters treat it as its visit's class — so the **"unclassified" filter no longer lists it** (it now shows only genuinely unlabelled/empty frames), while the *near-threshold* double-click-to-no-bird path explicitly excludes propagated tiles. **Nothing server-side changes**: the real per-frame `st`, the visit log, the retrain export, and Stats counts are all untouched (propagation is `dataset.prop`/`psp`/`pst` only, never `st`), so a visit is still one row for counting and relabel/confirm still operate on the true state. Known trade-off (accepted): a tail frame the bird already left inherits the bird's label — a display cosmetic, not a data error. This does **not** address genuinely empty light/shadow triggers (those stay unclassified); reducing those is motion-sensitivity/zone tuning, and model-based no-bird auto-hide is deferred (with detect_zoom on the model sees only the motion patch, not the whole scene, so it can't reliably judge "empty" — measured 1–30% diffuse output on empty crops).
- v2.00 — **Backfill-ROI editor now names each bird in the operator's language + Latin (firmware 0.70.6), refines §3.4/v1.99.** The v1.99 backfill worklist showed each row's species as the raw `corrected` value — the **canonical English common name** stored in the visit log (e.g. `Eurasian Magpie`), not the Norwegian display name — so the editor read "Eurasian Magpie" for what the operator had confirmed as Skjære, which looked like it might be surfacing un-confirmed model output. It wasn't: `h_roi_todo` only ever lists rows whose `corrected` column is set, i.e. human-confirmed birds (sentinels excluded), so the label was correct, just untranslated. Fixed by running the name through the same **`species_localize(corrected, latin, g_settings.lang, …)`** the Gallery/Stats/Live paths already use: `/api/roi-todo` now also parses the row's `latin` (binomial) field and emits `sp` as the localized name **with the Latin appended** — "Skjære (Pica pica)" under LANG_NO, "Eurasian Magpie (Pica pica)" under LANG_EN — so the operator sees exactly what they confirmed, disambiguated by the scientific name. Server-side only (the editor JS just renders `sp`); the progress line switched its `(sp)` wrapper to an en-dash to avoid nested parentheses. No route, write-path, or API-shape change; nothing about what gets stored or trained is affected.
- v1.99 — **Log the motion ROI on every visit row + a Maint tool to backfill it on old captures (firmware 0.70.5), extends §3.2.1/§3.4.** Prep for ROI-crop retraining (§3.2.1): the operator chose to train on tight bird-box crops, but an audit showed only **17% of confirmed images had a logged ROI** (872 of 4999), and those were biased toward Dompap — because the firmware only ever recorded the ROI when a *single zoomed frame* won best-of-N. When the multi-frame **pooled** result won (common), or the event was **unclassified** (exactly where model-missed rescue birds — Lavskrike/Bokfink/Skjære — land before a human relabel), `win_roi` was empty, so those rows carried no bird box even though `detect_once()` computes one for every event. **Two changes. (1) Firmware — always log the motion box.** `classify_task` now writes `job.rois[0]` (the trigger frame's motion cluster box, always populated) to the `roi` column whenever the aggregator's winning-crop ROI is empty — covering pooled wins **and** the unclassified branch — so every new row records where the bird was, independent of `detect_zoom` (which still only governs whether *inference* crops to it) and of whether the model identified it. A later human relabel preserves that box (`storage_relabel` keeps field 7), so rescue birds finally carry an ROI. This only helps captures going forward. **(2) Maint — click-to-backfill the existing 4127.** New **Backfill ROI** section: pick a day, `GET /api/roi-todo?date=` lists the day's confirmed real-bird rows still missing a box, and a focused editor shows each image where a **click sets the bird's centre** (a size slider sets the square's size, drawn as a live overlay). Save writes `x0-y0-x1-y1` to the row via **`POST /api/set-roi`** (+ `storage_set_roi()`, which replaces only the roi column, never adds a row); Skip advances. The box geometry matches the device's `decode_to_input` square-expand, so a backfilled ROI crops identically in training and on-device. Sentinel corrections (no bird / other / unknown) are excluded — those aren't birds. Two new routes (48/56 cap). **Training itself is deferred** until enough ROIs exist (new captures + this backfill); `train.py` is unchanged this version.
- v1.98 — **Gallery bulk "Set species" collapsed to one CSV rewrite; `/api/events` partially sped up (firmware 0.70.4), extends §3.4/§6.** Follow-up to v1.97, targeting the two SD-heavy gallery handlers still running inline on the single httpd task. **(1) Bulk "Set species" — the big win.** It fired one `/api/relabel` per selected image, and since each relabel rewrites the *entire* month's CSV, a 43-image op was 43 full rewrites of a ~600 KB file over the 1-bit SD (~4-5 min, and UI-blocking the whole time — the sluggishness seen live during a bulk classify). New **`POST /api/relabel-batch`** (+ `storage_relabel_batch()`) applies one species to many images in a **single** CSV pass: scan the month once, replace `corrected`+`latin` on every matching row, append a user-confirmed row for any selected image that had none — identical semantics to the single-image path, O(CSV + N) instead of O(CSV × N). The Gallery posts the selection in **300-file chunks** (one rewrite each, every body under the handler's 16 KB cap) driving the existing progress bar. Measured: 5 images in one 6.5 s rewrite vs ~32 s before; a 43-image set now ~6.5 s vs ~4 min. Route count 45→46 (cap 56). **(2) `/api/events` (loading a gallery day) — partial.** Two inline costs trimmed: the per-file `stat()` (whose only use was a cosmetic "· NN KB" tile label the operator confirmed is uninteresting — dropped, along with the `s` JSON field), and the one-`httpd_resp_send_chunk`-per-file pattern (now buffered, flushed at 4 KB, so a 2380-file day is a few dozen TCP writes instead of 2380). Measured **12 s → 8.3 s** on a 2380-file day. **Honest limit:** the rest is SD-bandwidth-bound, not fixable by trimming this handler — a **~2.2 s floor on every day** from reading the whole monthly `visits-YYYY-MM.csv` (confirmed: it's the same whether the day has 12 files or 2380), plus ~0.9 s to `readdir` a 2380-entry folder (measured ~0.37 ms/entry via `/api/days`). The real fix for both that floor *and* the 6.5 s batch-rewrite is **per-day log files** (`visits-YYYY-MM-DD.csv`) so neither operation touches a month's worth of unrelated rows — deferred as a larger change that also touches stats/export/reset. **Not touched:** the per-image ✎/✓/🔍/✨ paths, write semantics, the retrain export (`corrected` is still the confirmed flag), and bulk **Confirm** (still one `/api/confirm` per image — same-treatment candidate, but not the reported pain). `/api/events` JSON changed only by removing `s`; the Gallery no longer references it.
- v1.97 — **Fix: species-ID endpoints froze the whole web UI while they ran (firmware 0.70.3), fixes §3.4/§6.** Reported as "the SD card seems slow." It isn't: measured idle, a 136 KB capture reads off the card at ~309 KB/s — identical to the 69 KB index page served purely from flash — and `/stream` sustains ~147 KB/s from PSRAM. What's actually slow is intermittent, and it hits **every** endpoint at once: during a stall a 639-byte `/api/status` and the flash-only `GET /` both took **~8 s** (TTFB), while TCP `connect` stayed at 10-50 ms and the bytes flew once they started — so it's not throughput and not the SD, it's the server not answering. Root cause: **`esp_http_server` services every socket from a single task**, and four handlers ran their slow work *inline* on it — `h_classify_run`/`h_classify_file` block for a full on-device inference (**11-20 s** with the 5.5 MB TFLM arena at `SPIRAM_SPEED_40M`), and `h_claude_file`/`h_claude_test` block for a TLS handshake + Anthropic round-trip (~1-5 s). While any of these ran, the httpd task couldn't `select()` other sockets, so the entire UI hung for the duration — proven by forcing one `/api/classify-file` and watching an unrelated `GET /` stall for exactly the reported `durationMs`, then recover the instant it finished. Every Gallery 🔍/✨ identify did this. Fix: **offload all four to a worker task**, mirroring the `/stream` async pattern already in this file — `httpd_req_async_handler_begin` duplicates the request, a worker (`ident_task`) does the slow work and sends the response, and the httpd task returns immediately to serving sockets. The worker runs at **priority 4, below httpd's 5**, so even a CPU-bound inference can't starve request handling. **Single-flight** (`s_ident_busy`): one identify at a time — the TFLM path already serializes on classify.c's run mutex, and the Gallery's per-tile buttons disable in flight and fire one image at a time, so a second concurrent request is never the fast path; a collision returns `503 {"error":...}` (the tile prints it) rather than oversubscribing PSRAM (JPEG buffer + arena + TLS buffers). Handler bodies became thin validate-then-`ident_dispatch()` front ends; the on-device SD-file read also moved into the worker so the httpd task isn't even held for the file I/O (the POST `/api/classify` body is still received on the httpd task first — that's network-bound ~1 s, not the 20 s offender). No route, API-shape, or UI-markup change: the JSON replies are byte-identical, so the Gallery JS is untouched. **Note:** this does not make inference *faster* (still 11-20 s per frame, and the v1 iNat model remains a weak Nordic labeller, §3.2.1) — it stops one slow identify from freezing everything else.
- v1.96 — **Fix: a degraded boot silently rewrote the saved resolution, and the Debug tab reported the request as if it were fact (firmware 0.70.2), fixes §5.** Reported: the resolution had changed off **HD** on its own, showing as **"Current (2)"** in the Settings dropdown. "Current (N)" is the UI's fallback for a stored value not in the list (the selector has offered only HD=3 / SXGA=4 since v1.82), and **2 is XGA 1024x768** — a value the UI cannot produce. Root cause: `camera_init`'s **degrade ladder** (v1.21) steps down the RES table when a size won't initialize — right, it stops a bad size from failing boot and triggering OTA rollback — but it then did `g_settings.resolution = idx`, **overwriting the user's request with the fallback**. That was labelled "in RAM only, so the UI reflects reality", which underrates it: `/api/settings` serves that field, so the Settings tab selects the degraded value, and `h_settings_post` ends in an unconditional `settings_save()` that writes `s_res` straight from RAM. **Any** later save, from **any** tab (a Live-tab zone save, a sensitivity tweak — the resolution dropdown need never be touched), therefore **persisted the degrade to NVS permanently**. One transient bad boot plus one unrelated save silently destroyed a standing HD preference. **Why it degraded here (diagnosed, not the bug itself):** `camFault` was already true when a **`/ota/upload` reboot — a soft reset — inherited the wedged OV2640** (only a power cycle clears it, §3.5), so init failed at HD and fell to XGA; the next cold boot took HD first try with ~720 KB PSRAM free, so **capacity was never the issue** and the Claude classifier (§3.2.3) is not implicated. **Second bug, which is why this stayed invisible:** `camera_framesize_str()` returned `RES[res_idx()].str` — the *setting* — so the Debug tab's Resolution row read "HD 1280x720" while the sensor really ran XGA. The one field that could have exposed the degrade was echoing the value that was wrong; only v1.82's narrowed dropdown turned this into a visible artifact at all (pre-v1.82 a degrade showed as "XGA 1024x768" **selected**, indistinguishable from a deliberate choice — so this may well have been happening unseen). Fix: **split request from reality.** `g_settings.resolution` is now only ever the user's standing request (so every boot retries it, and no save can launder a fallback into NVS); what actually initialized lives in `camera.c`'s `s_active_idx`, exposed as `camera_active_res()` and reported by `camera_framesize_str()`/`camRes`. `/api/settings` gains **`resActive`** (RES index, `-1` = camera down) + **`resActiveStr`**, and the Settings tab shows an amber warning — *"Running at XGA 1024x768, not the size saved here…"* — that distinguishes the two ways they diverge (a change not yet rebooted into, vs. a size that failed at boot), naming the power-cycle-vs-soft-reboot distinction since that's the usual cause. A degrade also now logs a warning instead of passing silently. **Behaviour note:** `camera_recover()` shares this init path, so it now restarts the ladder at the *request* rather than at the previously-degraded size — a wedged sensor costs a few more `esp_camera_init` attempts per recovery, which is the right trade for a box that recovers back to the size you asked for. The "Current (N)" dropdown fallback is **kept**: units that already persisted a degraded 0-2 through this bug need it to stay visible and selectable. `/api/settings`' buffer went 700→800 B for the new fields — it `snprintf`s one JSON blob, and truncation there emits malformed JSON that takes the whole Settings tab down.
- v1.95 — **Fix: labels set on a busy day's newest images reverted to "unclassified" in the Gallery (firmware 0.70.1), fixes §3.4.** Reported: multiselecting 16 of the latest frames, **Set species → Dompap**, progress bar completing normally — then the same tiles reappearing unclassified on refresh. **The write was never the problem:** the label reached the visit log and `GET /api/labels/confirmed` returned it (so the retrain export (§3.2.2) had it all along) — only the Gallery's *read* dropped it. Root cause: `gal_build_labels()` loads the day's label table into a fixed `GAL_MAX_LABELS` array and stops at the cap; the reference unit's 2026-07-15 had **2527 captures** and `/api/events` returned **exactly 1199** labelled tiles — pinned against the 1200 cap. Because the month's CSV is read **front-to-back**, the rows past the cap are always the day's **newest**, which is precisely the range a user labels — so the bug hid until a day got big, then hit the freshest work first and looked like a failed write. The 1200 was itself the v1.79 raise of an earlier 300, chosen against *events* per day; the real bound is **captures** per day, because relabel/copy-last **append a row per no-row frame** (§3.4/v1.51), driving the row count toward one per image. Fixed by sizing for that: `GAL_MAX_LABELS` 1200→**6000**, affordable because the per-label cost dropped ~104→35 bytes — the localized species name is now **interned** in a small per-day pool (`gal_intern`, `GAL_MAX_SPECIES=96`; a day holds a handful of distinct species, and the old struct carried a full 72-byte copy on every row) and `confirmed` is derived from `state` rather than stored (`corrected` non-empty ⇔ states 2/4/5/6). Net ~210 KB of the one PSRAM allocation, against a ~550 KB largest free block (§7). `base` also trimmed 48→32 (a capture name is 27 chars — the v1.53 truncation trap, kept comfortably clear). **Truncation is no longer silent:** hitting the cap now logs a warning, since the whole failure mode was a legitimate label vanishing with no error anywhere. `/api/events` output is unchanged.
- v1.94 — **Cloud species identification via the Anthropic Claude API (firmware 0.70.0), new §3.2.3.** The v1 965-class iNat model is a weak labeller for Nordic species (§3.2.1), which makes building the retrain set (§3.2.2) a typing job: every image needs a human to supply the species, not just confirm one. Added an **opt-in cloud classifier** that asks Claude (`claude-opus-4-8`) to identify the bird, in two independently-gated places. **(1) Live path** — Settings → *Cloud Identification*, a toggle plus the user's own API key: when ON, each new motion event is identified by Claude **instead of** the on-device model (`claude_event()` in `classify.cpp`, called once per **event**, not per frame — best-of-N exists only to paper over TFLM's pose sensitivity, which Claude doesn't share, so scoring all frames would triple the bill for nothing). The whole frame is sent regardless of `detect_zoom`, for the same reason zoom hurts the iNat model (§3.2.1/v1.87): context (perch, feeder) is signal, and a tight crop throws it away. A failed call (no internet, bad key, no credit) **degrades to the on-device model**, and only if that's unavailable too does the row fall through to "unclassified" — never-drop-work (§3.2) is preserved end-to-end. **(2) Gallery ✨ button** — identifies one saved photo on demand; gated on a **stored key only**, not the toggle, so you can label training images without turning on billing for every live event. It mirrors 🔍 identify exactly, including writing the `corrected` column on a confident answer. **The live path deliberately writes `species` (state 1 = classified-by-model), never `corrected`** — so an unattended cloud label can never reach `dataset/<class>/`; the human's ✓/✎ still gates all training ground truth (§3.2.2). **Cost is the reason this defaults off:** roughly 1-2 US cents per event, so a busy feeder runs to a few dollars a day; the Settings copy says so plainly. **Key handling:** stored in NVS (`s_ckey`), never returned by `/api/settings` (which reports only `ckey_set`, same posture as `/api/wificfg` and SSIDs), and **deliberately excluded from the settings export/backup** (§5) — it's a billable secret, and an absent key reads as "keep current" on restore. Because an empty key box means "keep", deleting one needs its own explicit act: **🗑 Forget stored key** (`ckeyclear=1`), or a key could never be removed once stored. **`GET /api/claude-test`** validates reachability and the key against the free, unmetered `GET /v1/models` — no tokens spent — and reports a human verdict; a deployed box has no serial console, so this is the only way to tell a rejected key from a blocked network. New `claude.c`/`claude.h`; `main/CMakeLists.txt`, `settings.{c,h}` (`claude_enabled`, `claude_key[128]` — sk-ant- keys are ~108 chars, and a short buffer would truncate one into a silent 401), and two routes (`/api/claude-file`, `/api/claude-test`) which took `max_uri_handlers` 48→56 (44 in use; past the cap registrations are **silently dropped** and the endpoint 404s). **Implementation notes for future edits:** the request body is **streamed** — base64's encoded length is exactly `4*ceil(n/3)`, so Content-Length is computable up front and the JPEG encodes straight out of its PSRAM buffer in 3 KB chunks, costing one JPEG-sized alloc instead of three (free heap is only ~600-700 KB, §7). Opus 4.8 **removed the sampling parameters** — sending `temperature`/`top_p`/`top_k`/`thinking.budget_tokens` is a hard **400**, so determinism comes from the JSON output schema, not `temperature:0`; `thinking` is omitted entirely, which on 4.8 means no extended thinking (right for a single-image ID: it would add latency and cost per bird event for no accuracy gain). Every name the model returns is **scrubbed** of CSV/JSON metacharacters at the boundary (`scrub()`) — the visit log is fixed-column CSV and the replies are hand-assembled, so a comma in "Blue Tit, juvenile" would shift every later column; the schema makes that unlikely, not impossible.
- v1.93 — **Fix: the CA bundle can't validate a chain whose trust path runs through a cross-signed root (firmware 0.70.0), unblocks §3.2.3.** Every TLS connection to `api.anthropic.com` — and to `generativelanguage.googleapis.com`, tried first — failed at handshake with `ESP_ERR_HTTP_CONNECT`. DNS resolved correctly, raw TCP 443 connected, the clock was NTP-synced, the ciphersuites/curves were all present, and the bundle was already `DEFAULT_FULL` — so the network, the clock and the crypto were all exonerated. Root cause: **`CONFIG_MBEDTLS_CERTIFICATE_BUNDLE_CROSS_SIGNED_VERIFY` defaults to `n`**, and `api.anthropic.com` serves `leaf -> GTS WE1 -> GTS Root R4` where the R4 it sends is **cross-signed by GlobalSign Root CA**. Without that option `esp_crt_bundle` cannot walk a chain whose root is cross-signed, and its callback returns `MBEDTLS_ERR_X509_FATAL_ERROR` (**-0x3000**) **without setting any cert-verify flag**. The failure therefore surfaces as `certflags 0x00000000` — which reads exactly like *"the certificate verified cleanly"* and sends you hunting the network instead of the trust store; that misreading cost the whole first debugging pass. Fixed by enabling the option in `sdkconfig.defaults` (~700 B heap; needs a regenerated `sdkconfig`). **Not vendor-specific** — anything behind Google Trust Services presents the same cross-signed shape, so this is not a line to "clean up" when swapping API vendors. **Scope, stated precisely (an earlier draft of this entry overreached and claimed outbound HTTPS had never worked at all — it had):** the v1.88 GitHub-release OTA path (`esp_https_ota`, §8) was **verified working on 0.68.0** and was never broken by this, because its chain terminated at a root the bundle holds directly. Only a chain that *needs* the cross-signed hop fails. Worth knowing for the future, though: as of 2026-07-15 both `github.com` (Sectigo Root E46 cross-signed by USERTrust ECC) and `release-assets.githubusercontent.com` (ISRG Root YR cross-signed by ISRG Root X1) now serve cross-signed roots too, so whether §8 keeps working depends on which roots the bundle happens to hold as CAs rotate — this option makes that path robust rather than lucky. **Diagnostics kept**, because none of this was visible from `ESP_ERR_HTTP_CONNECT` alone and a deployed box has no serial console: `claude_test()` separates DNS from TCP from TLS (`resolve_api_host()`, plus a **non-blocking** `tcp_probe()` + `select` so a blackholing firewall can't hang the httpd thread for ~75 s), and `tls_detail()` reports the mbedTLS code, the verify flags and the failing stage, with -0x3000 explicitly annotated as *"no trusted root for this chain in the CA bundle"*. Note the sign convention that also cost time: esp-tls stores the mbedTLS code **negated**, so it must be printed raw — an earlier `(unsigned)-tls_err` printed `0xFFFFD000`, which reads like garbage. A "retry without cert verification" A/B probe was written and then **removed**: `CONFIG_ESP_TLS_INSECURE=n` means esp-tls refuses to set up TLS with no CA at all, so that probe failed at *setup*, proved nothing, and its "fails both ways" verdict was actively misleading — and a key-bearing request must never be retried unverified regardless.
- v1.92 — **Gallery: filter confirmed birds by a single species (firmware 0.69.0), extends §3.4.** The "Confirmed (bird)" Show filter lumped every confirmed species together, so on a busy day you couldn't isolate, say, just the Bokfink (Fringilla coelebs) visits. Added a **species sub-filter**: a second dropdown (`#gspecf`) that appears only when the Show filter is set to Confirmed (bird), populated from the distinct confirmed-species (`st===2`) names present on the loaded day (`fillSpecFilter()`), with an **All species** default. Picking a species narrows the grid — and the "shown" count in the tally — to confirmed tiles of that species; the selection is preserved across day reloads when the species is still present, and reset when you leave Confirmed mode. Pure client-side (inline JS/markup in `web_server.c`, same pattern as the existing filters) — no new firmware route or API field; it reads the species already carried per tile (`dataset.sp`, from `/api/events`). Prompted by wanting to review all confirmed captures of one species (e.g. for retrain-set curation).
- v1.91 — **Gallery thumbnails match the 16:9 capture aspect (firmware 0.68.3), fixes §3.4.** Thumbnail tiles were locked to `aspect-ratio:4/3` with `object-fit:cover`, a stale leftover from before HD (16:9) became the locked capture default (v1.80). A 16:9 frame is wider than the 4:3 box, so `cover` filled by height and **cropped the left/right edges** — birds near the frame sides could sit partly outside the thumbnail. Changed the tile box to `aspect-ratio:16/9` so it matches the HD capture shape and `cover` has nothing to crop; the full frame width is now visible in the thumbnail. One-line CSS change in `web_server.c`; the full image (click/👁 to open) was always uncropped. (Legacy 4:3/SXGA-5:4 captures would now crop a little top/bottom instead, an acceptable trade since HD 16:9 is the shipped default and recommended resolution — §1.82.)
- v1.90 — **Gallery toolbar + tally layout cleanup (firmware 0.68.2), refines §3.4.** The gallery toolbar was one non-wrapping flex row holding the day selector, seven action buttons, the filter, *and* the counts span, so on a narrow window the buttons shrank to their text (all different widths) and the counts span got squeezed into a one-word-per-line column ("961 / captures / · 504 / confirmed / …") — hard to read. Three fixes, UI-only: **(1)** the action buttons moved into their own responsive CSS grid (`.gacts`, `repeat(auto-fill,minmax(160px,1fr))`) so every button is the **same size** and they wrap into tidy rows instead of squishing; **(2)** the counts became an **aligned label/value table** (`renderTally()` builds a `.gtallytab` with right-aligned tabular-nums values) instead of a `·`-joined run-on sentence — captures / confirmed / classified / no bird always, plus conf. no-bird / other / unknown / shown only when non-zero/filtered; **(3)** the filter-specific hints ("double-click a tile = no bird", the near-threshold band) drop to a small caption line under the table. Two Recheck button labels shortened ("Recheck species (day)"→"Recheck (day)") to keep the grid compact. No backend, route, or API change — pure markup/CSS/JS in `web_server.c`.
- v1.89 — **Fix Gallery multiselect delete silently dropping most of a large selection (firmware 0.68.1), fixes §3.4.** Reported: selecting all 285 unclassified frames on a day and hitting **Delete selected** appeared not to delete them all. Root cause in `h_captures_delete_batch` (`POST /api/captures/delete`): it read the request body with a **single** `httpd_req_recv()` and null-terminated at that length, whereas the multi-select list is sent as one `files=a.jpg,b.jpg,...` body — ~8 KB for 285 filenames — which TCP splits across several segments. `httpd_req_recv` returns only the first segment (~1.4 KB), so only the ~50 filenames that fit were parsed and deleted; the rest were silently skipped, and the JS ignored the returned `deleted` count so no error showed. This is the exact empty-fields/partial-read class of bug the other batch handlers (`/api/relabel`, `/api/confirm`, `/ota/from-url`) already guard against with a **recv loop** — this handler was the one that didn't. Fix: loop `httpd_req_recv` until the whole body (up to the 16 KB cap, ~560 files) is read, matching the sibling handlers; and defensively, `gDelSel()` now compares the server's `deleted` count to the number requested and alerts *"Deleted N of M — reload and delete the rest"* if they differ, so any future truncation can never again pass silently. The whole-day **Delete all** / **Wipe day** paths (Maintenance tab, `all=1`) were never affected — they delete the day-folder server-side and carry no file list. No API or UI-layout change.
- v1.88 — **Flash a GitHub release directly from the OTA tab (firmware 0.68.0), extends §8.** No-PC updates: the OTA tab now lists this repo's published releases and flashes a chosen one over the air, with a progress bar. **Why device-side download:** the GitHub *API* sends `Access-Control-Allow-Origin: *` so the **browser** can list releases, but the release *asset* redirects to `release-assets.githubusercontent.com` with **no CORS header**, so the browser can't read the `.bin` bytes to re-`POST` them — the **device** must fetch it. So the browser fetches `api.github.com/repos/SEspe/BirdBox/releases`, populates a dropdown (all releases, running version marked), and `POST`s the picked asset URL to a new **`POST /ota/from-url`**; the device downloads+flashes it via `esp_https_ota` in a background task, and **`GET /ota/from-url`** reports `{state,read,total,msg}` for the progress poll. **Security — repo-locked:** the endpoint rejects any URL not starting with `https://github.com/SEspe/BirdBox/releases/download/`, so it can only ever flash this project's own signed releases, never an arbitrary binary. Reuses the existing dual-partition + rollback safety (a bad image reverts on next boot, same as `/ota/upload`). **Memory (measured, v1.87):** TLS peak ~40-60 KB lands on internal DRAM, of which ~180 KB is free and steady even under camera+classify load (both run in PSRAM) — comfortable; `CONFIG_MBEDTLS_DYNAMIC_BUFFER` keeps the peak small and the full CA bundle validates GitHub's cert. **Build:** adds `esp_https_ota`/`esp_http_client`/`mbedtls` to `main` and enables `CONFIG_MBEDTLS_CERTIFICATE_BUNDLE` (+dynamic buffer) in `sdkconfig.defaults` — the cert bundle is ~64 KB flash (69%→still ample free) and needs a clean rebuild (regenerate `sdkconfig`). `esp_http_client` `buffer_size_tx` is bumped to 4 KB because the signed redirect URL is ~900 chars. Manual `.bin` upload path is unchanged.
- v1.87 — **Report internal-DRAM and PSRAM heap separately in `/api/sysinfo` + Debug tab (firmware 0.67.0), extends §5.** Diagnostic groundwork for two upcoming decisions: (a) the "flash a GitHub release directly" feature, whose only real cost is the ~40-60 KB **internal-DRAM** peak of a TLS handshake, and (b) trying a larger/enhanced species model, whose TFLM arena is a large single **PSRAM** allocation. `esp_get_free_heap_size()` (the existing `heap`/`heapMin`) lumps both pools together, so it couldn't answer either question. Added four fields — `heapInt` / `heapIntBig` (free internal DRAM + its largest contiguous block, via `heap_caps_get_free_size`/`get_largest_free_block(MALLOC_CAP_INTERNAL)`) and `heapPsram` / `heapPsramBig` (same for `MALLOC_CAP_SPIRAM`) — and surfaced them in the Debug tab's System panel (internal DRAM flagged red under 40 KB, amber under 80 KB; PSRAM shown with its largest block, which is what a model arena must fit into). The largest-block figures expose fragmentation, which matters for both the TLS buffers and the model's single big alloc. Read-only; no behaviour change.
- v1.86 — **Progress bar for the Gallery bulk-label ops (firmware 0.66.0), extends §3.4/v1.84.** The v1.84 bulk actions (Confirm selected / Set species selected) run **strictly sequentially** — one `/api/confirm` or `/api/relabel` round-trip per image, so the tiny ESP32 httpd socket pool isn't fanned out — which means a large selection (a busy feeder day can be hundreds of frames) takes real time, and the only feedback was a small `i/N` counter tucked in the status line. Added a proper determinate **progress bar** below the gallery toolbar: `gSeq` now shows a filling bar with a `"<label> – done / total (pct%)"` readout (label "Confirming" or "Setting species"), updated after each image completes, and hides itself ~0.5 s after the batch finishes. Pure UI (new `.gprog`/`.gprogbar`/`.gprogfill` styles + a `gProg()` helper); no change to the sequential request pattern, the endpoints, or the write path — just visible progress so a long bulk-label run doesn't look hung.
- v1.85 — **Global-illumination compensation kills auto-exposure false triggers (firmware 0.65.0), fixes §3.1.** Reported live: motion firing on the empty feeder — repeatedly, and *the moment the live view was opened*. Investigation (pulling the flagged frames) showed no bird, just a whole-frame **brightness swing**: opening the live view starts `/stream`, the OV2640 re-converges its auto-exposure/gain, and because motion detection differences frames from that *same single sensor stream*, the brightness step reads as motion. The existing defenses — the dual-EMA background (fast ~1 s / slow ~11 s) and the 20-cell whole-frame cluster cap — only stop *gradual* drift and *uniform* whole-frame changes; a **sudden, non-uniform** AE swing (highlights on the bright stump clip while the dark shed barely moves) forms a bird-sized cluster on the bright region that slips past both and triggers. Fix in `motion.c`: before the fixed-threshold per-pixel test (`PIX_DIFF_THR=25`), compute each background's **frame-mean brightness delta** (`shift_fast`/`shift_slow`) and subtract it — `abs((cur-bg) - shift) > THR` — so a uniform step cancels to ~0 everywhere while a real *local* mover (a bird changes only a small fraction of the frame, so it barely moves the mean) still stands proud. Plus a guard: a frame whose mean shift exceeds `GLOBAL_STEP_THR=12` gray levels is deemed a lighting event and **cannot trigger**, even if the clipping residual still clusters; the normal quiet-frame EMA roll re-seeds the background so detection recovers within ~1 s. Bird sensitivity is essentially unchanged (a compact bird blob shifts the frame mean only ~1-3 levels, far below the guard). Independent of the zone mask and sensitivity (operator-owned, untouched) — those don't help a swing that hits every cell. Prompted by evening false positives on the reference unit, several coinciding exactly with opening the live view.
- v1.84 — **Gallery bulk-label ops + destructive day-ops moved to a Maintenance tab (firmware 0.64.0), extends §3.4.** Two coupled changes to make human labelling faster and safer. **(1) Bulk labelling on the selection:** the Gallery already had checkbox multiselect (Select all / Delete selected / Recheck selected), but confirming or setting a species was still one tile at a time — painful when a run of frames is all the same bird. Added two selection-scoped actions to the gallery bar: **✔ Confirm selected** accepts each selected tile's model guess as ground truth (loops `/api/confirm`; tiles with nothing to confirm are skipped), and **🔖 Set species▾** opens an inline datalist picker (same vocabulary as the per-tile ✎, via `/api/labels`/`ensureSpecs`) and applies one chosen species to every selected tile (loops `/api/relabel`, and seeds `g_lastSp` for the 📋 copy-last fast-path). Both write the visit-log `corrected` column, so they feed the retrain export exactly like the single-tile paths (§3.2.2). No new firmware endpoints — the existing per-image handlers are reused. Requests are issued **strictly sequentially** (`gSeq`), never a fan-out, because the ESP32 httpd has only a handful of worker sockets; progress shows as `i/N…` in the count line. **(2) New Maintenance tab:** the whole-day destructive buttons — **Delete all photos** and **Wipe day (photos+stats)** — moved out of the Gallery bar (where they sat next to the labelling controls and could be mis-clicked) into a dedicated **Maint** tab with its own day selector. Behaviour is identical (same `POST /api/captures/delete` with `all=1` / `all=1&stats=1`); only the placement changed, plus in-page result text instead of a modal alert. Client-side/UI only — no route-table or backend change.
- v1.83 — **Gallery "Select all" respects the active filter (firmware 0.63.0), fixes §3.4.** Bug: with a Show filter applied (e.g. unclassified / near-threshold / other), **Select all** selected *every* image for the day, not just the visible filtered subset, and the "N selected" count reflected the full day — so a follow-up Delete/Recheck could act on hidden images. Root cause: `gSelAll()` and the counter iterated `gChecks()` (all `.gchk` in the grid) irrespective of each tile's filter visibility. Fix: selection is now scoped to the visible subset — `gSelAll()` toggles only currently-shown tiles (new `gVisChecks()` helper), and `applyFilter()` **deselects any tile it hides** and refreshes the count. Net effect: selection is always a subset of what's on screen, so **Select all → count == filtered subset**, and Delete/Recheck-selected can only ever touch visible tiles. Whole-frame "all" filter behaviour is unchanged.
- v1.82 — **Resolution selector offers just HD and SXGA (firmware 0.62.0), refines §5.** The Settings resolution dropdown previously listed VGA/SVGA/XGA/HD/SXGA; for a bird-photo device the sub-HD sizes aren't useful choices, so the UI now presents only the two that matter, each annotated with its trade-off: **HD 1280×720 — recommended (best motion)** and **SXGA 1280×1024 — +vertical detail, weaker motion**. Help text spells out that HD and SXGA share the same 1280 px width (so the bird gets identical horizontal detail; SXGA only adds *vertical* rows), that HD keeps motion detection most responsive while SXGA's larger frames slow the detect loop (§3.1), and that nothing higher is offered because UXGA leaves no PSRAM for the on-device classifier (§camera.c). Backend unchanged — the `res` field still clamps 0–4 and `res_idx()` still falls back to SVGA for stale/out-of-range values, and `stLoad` appends a "Current (n)" option so a box on a legacy sub-HD value still shows its setting. UXGA remains off the table by design.
- v1.81 — **Document the SXGA-vs-HD motion-detection penalty (§3.1); no firmware change.** Records the on-device A/B (2026-07-13, firmware 0.60.0→0.61.0): running the camera at **SXGA (1280×1024) degrades motion detection vs HD (1280×720)** and there is no clean fix on the OV2640. Motion runs on the *same* single sensor stream as capture (no separate low-res detect feed), differencing each JPEG decoded at 1/8. SXGA hurts two ways: **(a) sensitivity** — larger frames are slower to grab+decode, so the fixed-period detect loop (`DETECT_PERIOD_MS`) samples less often and misses quick hops; SXGA also sits at the *exact* 1/8 decode-buffer ceiling (160×128, zero headroom) and costs ~300 KB more PSRAM (measured ~547 KB free at SXGA vs ~854 KB at HD), which correlated with a spontaneous OOM reboot; **(b) framing** — SXGA's 5:4 field of view is taller/narrower and mis-aligned to a horizontal feeder, where HD's 16:9 keeps birds in-view. No dual low-res-detect/high-res-capture stream exists on this sensor, and per-frame res-switching is too slow and its FOV change would break the ROI-crop box — so **HD is the locked default** (v1.80/0.61.0). SXGA's only edge (extra pixels for the classifier) is dissolved by ROI-crop (§3.2.3), which maximizes pixels-on-bird from HD anyway. Perceived still quality is recovered instead via JPEG quality (lower `stream_quality` number). See §3.1.
- v1.80 — **Parameter-defaults cleanup + Settings backup/restore (firmware 0.61.0), extends §5.** Two parts. **(1) Compiled-in defaults** were retuned to the operating point this project settled on, so an NVS wipe (portal re-provision / boot-button erase) lands on sane values instead of stale ones: `resolution` **SVGA→HD (1280×720, index 3)** — with ROI-crop making classification aspect-independent (§3.2.3), we lock one high-detail resolution and stop tuning aspect ever again; `capture_interval_ms` **1000→1500**; `cooldown_s` **10→3**. `capture_count` stays 5. `detect_zoom` stays **0** (whole-frame) — it flips ON only in the same step a ROI-trained model ships (§3.2.1), never before, or it degrades the stock model. Deployment-specific values (`detect_zone`, `motion_sensitivity`, `confidence_pct`, `rotation`) are deliberately **not** baked as globals — they're per-scene/per-mount and belong to the operator; the backup/restore feature below is the durable way to preserve them across a wipe. **(2) Settings backup/restore:** `GET /api/settings/export` streams every setting as one form-urlencoded blob (`birdbox-settings.cfg`, `Content-Disposition: attachment`) — byte-identical to the body `POST /api/settings` accepts. Restore is the Settings-tab **Restore from file** button POSTing that file straight back, so the restore path reuses the existing handler's full validation/clamping rather than a second, drift-prone parser. `tz`/`region`/`ntp` are emitted raw (their only special chars `,./-` survive `url_decode`). Lets a tuned box (zone mask, sensitivity, confidence) recover its config in one click after a reset without hand re-entry.
- v1.79 — **Log the motion `roi` on every event + expose it in `/api/labels/confirmed` (firmware 0.60.0), enables ROI-crop training (§3.2.1/§3.2.2).** Two coupled changes. **(1)** The visit-log `roi` column was only written when `detect_zoom` was ON (`classify.cpp`), so with zoom off (the shipped default since v1.75) the box `motion.c` computed was thrown away — every historical row has an empty `roi`. Now the ROI is logged **unconditionally** (it's field data + training input; `detect_zoom` still only controls whether *inference* crops to it). Applies to both the live event and recheck log-write paths. **(2)** `/api/labels/confirmed` now emits `"roi":"x0-y0-x1-y1"` (fractional, empty = whole frame) per record. Together these let the export carry the bird's box so `train.py` can crop each image to it — the same square-expand-on-ROI-center geometry `decode_to_input` uses on-device — training the classifier on **ROI-crops (bird-centered squares)** to match the `detect_zoom` inference path and gain position/scale/aspect invariance (a square around the bird never depends on the camera's frame aspect). **Note:** only *new* captures get a logged ROI; the existing 4-day dataset stays empty-ROI (logged before this fix), so an immediate v0.4 ROI-crop retrain needs offline ROI re-derivation. Backward-compatible additive API field.
- v1.78 — **Live view highlights the exact 8×8 grid cell(s) that triggered a detection (firmware 0.59.0), extends §3.1/§3.3 and the v1.77 motion border.** The v1.77 border flash showed *that* motion fired but not *where*. Motion detection already computes a per-cell `moved[]` map over the 8×8 grid and picks the largest 4-connected cluster as the trigger (`motion.c`); that winning-cluster cell mask is now captured into `s_trigger_cells` and exposed via `motion_trigger_cells()`. `/api/motion` gains `"c"` — a 64-char '1'/'0' mask in the same bit order as the settings `zone` — and the live view overlays a non-interactive 8×8 grid (`.motgrid`, `pointer-events:none`, below the zone editor) that highlights exactly the cells that fired, in red, for the same 1 s as the border flash. This doubles as a **detection debugging tool**: you can see which boxes register motion, whether the detection `zone` mask still aligns with where subjects land (relevant after a resolution/FOV change), and why large/whole-frame motion doesn't trigger (the winning cluster is capped at `MAX_CLUSTER_CELLS=20` — hand-close-to-lens fills too many cells and is rejected as wind/foliage). Cell mask is row-major in raw-frame space (same as the zone editor), so like the zone editor it doesn't compensate for 90/270° display rotation. Prompted by the user wanting to see *which* grid boxes contribute to a detection, after finding motion hard to trigger during testing.
- v1.77 — **Live view flashes a red frame border for 1s on each motion detection (firmware 0.58.0), extends §3.3.** The live view already drew a steady dim-red outline (`.liveWrap.det`) while an event was capturing, but it was driven by the 2 s `/api/status` tick — too coarse to show *when* a detection actually fired, and it tracked the whole capture window rather than the trigger. Added a crisp per-trigger indicator: a new tiny `GET /api/motion` (`{"n":trigger_count,"a":active,"q":quarantineS}`) that the live tab polls at ~2 Hz (500 ms) **only while the live pane is open**; a rising `n` (a new `motion_trigger_count()`) flashes a distinct bright-red border (`.liveWrap.mot`, 5px `#ff2f2f` + inner glow) for exactly **1 s**, then clears (successive triggers re-arm the 1 s timer, so continuous motion holds it lit). Kept separate from `/api/status` so the fast poll stays cheap (64-byte response). The pre-existing steady `.det` outline is unchanged; the new flash stacks brighter on top during the trigger. Client edge-detect seeds its baseline on the first poll so a stale count never flashes on page load. Prompted by the user wanting to *see* which moments contribute to motion detection in the live view.
- v1.76 — **Two new human "reject" states: `other` (hard negative) + `unknown` (excluded) (firmware 0.57.0), extends §3.4/v1.60.** The v1.60 model had only one negative at the not-a-species end — *empty frame* (confirmed no-bird → `no_bird/`). It couldn't express *"something is here but it isn't a bird I can name."* User request: split that gap into two, because the sub-cases don't train the same way. **Key rule: anything that IS a bird can never be a hard negative** — a hard negative teaches the detector "suppress this," so putting a bird there poisons detection in exactly the hard poses (motion, low light, distance) we most want caught. So: **state 5 `other`** (cat, sheep — a real non-bird subject) → a human tag `corrected="other"` → exports to a `dataset/other/` **hard-negative distractor** class, kept distinct from empty-frame `no_bird/`. **State 6 `unknown`** (a bird IS present but unidentifiable, or too blurry to use) → `corrected="unknown"` → **excluded from the export entirely**: it's neither a usable species positive (no label) nor a valid negative (it's a bird). Unknown rows are kept *tagged* on the device (recoverable later — a better crop or an improved model may ID them) but `export-labels.ps1` skips them (prints an `excluded` count). Both reuse the existing sentinel-string `/api/relabel` mechanism (no new endpoint): added to the ✎ picker as **"Other (not a bird)"** / **"Unknown bird"** — the picker, not a second double-click gesture, to avoid colliding with the v1.62 double-click-=-no-bird. New badge colours (`.goth` purple, `.gunk` amber), two Show filters (**Other (not a bird)** / **Unknown / bad bird**), and `nO`/`nU` status-line tallies. The per-image state model is now **seven** (0 unclassified, 1 classified, 2 confirmed species, 3 no-bird review, 4 confirmed no-bird, 5 other, 6 unknown). Accepted trade-off: blurry birds land in the same `unknown` bucket as unidentifiable-but-sharp birds — both are excluded from training anyway, so they're one user-facing bucket for now; split later only if the unknown-species pile is worth mining. `web_server.c` state derivation + inline JS/CSS/filters; `export-labels.ps1` mapping.
- v1.75 — **Default `detect_zoom` to OFF (firmware 0.56.0), §3.2.1.** The compiled default for `detect_zoom` was `1` (ON), a stale leftover from before we found that motion-region cropping *hurts* the v1 965-class iNat model: tight crops of a bird frequently classify as "no bird" because the model is whole-frame-biased, pose-sensitive, and underconfident on close crops — feeding it the full frame classifies noticeably better. A saved `dzoom=0` normally masked the bad default, but a fresh unit (or an NVS erase — e.g. the boot-button credential reset, or the v1.73/74 portal testing that wiped settings) would silently come up with zoom ON and degraded classification. Flipped `settings.c` default to `0` so new/reset units start with the setting that actually works for the shipped model. No behaviour change on units that already saved `dzoom=0`. Revisit when a Nordic-retrained model lands (§3.2.2) — zoom may help a model trained on tighter framing. Noticed by the user after the portal-test NVS erase reverted the reference unit to defaults.
- v1.74 — **Fix portal IP never appearing — wait for the phone to fetch it before rebooting (firmware 0.55.0), §4.4.** The v1.73 "show the DHCP IP" screen never actually showed on the reference unit: the setup page displayed "Connecting to VK24…" and then nothing. Root cause is the single-radio ESP32 APSTA channel constraint — the SoftAP starts on channel 1, but when the STA associates with the home AP on a different channel (VK24 is on ch6; serial showed `new:<6,0>, old:<1,0>`) the one radio must move the SoftAP to the STA's channel, which **kicks the phone off `BirdBox-Config`**. The box then got its IP and rebooted on v1.73's blind 12 s timer, long before the phone (which auto-rejoins the AP — now co-channel with the STA — a few seconds later) could poll `/api/portal-status`. Fix: the portal loop no longer reboots on a fixed timer after `GOT_IP`; it waits for `s_portal_ip_seen` — set when `wifi_portal_status()` serves a `CONNECTED` result, i.e. the phone has rejoined and actually fetched the address — then runs a `PORTAL_SHOW_MS` (10 s) on-screen countdown and reboots. Capped by `PORTAL_SEEN_MS` (60 s) so a phone that never returns can't hang the box in the portal. The setup page's "Connecting…" state now also warns that the phone may briefly drop off `BirdBox-Config` and to reconnect to it, and the page's 10 s countdown is now aligned with the firmware's (both start the instant the page reads the IP). Verified the v1.73 backend was already correct (creds saved, box came up on the LAN at `192.168.10.236`); this makes the address reliably visible.
- v1.73 — **First-boot portal shows the box's DHCP IP + "DHCP" hint (firmware 0.54.0), §4.4.** After entering WiFi credentials at the `192.168.4.1` config portal the box just rebooted and vanished onto the LAN — the user had to hunt for its new address with an external scanner (Fing) before they could open the dashboard. Now the portal *verifies* the submitted credentials while the SoftAP is still up (it's in APSTA mode) and reports the acquired address back to the setup page. Flow: on submit, `start_config_portal` forces the STA netif to DHCP (so a stale static from a prior location can't report a wrong, out-of-subnet address), applies the entered creds, `esp_wifi_connect()`s, and waits up to `PORTAL_VERIFY_MS` (25 s); the `GOT_IP` handler, instead of rebooting immediately as before, captures the IP into `s_portal_state`/`s_portal_ip` when a portal verify is `PENDING`. New `GET /api/portal-status` (`{"state":"idle|pending|connected|failed","ip":"…"}`); the setup page intercepts the form, POSTs via `fetch`, polls the status, and on success shows **✅ Connected — http://<ip>** as a tap-through link with a 10 s "reconnect your phone to home WiFi, then reboot" countdown before the box restarts onto the LAN (`PORTAL_SHOW_MS`, 12 s). A failed verify (mistyped password / AP unreachable) surfaces "❌ couldn't connect — try again" and drops back to the portal for another attempt rather than saving bad creds and rebooting blind (the old path saved + rebooted without ever testing the credentials). Also added a small "IP is assigned automatically (DHCP)" note to the setup page — the first-boot portal has no static-IP controls, and v1.72 already forces DHCP on save, so this just makes the behaviour visible. Prompted by relocating the reference unit and needing Fing to find it at `192.168.10.236`.
- v1.72 — **SoftAP portal reprovision resets IP config to DHCP (firmware 0.53.0), §4.5.** Moving the box to a new location and re-entering WiFi credentials through the `192.168.4.1` config portal left the *previous* location's static IP in NVS (WiFi creds and IP config are separate NVS keys, and `start_config_portal` only saved credentials). The box then associated with the new AP but applied the stale static address — out-of-subnet, so unreachable — and because a static IP produces an immediate `GOT_IP` it never fell back to the portal: a silent dead end that previously required a boot-button erase to recover. Now the portal save path also calls `nvs_reset_ipcfg_dhcp()` (erases `ipmode`/`ip`/`mask`/`gw`/`dns`), so a portal reprovision always comes up on DHCP — the box gets a valid address on whatever subnet it lands on. Scoped to the portal only; the in-service WiFi tab keeps its own explicit static/DHCP controls (a network change there stays on the reachable subnet). Found relocating the reference unit — it associated with the new AP but was unreachable at the old `192.168.1.111` on a `192.168.10.x` LAN. The boot-button reset (which already erases IP config) remains the recovery for a box that predates this and is stuck on a stale static.
- v1.71 — **Gallery per-tile 👁 "open full image" button (firmware 0.52.0), extends §3.4.** In the Unclassified/Near filter you often need to look closer before deciding, but the image's own click-to-open link is disabled there (`pointer-events:none`, v1.60) so the double-click = no-bird gesture stays clean — leaving no way to open the full frame. Added a small eye button to every tile's action row that opens the capture in a new tab via `window.open` (a direct click gesture, so not popup-blocked). Keeps the double-click = no-bird gesture unchanged (single-click-to-open was considered but drops the double-click gesture, so a dedicated button was chosen instead). Works in every filter, not just Unclassified. Client-side only.
- v1.70 — **Brightness (AE-level) camera control + fix relabelled-unclassified frames showing as unclassified (firmware 0.51.0), §3.4/§5.** Two changes. **(1) Brightness:** the reference unit's daytime captures read a little dark and there was no exposure knob (only contrast, which doesn't brighten). New Settings → **Brightness** slider (−2…+2) maps to the OV2640 `set_ae_level`, shifting the auto-exposure target brighter/darker; wired exactly like contrast (`camera_set_ae_level`, `g_settings.ae_level` NVS `s_ael`, applied live at settings-save and at `camera_init`, default 0 = unchanged). AE-level keeps auto-exposure adapting rather than fixing exposure, so it corrects under/over-exposure without the fast-shutter darkening trade-off. **(2) Gallery label-cap bug:** a frame the model left unclassified that a human then relabelled (✎ / the v1.68 📋 copy-last) saved its `corrected` label correctly — it appears in `/api/labels/confirmed` and the training export — but the Gallery still showed it as "unclassified" after a reload. Cause: `gal_build_labels` loaded at most `GAL_MAX_LABELS = 300` visit-log rows per day, and a busy feeder day at the reference unit runs 600+ captures (that day: 619 files, ~297 labelled — right at the cap). `storage_relabel` *appends* a new row for a frame that had none (not an event's first-frame), so those land past the 300th row and were silently dropped → the image reverted to "unclassified" despite the saved label. Raised the cap to 1200 (well over a realistic day's row count) and moved the ~150 KB label array to PSRAM (`heap_caps_calloc`, too big for internal heap). The label was never lost (always in the export); this restores the confirmed badge in the gallery. Found via a user copy-last on `2026-07-11_09-10-11-716.jpg` (619-capture day). *(Note: a first pass misdiagnosed this as an empty-species guard — `storage_relabel` actually writes the common name into the species column of an added row, so species is never empty; the true cause is the row-count cap.)*
- v1.69 — **`POST /model/delete?name=` — remove a model/labels file from `/sd/model` over the network (firmware 0.50.0), §3.2/§6.** The symmetric counterpart to `/model/upload` (v1.13): candidate models accrete on the card during the §3.2.2 retrain iteration (each `train.py` run's `<lineage>-v<ver>.tflite` + `.txt`) and there was no way to clean them up short of pulling the card. Same name validation as upload (plain `.tflite`/`.txt`/`.csv`, no path traversal); **refuses to delete any file belonging to the currently loaded model** — its `.tflite` or `.txt`, matched by basename stem via `classify_model_name()` — returning 409 so you can't blow away what's running (select another model + reboot first). `unlink` under the §7 write lock; 404 if the file isn't there. Prompted by cleaning up the `nordic-v0.1` proof files left on the card after the v1.68 live PAD test.
- v1.68 — **Register the PAD op + Gallery "copy last species" fast-label button (firmware 0.49.0), enables §3.2.2 Phase B end-to-end and extends §3.4.** Two changes landed together while bringing up the retrain pipeline on real data. **(1) PAD op:** `train.py` produced the first device-ready retrained model (`nordic-v0.1`, MobileNetV2-224, full-int8, input `zero_point==0` scale `1/128` — matching the `classify.cpp:427` XOR-0x80 contract exactly), but the on-device op-set check failed on `PAD`: Keras's `MobileNetV2` emits explicit `ZeroPadding2D` before its stride-2 convs, which the stock Coral model (SAME-padding) never did. Registered `PAD` in the classifier's `MicroMutableOpResolver` (now `<7>`, `classify.cpp`); harmless for the stock model, required for any Keras-built retrain. The op contract in §3.2.1 is now seven builtins. **(2) Copy-last:** speeds up the human labelling that feeds the retrain (§3.4). In the Gallery, a run of unclassified frames is often the *same* bird; you'd re-type the species each time. New per-tile **📋 copy-last** button applies the last **manually** entered species in one click — disabled until a manual ✎ relabel sets it (`g_lastSp`), and updates the tile in place so it leaves the Unclassified/Near filter as you go. Pairs with the existing double-click-=-no-bird (v1.62). Client-side only; no new persisted state. Together with the export/train/verify tooling (v1.57/§3.2.2), the loop now runs: confirm on device → export → train → int8 + verify → upload.
- v1.67 — **Relabel picker offers off-model boreal target species, starting with Lavskrike (firmware 0.48.0), enables §3.2.2 Phase A / §3.4.** First concrete step of the closed-loop retrain (§3.2.2): to collect ground-truth for the Nordic retrain you must be able to hand-label species the current model can't emit — but `/api/labels` (the relabel picker vocabulary) walked only the model's own classes, so *Perisoreus infaustus* (Lavskrike) — the species that prompted the whole retrain (§3.2.1/v1.40) — was never offered, even though its Norwegian name has sat "inert" in `species_i18n.c` since v1.40. You *could* free-type it, but with no binomial attached `export-labels.ps1` keys the class folder on the raw common-name string, so any spelling drift (`Lavskrike`/`lavskrike`/`Siberian Jay`) fragments one species across several folders — the same failure mode as the fat-fingered `"dom"` typo-class (v1.51-53). Fix: `/api/labels` now appends a small firmware-side `EXTRA_LABELS` table of off-model target species (Lavskrike only for now, per the collection scope) after the model classes, each carrying its stable binomial. So Lavskrike is a one-click picker suggestion, localizes to "Lavskrike (Perisoreus infaustus)" via the existing `species_localize` (both in the picker and on the confirmed gallery badge, `web_server.c:1496`), and every tag exports into a single `Perisoreus_infaustus/` class folder — typo-proof. No model change (the model still can't *identify* Lavskrike; this is purely the human-label path). Dompap needs nothing — it's already a model class. Extending the picker to more boreal specialists is a one-line table addition.
- v1.66 — **Closed-loop retraining pipeline formalized as an end-to-end feature (docs only, no firmware change) — new §3.2.2.** Turns the scattered retrain pieces into one operational loop and records where the build actually stands. Human label collection on the device (Gallery ✎relabel/✓confirm/🔍identify, 5 per-image states, the Near-threshold rescue filter — §3.4/v1.65) → PC export (`training-data/export-labels.ps1` → `dataset/<class>/` + `labels.csv`, v1.57) → an **as-yet-unbuilt** off-device fine-tune → int8 convert (`tools/convert_model_int8.py`) and reinstall via `POST /model/upload` — no firmware change, the model is data on the SD card by design. §3.2.2 pins that **three of the four stages already exist** (collection, retrieval, install) and the single real gap is the training script, then adds the close-the-loop pieces the §3.2.1 sketch omitted: the index-aligned label-`.txt`/`species_i18n.c` **regeneration contract** (sharpest correctness risk — a mismatched label file silently mislabels every inference), an **on-device validation harness** (`/api/classify` over held-out real captures, gating the install per §3.2.1 step 6), and **model versioning/rollback**. Includes a phased build plan — prove the whole loop on a throwaway ~3-class model before scaling to §3.2.1's ~100-150 species — and an effort estimate: **≈6.5-8.5 focused engineering days**, but **data volume is the long pole** (150-300+ varied images per priority species → weeks of passive capture + human labeling), consistent with §3.2.1's "data collection and the augmentation pipeline are the real effort." Docs only; no code changed.
- v1.65 — **Gallery "Near threshold" filter — the rescue queue for under-scored birds (firmware 0.47.0), extends §3.4/v1.60.** Motivated by a live case: a clear dompap visit where the model ranked Dompap #1 but at 14–16%, just under the 17% confidence threshold, so every frame logged as "Unidentified" (state 0) and got buried among hundreds of genuinely-empty unclassified frames. The new Show → **Near threshold** filter surfaces exactly the near-misses: unclassified rows whose logged top-1 confidence sits within **5% below** the current `confidence_pct` setting (e.g. 12–16% when the threshold is 17). These are the frames most likely to be a real bird the model under-scored — the highest-value targets for a human ✎ relabel / ✓ confirm (which also become training data, v1.57). Implemented client-side: `/api/events` already carries each row's `pct`; the gallery caches the threshold from `/api/settings` and filters `state==0 && conf-5 <= pct < conf`, showing the live band in the status line ("top guess 12–16%, just under the 17% threshold"). No-bird "review mode" (double-click = no bird, v1.62) is enabled in this filter too, since a near-miss can also turn out to be a false positive. The band tracks the threshold automatically, so lowering/raising the confidence setting re-scopes what counts as a near-miss. Pure display; no new persisted state. **(0.47.1 fix:** the initial 0.47.0 build shipped with a JS syntax error in the near-filter status-line ternary — a misplaced paren wrapped the `:` — which threw at script parse time and disabled *all* inline handlers, so no tab responded to clicks; the live `<img src='/stream'>` kept working since it's HTML, not JS. Corrected the ternary to `cond?(true):''`.)
- v1.64 — **Live view shows a "stream busy" message + reconnects; ungraceful-disconnect timeout (firmware 0.46.0), extends §3.3/v1.63.** Follows the v1.63 slot-leak fix. Before, a browser that couldn't get one of the 2 stream slots just showed the `<img>`'s generic `onerror` alt ("no camera / stream unavailable") — indistinguishable from a real camera fault. (1) `/api/status` now reports `streamUsed`/`streamMax`. (2) The live `<img>`'s `onerror` calls `liveErr()`, which reads that status and shows an overlay saying either **"Live view busy — all N stream slots are in use by other viewers, it will reconnect automatically when one frees"** (when `streamUsed>=streamMax`) or **"No video — camera stream unavailable"** otherwise, each with a Retry button; `onload` clears it. The 2 s status tick auto-retries the moment a slot frees (guarded to the live tab via the src check, and only when a slot is actually free, so it never hammers). (3) Hardened the *ungraceful* disconnect the v1.63 poll can't catch — a client that vanishes without sending a FIN (Wi-Fi drop, sleep, power loss): `SO_SNDTIMEO`=5 s on the stream socket makes the blocked send to its dead connection fail (and free the slot) within ~5 s instead of hanging on the stack's much longer retransmit timeout. Together: graceful close frees a slot in ~100 ms (v1.63 poll), an ungraceful vanish within ~5 s, and any client that hits the cap is told why and auto-recovers.
- v1.63 — **Fix: MJPEG stream slots leaked, eventually killing live view (firmware 0.45.2), fixes §3.3.** Reported as "no video in live view" after a period of use. Root cause (long-standing, unrelated to any recent feature): `stream_task` only ended — and only then decremented `s_stream_clients` — when an `httpd_resp_send_chunk` *failed*. But a browser that navigates away, clears the `<img>` src, reloads, or is cut mid-stream by an OTA reboot leaves a half-open TCP socket that keeps accepting sends into its buffer, so the task's sends kept returning OK and it never noticed the client was gone. Each such disconnect leaked one of the **2** stream slots; once both leaked, every new `/stream` request got `503 "stream busy"` and the live `<img>` showed nothing until a reboot reset the counter. Diagnosed live: with no browser connected, a slot was already stuck occupied (a second concurrent probe got 503). Fix: the task now polls the client socket each frame (`httpd_req_to_sockfd` + non-blocking `recv(MSG_PEEK)`) — an MJPEG client sends no request data, so any readability is EOF/RST, i.e. the client closed — and breaks immediately, freeing the slot. Server-side capture/stream were otherwise healthy (frames flowing), confirming this was purely the slot bookkeeping. Deploying the fix also clears any currently-stuck slots, since the reboot resets the counter.
- v1.62 — **Fast no-bird tagging: double-click in the Unclassified filter + "No bird" in the relabel picker (firmware 0.45.0, same build as v1.61), extends §3.4/v1.60.** Speeds up the human review the v1.60 states enable. (1) When the gallery Show filter is **Unclassified**, the grid enters a "no-bird mode": the thumbnail image link is disabled and **double-clicking any tile marks it no bird** (relabels `corrected="no bird"` → state 4, the tile then drops out of the unclassified view), so a run of genuinely empty frames can be cleared with a quick series of double-clicks rather than opening each. Guarded so double-clicks on the checkbox/action buttons don't misfire, and a "double-click a tile = no bird" hint appears in the status line while the filter is active. (2) The ✎ set/correct dialog's autocomplete now offers **"No bird"** as a first-class pick (mapped to `corrected="no bird"`, empty latin) alongside the species list — previously the only way to record a false positive from that dialog was to type the exact string. Both paths go through the same `/api/relabel` write as the v1.60 ✓ button, so all three (double-click, picker, button) produce an identical confirmed-no-bird row that feeds the v1.57 export as a `no_bird` negative.
- v1.61 — **Boot detection quarantine (firmware 0.45.0), extends §3.1/§5.** After a reboot the reference unit was filing one or two spurious captures under `/captures/no-date/`: the OV2640's auto-exposure/gain swing wildly over the first frames (reads as motion) and SNTP hasn't synced yet (clock ~1970, so any event lands in the no-date folder). New `detect_quarantine_s` setting (default **60 s**, 0 = off, Settings → Motion & Capture): `motion.c`'s task suppresses all triggering until uptime passes it, re-seeding the background baseline each tick so detection starts clean the moment it lifts — the camera has settled and (typically) SNTP has landed, so the first real event is well-exposed and correctly dated. Persisted in NVS (`s_qtn`); `/api/status` gains `quarantineS` (seconds remaining, 0 once live) via new `motion_quarantine_remaining_s()`, and the Live tab shows an "⏳ BOOT QUARANTINE Ns" badge + status-line countdown so it's obvious why nothing triggers right after boot. Independent of the manual detection pause (v1.31): quarantine is time-boxed and self-clears, the pause is indefinite and user-driven. Does not touch the live stream or manual `/api/capture` — only automatic motion events are held.
- v1.60 — **"No bird" review queue + human-confirmed no-bird as a valid state (firmware 0.44.0), extends §3.4/v1.59.** User insight: the model's confident "no bird" calls are exactly where its domain weakness hides — on this fisheye/dusk/feeder framing a real bird routinely reads as `background ≥ threshold` (see v1.56), so many "no bird" captures actually contain a bird. v1.59 lumped those into the grey *unclassified* state, making them hard to review. Two additions turn the false-positive pile into a worked review queue, expanding the per-image state model to **five**: 0 unclassified ("Unidentified"/no-row), 1 classified (model species, unverified), 2 confirmed species, **3 no-bird (model, unreviewed)**, **4 confirmed no-bird (human-verified empty)**. (1) The confident-background row (`species=="no bird"`) becomes **state 3**, badged in a warm "review" colour and filterable. Any frame that's actually a bird is fixed in place with 🔍 identify or ✎ relabel (both bank it as training data, v1.57 — the model's own hard negatives become positives). (2) For frames that really are empty, a **✓ "confirm: no bird"** button (on state-3 tiles) records the human's agreement: it relabels `corrected="no bird"`, moving the tile to **state 4** — badged confirmed-green. This is a first-class classification, not a non-answer: it clears the tile from the review queue *and* feeds the confirmed-label export (v1.57) as a `no_bird` **negatives** class (empty-feeder ground truth the retrain needs to stop over-predicting background). Stats still count it as a confirmed false positive (v1.46). (3) The Show filter now offers **All / Classified / Confirmed (bird) / No bird (review) / Confirmed no-bird / Unclassified** — the confirmed-bird and confirmed-no-bird buckets are deliberately separate per user request; the status line reports both counts. (No ✓-confirm-species button on no-bird tiles, and no no-bird button elsewhere — each confirm affordance only appears where its answer is valid.)
- v1.59 — **Explicit per-image classification state + one-click confirm + gallery filter (firmware 0.43.0), extends §3.4.** Makes the human-in-the-loop workflow first-class. Every gallery image now shows one of three states, colour-coded on the thumbnail badge: **unclassified** (grey — no real species: a "no bird"/"Unidentified" sentinel row, or no visit row at all), **classified** (amber — the ESP32 model decided a real species, latin present, not yet human-verified), **confirmed** (green ✓ — a human set the label, whether accepting the model's guess or typing their own). State is computed server-side in `gal_build_labels` (confirmed = `corrected` set; else classified = latin non-empty; else unclassified) and emitted as `"st":0|1|2` on each `/api/events` item. (1) **One-click confirm:** a ✓ button appears on classified-but-unconfirmed thumbnails; `POST /api/confirm` (date, f) → new `storage_confirm()` copies the row's model `species` into its `corrected` column (keeping latin), so accepting the ESP32's call no longer requires retyping it via ✎ relabel or re-running the model via 🔍 identify — and like those it feeds the v1.57 training export as human-verified truth. It refuses (`ok:false`) when there's nothing to confirm (sentinel row / already confirmed / no row), so it can't fabricate a label. (2) **Gallery filter:** a Show dropdown (All / Classified / Confirmed / Unclassified) filters the grid client-side by state, with a live "N shown" count; the status line now reads "N captures · X confirmed · Y classified". Filtering is pure display (no reload), and persists across the in-place badge updates from confirm/identify/relabel. Deleting a "no bird" capture with the ❌ button remains safe: it unlinks only the JPEG, leaving the visit-log row (and its false-positive tally) intact — the confirmed-false-positive statistic is preserved even after the image is cleared.
- v1.58 — **Gallery "identify" now persists its result as the confirmed label (firmware 0.42.0), changes §3.2/§3.4, supersedes v1.26's read-only note.** User expectation: pressing 🔍 identify on a saved capture that the model originally mislabelled (e.g. a dompap logged as "kråke") and getting the right species should *change the record*, not just display it — v1.26 deliberately made identify a look-up ("no visit-log row is written"), which is the behaviour being revised. Per the user's explicit choice (auto-commit, as the *confirmed* label): when `/api/classify-file` decides a real species (non-empty latin ⇒ above the confidence threshold), the handler now writes it to the row's `corrected`+`latin` columns via the same `storage_relabel()` path the ✎ button uses — so the gallery badge and stats update immediately **and** it feeds the v1.57 confirmed-label training export as human-verified ground truth. An under-threshold "Unidentified"/"no bird" identify has empty latin and writes nothing, so an inconclusive press can never wipe a good row. `classify_send_json()` gained a `saved` flag (always false for the posted-image `/api/classify`, which has no row); the gallery flips the thumbnail badge to the green ✓ confirmed state and appends "saved ✓" to the identify line. Deliberate trade-off the user accepted: identify is a single whole-frame pass, occasionally weaker than the original best-of-N/pooled call, and it commits without a prompt — so an idle identify can overwrite a prior label with the model's newest guess; the ✎ relabel button remains for a hand-typed correction, and only a *decided* species is ever written. (Contrast: the async `/api/recheck` still writes the model's `species` column and *skips* corrected rows — recheck is a bulk model re-run, identify is now an explicit per-image confirm.)
- v1.57 — **Confirmed-label export endpoint + training-set export tool (firmware 0.41.0), extends §3.2.1/§3.4.** Turns the v1.51 relabel data into a retrain-ready dataset — the next concrete step of the §3.2.1 Nordic retrain, after v1.51 closed the label-capture gap. (1) New `GET /api/labels/confirmed` streams every human-verified visit row (non-empty `corrected` column) across *all* monthly logs as `{"f":capture-path,"c":common,"l":latin,"ts":…}` — the raw stored common name and Latin binomial (language-independent, unlike `/api/events`' localized label), plus the capture path to pull the image. Scans `/log/visits-*.csv` with the existing hand-rolled field splitter and JSON-escapes each field; chunked since the set grows unbounded. (2) `training-data/export-labels.ps1` (PC-side, not firmware) calls that endpoint, downloads each confirmed capture into `training-data/dataset/<Latin_or_common>/` (one folder per class, filename-safe), and writes `training-data/dataset/labels.csv` (`file,common,latin,timestamp,relpath`) — the manifest a fine-tune script consumes directly. Idempotent (skips already-downloaded files), so it can run repeatedly as more labels accumulate. The actual off-device fine-tune (transfer-learn from the current checkpoint on these in-domain images + GBIF/iNat pose diversity, int8-convert via `tools/convert_model_int8.py`, install the new `.tflite`) remains the following step; this makes the data collectable in one command. `dataset/` is gitignored like the rest of `training-data/`.
- v1.56 — **TTA default flipped to OFF (firmware 0.40.1), tunes v1.55.** Field test on a textbook but hard dompap (`2026-07-10_21-30-08-366.jpg`, bird small in the left third of the 16:9 frame): the model reads the whole frame as `background` at 60–69% and, crucially, tighter crops make it *worse* (context crop 51%, tight crop 87%) — confirming the documented "zoom hurts this global iNat model" finding. TTA changed nothing structural: it nudged the wrong `background` score down (69→60%) but reshuffled the sub-3% real-species noise floor so Dompap fell out of the visible top-3. So on exactly the hard poses TTA was meant to help, it delivered no usable gain while costing ~2x inference time and extra heat on an already-warm S3. Default is now **off** (opt-in); the mechanism and setting are unchanged from v1.55, and pooling/best-of-N remain the floors. The real fix for this class of miss is the FSD v1.40 Nordic retrain, seeded by relabeling these confirmed misses (v1.51) as ground-truth training data — which was done for this image.
- v1.55 — **Detection enhancement Phase 1: horizontal-flip test-time augmentation (firmware 0.40.0), extends §3.2.** Next phase after v1.54's evidence pooling, targeting the same underconfident/pose-sensitive model on the same hard case (the head-on/backlit dompap). A left/right mirror is label-preserving for a bird (a dompap is a dompap facing either way), so classifying each frame *and* its horizontal flip and averaging the two softmax vectors gives the model a second, slightly different look at the same subject — the standard test-time-augmentation (TTA) trick — which lifts confidence on off-axis poses the single view under-scores, at the cost of a second inference per frame. Implemented cleanly in the inference core: `decode_to_input()` gained an `hflip` flag that writes each sampled column to the mirrored destination (`dx = SIDE-1-x`) in both the plain and 90/270-rotate sampling branches (no extra decode cost — same nearest-neighbor sample, mirrored index); a new `infer_scores()` helper factors out one decode+invoke+dequantize pass, and `run_locked()` now calls it once normally and, when enabled, once flipped, averaging the two score vectors *before* `decide_from_scores`. Because every path (live event best-of-N, the v1.54 pooling accumulate, the recheck task, and the manual gallery identify) goes through `run_locked`, all of them get TTA for free and pooling now accumulates TTA-averaged per-frame scores. Gated by a new **`tta` setting (Settings → Species Identification → "Extra-look identification", default ON)** because it roughly doubles inference time per frame (~4 s → ~8 s on the S3, which already runs warm) — a user watching a busy feeder can trade it back for speed. Degrades gracefully to the single pass if the extra `float[n_labels]` flip buffer won't allocate. Saved photos are unaffected (augmentation is classifier-input-only, like the v1.32 zoom). Best-of-N and pooling remain the floors, so this can still only *add* confidence, never score below v1.54. Later phases (prototype/embedding classifier from confirmed labels; fine-tune retrain; detector-then-classifier; audio) remain scoped but unbuilt.
- v1.54 — **Detection enhancement Phase 0: multi-frame evidence pooling (firmware 0.39.0), extends §3.2.** First of a phased plan to lift species ID without a model change, motivated by a good but head-on/backlit dompap (`2026-07-10_17-13-34-993.jpg`) that scored just 2% — the model ranks correctly but won't commit confidence, and single poses (head-on, backlit) fall well below threshold. Rather than trust one frame, aggregate an event's frames two ways and keep the more confident real species: (1) **best-of-N**, the single most-confident frame (the pre-v1.54 behaviour, retained as a floor so a lone good frame among background ones is never diluted); and (2) **evidence pooling**, the mean softmax across every scored frame, which combines weak complementary views (a head-on frame + a side-on frame of the same bird) into a call neither reaches alone. Implemented by refactoring the inference core: `run_locked` now dequantizes the full output vector (shared `decide_from_scores` ranks/decides identically for one frame or a pooled distribution) and can hand the raw scores back via an out-param; a new `aggregate_frames()` runs the per-frame loop, pooling, and the whole-frame rescue, and is now shared by **both** the live event task and the recheck path (so re-runs still match live, per v1.49). Pooling means and thresholds identically to a single frame, so it can only *add* wins through the best-of-N floor — it never scores below the old logic on confidence. One honest tradeoff: if a distractor species is *consistently* second across frames it can out-mean a real ID that spiked in one frame, so a pooled winner may occasionally differ from the best single frame; both must be real species for this to happen, the more-consistent one wins (often correct), and the v1.51 relabel UI lets the user correct any miss (which also becomes training data). Cost: one extra `float[n_labels]` accumulate per frame (negligible beside the inference), degrading gracefully to plain best-of-N if the pooling buffers don't allocate. Later phases (prototype/embedding classifier from confirmed labels; fine-tune retrain; detector-then-classifier; audio) remain scoped but unbuilt.
- v1.53 — **Fix: gallery species badges silently blank since v1.30 (firmware 0.38.2), fixes §3.4.** Surfaced while verifying the v1.51 relabel UI: a relabel wrote correctly to the visit log (the species appeared in the stats table) but never showed on the gallery thumbnail. Root cause is old and unrelated to relabel: `gal_label_t.base` — the buffer that joins a log row's first_frame to its image file in `/api/events` — was `char[16]`, sized for the pre-v1.30 "HHMMSS.jpg" filenames. Millisecond capture filenames (v1.30, "YYYY-MM-DD_HH-MM-SS-mmm.jpg", 27 chars) truncate to 15 in that buffer, so `strcmp(base, dirent_name)` never matched and every event thumbnail came back unlabelled — the "N labelled" gallery counter had quietly read 0 for ~20 firmware versions. Enlarged to `char[48]`. The relabel/label *data* was never affected (stats read the CSV directly); only the gallery's image-to-label join was broken.
- v1.52 — **Fix: route table overflowed `max_uri_handlers`, 404ing later endpoints incl. /api/classify-file (firmware 0.38.1), fixes §6.** Symptom: the gallery "identify" button began failing instantly ("Identify Failed") — `/api/classify-file` returned 404 "Nothing matches the given URI" while the classifier itself was healthy. Root cause: the esp_http_server registers handlers into a fixed `max_uri_handlers` table (was 32) and silently drops registrations past the cap (the loop logs "Failed to register" but boots on). The route count had grown to 35 — `/api/stats/images` (v1.50) first crossed 32 at 0.37.0, dropping one route; `/api/labels`+`/api/relabel` (v1.51) pushed it to 35, dropping three, and classify-file happened to be among the last registered. Raised the cap to 48 (comfortable headroom over 35). This is the same failure mode the header comment already warned about from RemoteStart v1.27 — the cap just hadn't been raised as routes accreted. No route logic changed; all 35 now register.
- v1.51 — **Relabel UI: user-confirmed species become the authoritative tag + training labels (firmware 0.38.0), extends §3.2/§3.4.** The visit log has always had a `corrected` column that stats and the gallery honour (corrected wins over the model's guess), but nothing ever wrote it — this adds the write path, turning the user's sightings into ground-truth labels and the seed of a training set (the data-driven version of the FSD v1.40 Nordic retrain, bootstrapped from in-domain feeder images). (1) New `storage_relabel()` rewrites the matching visit row (matched by first_frame basename) setting `corrected`=common name and `latin`=binomial (both CSV-sanitized), keeping the model's original `species` for reference; if the image has no row (not an event's first frame, or wiped by a reset) a new user-confirmed row is appended with the timestamp parsed from the filename. A non-empty `corrected` is the human-confirmed flag downstream code and training export key on. (2) `POST /api/relabel` (date, f, c[, l]) drives it; `GET /api/labels` returns the model's Northern-European species subset (raw common/latin + localized display) for the picker — new `classify_label()`/`classify_label_region()` accessors expose the label table. (3) Gallery: each image gets a ✎ set/correct-species control (datalist autocomplete over `/api/labels`, plus free text for species the model lacks, e.g. lavskrike); confirmed labels render with a ✓ and green badge, and `/api/events` now emits `"c":true/false` so the UI distinguishes verified from guessed. (4) The two display sites that blanked a corrected row's latin now keep it — since the relabel writes the corrected species' own binomial, the localized display resolves correctly. Off-device training itself is out of scope here (the S3 only runs inference); this closes the label-capture gap so data starts accumulating immediately.
- v1.50 — **False positives as a species-table row + per-row last-10 images (firmware 0.37.0), extends §3.4.** User request for the Stats tab: show confirmed false positives ("no bird") as their own row equal to a species line (name + count + first/last), and let any row list its last 10 detection images. Two parts. (1) The `/api/stats/species` object now appends a false-positive row (`{"s":<localized "no bird">,"key":"no bird","fp":true,"n":…,"first":…,"last":…}`) — `stats_t` gained `fp_first`/`fp_last`, tracked in `ingest_line` with the same first-encountered/last-updated convention as species rows; false positives stay out of the bird buckets (species/daily/hourly/total) as before, so the row is presentational only. Each species row also now carries a `key` (the raw log species value, the CSV join key) so it can be matched language-independently. The Stats total line now reads "N bird visit(s) · M false positive(s)". (2) New `GET /api/stats/images?sp=<raw species>&limit=N` (N≤10) → the last N `first_frame` paths whose decided species equals `sp`, newest first — backed by a new `stats_list_images()` in stats.c that scans the monthly logs oldest-first through a size-`max` ring buffer (so it ends holding the newest matches) reusing the existing `next_field` scanner. In the UI every species/false-positive row is now clickable and renders up to 10 thumbnails (linking to the full capture) below the table. Match key round-trips raw→`encodeURIComponent`→`url_decode`, so names with spaces (incl. "no bird") work.
- v1.49 — **Recheck now does best-of-N, matching live quality (firmware 0.36.0), fixes §3.4/v1.46.** A full-day recheck was found to *degrade* correct labels: on 2026-07-10 it dropped live-identified Dompap rows from 5 to 2. Cause: the recheck re-scored only each row's logged `first_frame`, while the live event task scores best-of-N (`CLASSIFY_BEST_OF_N`=3) and keeps the most confident — and a bird's first frame is usually its worst (mid-entry / motion-blurred), the very reason best-of-N exists. So rechecking a best-of-3 row with 1 frame systematically lowered confidence below threshold. Fixed by reconstructing the event's frames in `recheck_task`: the visit log stores `first_frame` and the frame count but not the follow-up paths, so `rc_event_frames()` re-derives them as the next (frames−1) timestamp-successors of `first_frame` in the day's capture folder — sound because motion saves an event's frames back-to-back with cool-down between events (single motion task, no interleaving), so the immediate successors *are* that event's follow-ups. Each row is then scored best-of-N exactly like live (frame 0 keeps its logged ROI, follow-ups score whole-frame; the whole-frame rescue is preserved). Cost: a day recheck now runs up to 3× the inferences (only rows with frames>1; single-frame false-positive rows are unaffected) — acceptable given recheck is async/low-priority and the user's "some minutes of delay is OK" direction; recheck-selected stays the fast path for targeted fixes. Also verified this session: recheck-selected on the 12:35 dompap frames (wiped earlier by a stats reset) re-logged them as Dompap via the v1.48 add-row path.
- v1.48 — **Recheck-selected re-logs rowless photos; recheck always reports; reset-reason diagnostic (firmware 0.35.0), extends §3.4/v1.47, §5.** Live confusion within minutes of v1.47: the user selected 5 photos and got "slow / no response". Reality: the run had finished *instantly* with 0 matched rows — the photos predated the day's stats reset, so their visit rows no longer existed — but the UI's completion message was gated on `total>0`, so nothing was shown and instant-nothing read as a hang. Three changes: (1) a selected photo with **no** matching visit row is now synthesized into one — timestamp parsed from the capture filename (`YYYY-MM-DD_HH-MM-SS-mmm.jpg`), frames=1, whole-frame ROI — classified, and **appended** to the log, so recheck-selected doubles as "re-log these photos into statistics" (exactly the post-reset dompap use case); day-wide rechecks stay strictly row-based, since blanket re-logging would turn every follow-up frame into a phantom visit, and the confirm dialog spells out the update-vs-add behaviour (an explicitly selected follow-up frame does become its own row — the user's selection is the intent). Appended rows land in the current month's CSV even for an old date — stats bucket by the row's timestamp field, not the filename, so aggregation is unaffected. (2) The gallery progress line now always reports completion (`done/total`, including `0/0`) whenever a recheck has run. (3) `/api/sysinfo` gains `resetReason` (`esp_reset_reason()` mapped to "power-on"/"software"/"panic"/"task-wdt"/"brownout"/…), shown on the Debug tab and flagged red for anything other than power-on/software — motivated by an unexplained reboot (uptime 171 s) noticed during this same session that could not be classified remotely.
- v1.47 — **Recheck selected photos (firmware 0.34.0), extends §3.4/v1.46.** User feedback on v1.46: a whole-day recheck is the right tool after a model/threshold change but is knowingly slow (~5–10 s per row) — reviewing a handful of interesting captures shouldn't cost the full day. `POST /api/recheck` gains an optional `files=a.jpg,b.jpg,…` parameter (first_frame basenames, same comma-list convention as `captures/delete`) that narrows the row scan to matching rows; the handler's body is now heap-read (8 KB cap) since a large multi-select overflows any sensible stack buffer, and the filter list is copied to PSRAM owned by the recheck task (the request body is transient). Matching is exact-token within the comma list (plain `strstr` would accept a name that's a substring of another entry) against the row's first_frame basename. The Gallery gains a "🔍 Recheck selected" button beside the day-wide one, reusing the existing checkbox selection (`gChecks`); the confirm dialog notes that only a photo that is an event's *first frame* has a visit-log row — other selections simply match nothing. Progress/one-at-a-time/priority semantics unchanged from v1.46.
- v1.46 — **Confirmed false-positive counter + per-day species re-check (firmware 0.33.0), extends §3.4.** User direction after reviewing post-v1.45 rows: a motion trigger the classifier confidently decided contained *no bird* is a false positive, not a bird visit — it must not appear in the species table (least of all as "Unidentified bird"), but it should be *counted*, since the false-positive rate is exactly what zone/sensitivity/cluster tuning needs to watch. (1) `stats.c` now diverts rows whose decided species is `no bird` (background class at/above the confidence threshold, §3.2) into a separate `false_pos` counter, excluded from the species/daily/hourly buckets and the visits total; `/api/stats/species` becomes an object `{"falsePos":N,"rows":[…]}` (was a bare array — UI and firmware ship together, no external consumers) and the Stats tab shows "N confirmed false positive(s) (no bird)" next to the visits total. Under-threshold background stays "Unidentified bird" — only *confirmed* no-birds are diverted. (2) New **Recheck species (day)**: `POST /api/recheck` (`date=YYYY-MM-DD`) re-runs species ID over every one of that day's visit-log rows with the *current* model and settings — motivated by v1.45: two days of rows written by the broken zoom path (and any rows predating a model/threshold change) can now be re-labelled retroactively instead of wiped. Runs asynchronously in its own task (priority 2, below the event task's 3, so live visits always win the run mutex); each row re-classifies its `first_frame` with the row's logged ROI plus the same whole-frame safety net as live events, then is rewritten in place (timestamp/frames/first_frame preserved, species/confidence/latin/roi/top3 replaced, user-corrected rows skipped) via a filter-to-temp rewrite per row — the write lock is held only for the quick file swap, and an interrupted recheck keeps everything already done. `GET /api/recheck` reports `{busy,date,done,total}`; the Gallery gains a "🔍 Recheck species (day)" button with progress polling that survives a page reload. One recheck at a time; `visits-no-date.csv` is excluded (rows are matched by timestamp). Drive-by: `storage_reset_stats_day()`'s line buffer was still 224 B — an fgets split of a long (roi/top3-bearing, ~330 B) row would misjudge the tail as a separate kept line; now 400 B like every other row reader.
- v1.45 — **Fix: zoomed species ID silently failed on out-of-memory, events logged "unclassified" (firmware 0.32.3), fixes §3.2.** Live symptom: a dompap pair visited the reference unit 12:34–12:37 (13 events, images saved and identifiable manually via `/api/classify-file`), yet every visit-log row came out `unclassified,0` and stats showed no recognition; historically 264 of 297 rows were "unclassified". Root cause was an unlogged allocation failure in `decode_to_input()`: the ROI-aware scale selection keeps a *zoomed* decode at high resolution — at SXGA a tight motion ROI (a bird spanning a few grid cells, precisely the interesting case) asks for a 640×512 RGB888 buffer (983 KB contiguous PSRAM), while the unit runs with ~600–700 KB free once model + arena are resident, so the `heap_caps_malloc` could never succeed and returned `ESP_ERR_NO_MEM` with no log line. Every frame of every zoomed event failed, `have_best` stayed false, and the row was written "unclassified"; the whole-frame rescue pass was gated on `have_best && …` so it never ran in exactly the failure mode it existed for (manual whole-frame classification decodes at 320×256 / 245 KB, which is why it kept working and masked the bug). The v1.44 dominant-cluster ROI made this near-total: tighter single-cluster bboxes push more decodes into the unallocatable range. Four changes in `classify.cpp`: (1) `decode_to_input()` now retries the allocation at progressively halved scales (down to 1/8) instead of failing, logging each degradation — a bird cropped from a smaller decode still classifies; (2) `zoomed_any` is set when zoom is *attempted* rather than only after a successful run, and the whole-frame rescue also fires when *zero* frames scored (`!have_best`), so a total zoomed-decode wipeout still gets one whole-frame attempt; (3) the classify job queue is deepened 2 → 16 (~7.5 KB) — a best-of-3 job takes ~13 s while a busy visit produces an event every ~5 s, so at 2 deep most events of a continuous visit overflowed straight to the fallback row; classification is async and a minutes-deep backlog is fine (rows carry the event timestamp, not the write time); (4) on a still-full queue, `classify_submit_event()` now waits up to 15 s for a slot (spanning one in-flight job) before falling back — it runs in the motion task after the event's frames are already on SD, so the cost is a longer cooldown, not lost images. Per user direction: queueing/waiting always beats a permanent "unclassified" row. Verified live via OTA (0.32.3 on the reference unit); end-to-end confirmation awaits the next zoomed visit.
- v1.44 — **Motion trigger moved to the dominant cluster's share, not the zone-wide total (firmware 0.32.3), changes §3.1.** Wind-blown grass/foliage was producing false visit events: it changes many scattered or loosely-connected cells a little each, and summing change over the whole zone conflates that with a bird — which changes one compact blob a lot. Previously the only remedy was masking every wind-prone patch out of the detection zone. `detect_once()` now reuses the ROI clustering pass (largest 4-connected cluster of moved cells, weighted by changed pixels) as the *trigger metric*: motion fires only when the dominant cluster's weight crosses the sensitivity-derived threshold (`cluster_pct >= area_thr`), and the clustering runs *before* the trigger decision instead of after it. Clusters spanning more than `MAX_CLUSTER_CELLS` (20 of 64 grid cells) are skipped entirely rather than capped — a wide contiguous wind swath is connected too, but spans far more of the frame than a bird ever does, and skipping (not capping) lets a smaller genuine cluster elsewhere in the same frame still win. Two calibration notes: (a) 20 cells is a starting point, not field-calibrated; (b) the effective sensitivity is now stricter for the same `sens` setting, since a cluster's weight is at most the old zone-wide sum — if small birds stop triggering, raise `sens` before touching the zone mask. The motion log line now reports both `cluster %` and `zone %` for tuning. Field evidence: a dompap pair visit (2026-07-10 12:34–12:37) produced 13 automatic events under this logic at `sens=77`. Shipped in the same 0.32.3 binary as v1.45.
- v1.43 — **Dual-rate background for motion detection (firmware 0.32.2), extends §3.1.** Live miss investigated from a field photo: a bird landed on the usual feeder stump but never triggered an automatic visit, requiring a manual `/api/capture` save. The zone mask and sensitivity (`sens=77`) were both ruled out — the bird's position mapped cleanly inside the enabled zone — leaving the single fast-EMA background (7/8 old + 1/8 new, ~1 s half-life) as the likely culprit: a small, low-contrast bird against dark conifers that settles gradually can get partly absorbed into that background before any one sampled frame's changed-area crosses the trigger threshold. `motion.c` now rolls a second background in parallel, `s_bg_slow` (63/64 old + 1/64 new, ~11 s half-life), seeded and reset alongside the existing one; a pixel counts as changed if it diverges from *either* background, so a gradual arrival that the fast EMA absorbs still stands out against the slower one for longer. One extra `uint8_t[DETECT_MAX_PX]` SPIRAM buffer and one more diff/blend per pixel — cheap on the ESP32-S3. Verified live via OTA: the reference unit's next visit to the same stump, previously missed under the old single-background logic, triggered automatically and filed correctly.
- v1.42 — **Fix: clockSrc stuck on "manual" after SNTP actually synced (firmware 0.32.1), fixes §3.4.** Live symptom: `/api/status` kept reporting `clockSrc:"manual"` and captures were landing in `/captures/no-date/` on a unit that was demonstrably NTP-synced (a plausible current time was showing after ~100 min uptime). Root cause was a one-way latch, not a failed sync: `web_server.c`'s `s_clock_manual` is set `true` whenever the browser-time fallback (`POST /api/time`, called by the web UI on load/before every snapshot) wins the race against SNTP at boot, but nothing ever set it back to `false` once SNTP completed in the background and silently overwrote the clock again via its own `settimeofday()`. Fixed by wiring `wifi.c`'s `start_sntp()` to register the `esp_sntp_config_t.sync_cb` (left `NULL` before) — a new `on_sntp_sync()` calls a new `web_server_note_ntp_sync()` that clears the latch whenever a real sync lands, so `clockSrc` self-corrects instead of staying wrong for the rest of the boot. Also corrected a stale comment on `h_time_set()` that still claimed it re-bases `/captures/no-date/` files into the day folder — that migration was removed in `2cdc828` after it caused FATFS rename data loss, and files stranded there still aren't recovered by this fix, only new ones are prevented. Verified live via OTA: fresh boot now reports `clockSrc:"ntp"`.
- v1.41 — **Expand the Northern-European species table with boreal/taiga specialists (firmware 0.32.0), extends §3.2.1/`species_i18n.c`.** Follow-through on v1.40's scoping: audited the ~80-species lowland garden/feeder table for forested/mountain-terrain gaps and added 24 boreal specialists (tits, crossbills, woodpeckers, grouse, owls, large raptors, and *Perisoreus infaustus* itself). Checked each addition against the v1 model's actual 965-class label list rather than assuming: **6 (Acanthis flammea/Gråsisik, Loxia curvirostra/Grankorsnebb, Pinicola enucleator/Konglebit, Coccothraustes coccothraustes/Kjernebiter, Surnia ulula/Haukugle, Aquila chrysaetos/Kongeørn) are real classes the model already predicts** — these were previously getting silently dropped by the region filter (when enabled) despite the model correctly recognizing them, so they start working immediately. **The other 18 — including Lavskrike, the species that prompted this whole investigation — are not classes in the v1 model at all**, so these entries stay inert (`species_in_region()` can never be matched by an inference the model is structurally incapable of producing) until a retrained model that includes them is installed (§3.2.1's scoped retrain). Table now covers ~107 species. No behavior change for anyone with the region filter off.
- v1.40 — **Documentation only (no firmware change): scoped a Nordic/boreal species-model retrain, extends §3.2.1.** A live miss — the reference unit repeatedly photographed a Siberian Jay, correctly always logged as "Unidentified bird" — traced to a genuine model-coverage gap, not a firmware bug: the species is absent from both the v1 model's 965-class label set and the `species_i18n.c` Northern-European table, and a broad GitHub search found no ready-made replacement model that covers it either (every alternative found is the same "~1000 common species" tier). Google's SpeciesNet (camera-trap-specific, 2000+ species, confirmed via its published labels to include Siberian Jay) is a useful taxonomy cross-check but not a usable model or data source for this device — EfficientNetV2-M, cloud/desktop-only, training corpus unpublished. §3.2.1's existing regional-retrain sketch is sharpened into a concrete next step: an expanded ~100-150-species target list (existing table + boreal specialists), the same iNaturalist/GBIF data pull, a new domain-gap mitigation (synthetic motion-blur/edge-clip/close-range augmentation, motivated by today's real captures all being edge-clipped/blurred/point-blank rather than clean nature photos), transfer-learning from the current checkpoint, and validation against today's actual hard captures before field use. No code changed; this is a scoping record for future work.
- v1.39 — **CI: drop the AI-Thinker ESP32-CAM ("constrained") build target (no firmware change).** That target's CI build has been failing independent of any of this session's changes — `TEMPERATURE_SENSOR_CLK_SRC_DEFAULT` is undeclared for classic ESP32 in the pinned ESP-IDF v6.0.1's temperature-sensor driver, breaking `web_server.c`'s `soc_temp_c()` — and the project's actual hardware is the ESP32-S3 reference unit (§2.1), so `.github/workflows/release.yml`'s `build-esp32` job is removed rather than fixed: CI now only builds/releases `esp32s3`, and the release body no longer mentions an `esp32` `.bin`. `board_config.h`'s `BOARD_AITHINKER_ESP32CAM` block and the per-target `sdkconfig.defaults.esp32`/partition table are left in place (unbuilt, not deleted) in case classic-ESP32 support is picked back up later.
- v1.38 — **Fix: fast-shutter overexposed in daylight — gated on ambient darkness (firmware 0.31.0), fixes v1.37.** Live use caught the flaw v1.37 flagged as a risk: with `fshut=1` applied unconditionally (AEC disabled, exposure pinned to a fixed value tuned against dawn light), the image overexposed a couple hours later as the sun rose — a truly fixed exposure has no way to track *rising* light once auto-exposure is off, whereas normal AEC would have shortened exposure to compensate. Fixed by making fast-shutter conditional on actual darkness instead of a static toggle: `motion.c`'s per-frame ambient-brightness hysteresis (added for the v1.36 illuminator auto mode) now drives *both* the illuminator and fast-shutter from one shared `s_dark` flag — `fast_shutter` (the setting) only means "allowed to engage," and `camera_set_fast_shutter()` is actually called only when the scene reads dark (avg luma < 35/255) and released once it's bright again (> 90/255), so a fixed short exposure can never be active in daylight bright enough to overexpose it. `camera_set_fast_shutter(g_settings.fast_shutter)` was removed from both `camera_init`/watchdog-recovery and the live-apply path in `POST /api/settings` (both used to force the setting unconditionally, which is exactly what caused the bug) — camera (re)init now always resets to normal auto exposure and leaves the dark/bright decision entirely to `motion.c`. Settings copy updated to describe the auto-gating. Verified live: `fshut=1` left set through full daylight no longer overexposes (confirmed by re-capturing and comparing against the pre-bug baseline); the shared dark/bright signal was already field-tested via the illuminator in v1.36.
- v1.37 — **"Reduce motion blur" fast-shutter setting (firmware 0.30.0), extends §2.1/§5.** Prompted by a field photo (a bird filling most of the frame at point-blank range, blurred, unidentifiable even on the whole unzoomed frame — see v1.34-adjacent discussion) where fixed-focus-lens blur on a close/fast subject was the suspected cause. New Settings → "Reduce motion blur (fast shutter)" toggle calls a new `camera_set_fast_shutter()` (`camera.c`): when on, it disables OV2640 auto-exposure (`set_exposure_ctrl(0)`) and pins a short, fixed integration time (`set_aec_value`, a `FAST_SHUTTER_AEC_VALUE` constant out of the sensor's 0-1200 range), while leaving auto-gain (`set_gain_ctrl(1)`) on so the image still self-brightens rather than just going dark — trading noise for less blur, rather than a true DSLR "shutter priority" auto-balance (the OV2640 driver doesn't expose one). Applied live via `POST /api/settings` (`fshut=`) and re-applied on every camera (re)init, including watchdog recoveries, alongside the existing rotation/contrast calls. **Default off** — matches normal auto exposure until opted in. The fixed AEC value is a starting point, not a calibrated one (its exposure-time meaning isn't documented in µs, only relative to the sensor's own scale) — may need field tuning if it's too dark/noisy or not obviously less blurry once tested against a real moving bird.
- v1.36 — **Illuminator LED wired to `ir_led_mode` auto (firmware 0.29.0), realizes the §2.1 IR-illuminator slot.** The onboard WS2812 illuminator found in v1.34/v1.35 (GPIO48, `PIN_IR_LED`) moves from a Debug-only probe to a real, optional feature. New `illum.c`/`illum.h` wraps the RMT-backed WS2812 handle (shared by both the Settings-driven auto mode and the Debug-tab GPIO probe, which now routes `num=48` through it instead of a private duplicate handle); `illum_init()` runs at boot and is a safe no-op on boards where `PIN_IR_LED` is `-1`. **Auto mode** (Settings → Illuminator LED → Auto) reuses the grayscale detect-frame `motion.c` already decodes every 250 ms for differencing — no separate light sensor, no extra sampling cost — and averages its luma: below 35/255 turns the illuminator on, above 90/255 turns it off, with everything in between left alone (wide hysteresis band, chosen because ordinary daylight reads far brighter than a small onboard LED's own bounce-back at typical nest-box/feeder range, so the LED turning itself on can't false-trigger a flicker loop). It runs continuously, independent of motion events, so it behaves as steady night lighting rather than a per-shot flash. **Default is Off** — `ir_led_mode` already defaulted to 0 (unused since v1.9, when the setting was scaffolded ahead of the hardware existing), so existing/new units ship with the illuminator untouched unless a user opts in. The Settings label changed from "IR LED" / "Auto (needs IR hardware)" to "Illuminator LED" / "Auto (on during dark hours)" now that real hardware backs it, with inline copy explaining it's brightness-based, not a clock/sunrise schedule.
- v1.35 — **Fix: GPIO debug toggle couldn't clear the WS2812 illuminator (firmware 0.28.1), fixes v1.34's GPIO Test.** Live use of the new Debug-tab GPIO probe (v1.34) traced the reference unit's onboard flash/illuminator LED to **GPIO48**, and found it's a WS2812, not a plain LED: driving the pin with a raw `gpio_set_level(1)` clocked garbage into its shift register (lit a dim reddish-white rather than a clean color), and a follow-up `gpio_set_level(0)` didn't turn it off — a WS2812's data-low state just *latches* whatever was last shifted in rather than clearing it, so the board was left stuck lit with no way back to off via a plain level change. `h_debug_gpio` now special-cases GPIO48: a new `espressif/led_strip` RMT-backed handle sends a real bit-timed frame (`level=1` → dim white pixel, `level=0` → `led_strip_clear`), so on/off actually works. Other pins still take the raw `gpio_set_level` path unchanged.
- v1.34 — **Debug-tab raw GPIO toggle (firmware 0.28.0), extends §5.** Prompted by a field question: the reference unit's onboard LED is a strong white illuminator (not a status/power LED), lit at night and off in daylight, with no code anywhere in this firmware driving it — `PIN_STATUS_LED` (`board_config.h`, GPIO2 on the generic S3 board, marked `TODO: verify`) is never passed to `gpio_set_level()`, and `PIN_IR_LED` is `-1` (unwired) on every board variant — so the day/night behaviour is most likely an autonomous hardware LDR circuit, not something software controls. To let the pin be found by trial without a flash/rebuild cycle, the Debug tab gains a **GPIO Test** control: a pin-number field plus On/Off buttons hit a new `POST /api/debug/gpio` (`num=`, `level=`), which `gpio_reset_pin()`s the requested pin, sets it to output, and drives the level directly — no NVS persistence, so a pin reverts to input on the next reboot rather than staying stuck driven. A `gpio_debug_reserved()` allow-list check refuses camera (`CAM_PIN_*`), SD (`SD_PIN_*`) and the ESP32-S3 octal flash/PSRAM bus (GPIO26-37) plus strapping/USB/UART0 pins (0, 3, 19, 20, 43-46), since reconfiguring one of those as a bare GPIO mid-boot could wedge the camera or SD card rather than just (not) light an LED.
- v1.33 — **DetectEnhance2: zoom made reliable (firmware 0.27.0), extends §3.1/§3.2/§3.4.** Five refinements that turn v1.32's motion-zoom from opportunistic into reliable, from a code-level analysis of the detect→identify pipeline. **(1) Per-frame ROI:** the zoom region is no longer the trigger-time snapshot applied to all best-of-N frames (a hopping bird moves 2–3 grid cells between ~1 s frames, so frames 2–3 were cropped to where the bird *was*); each saved frame now carries the ROI computed by the `detect_once()` immediately preceding its grab — the ROIs were already being computed and discarded, so this is free. **(2) ROI-aware decode scale:** `decode_to_input` chose its JPEG decode scale before knowing the ROI, e.g. SVGA decoded at 400×300 and a half-frame ROI upscaled 1.5× from 150 px; the halving condition now keeps the *ROI* ≥ 224 px within the decode-buffer budget (SVGA with a zoom now decodes full 800×600 = 1.44 MB ≤ the 1.5 MB cap; SXGA at 640×512), roughly doubling effective pixels on the bird for small ROIs. **(3) Square-expand crop:** the ROI was square-*shrunk* (`side = min`), truncating the ends of an elongated ROI (bird moving horizontally); it's now square-*expanded* (`side = max`, clamped to frame, re-centred), keeping the whole ROI plus context — the whole-frame case degenerates to the classic center-crop, unchanged. **(4) Largest-cluster ROI:** the ROI was the bbox of *all* moved cells, so two separate in-zone movers (bird + shadow/feeder swing) merged into one diluted bbox; a 4-connected flood fill over the ≤64 cells now keeps only the cluster with the most changed pixels. **(5) Whole-frame fallback:** if every zoomed frame returns without a real species (empty Latin — the crop may have missed the bird), one extra whole-frame inference runs on the best-scoring frame and wins if better; bounded cost, only on failure, skipped when nothing was zoomed. **(6) Observability:** the visit log gains two trailing columns — `roi` (the winning frame's zoom rect `x0-y0-x1-y1`, empty = whole frame) and `top3` (`Latin=pct;…`) — for field-tuning; all three CSV readers (stats, gallery labels) read positionally and stop at `latin`, so old and new rows parse identically, and their `fgets` buffers were grown 224→400 so a longer row can't split into a bogus phantom row (the header row gains the new column names). Score-averaging ensemble, bilinear resize and adaptive pixel threshold were analysed and deliberately deferred — documented under §11 future candidates.
- v1.32 — **DetectEnhance: 8×8 detection zone + motion-zoomed species ID (firmware 0.26.0), extends §3.1/§3.2.** Two linked upgrades to detection quality, sharing one 8×8 grid over the frame. **(1) Detection zone.** A new `detect_zone` setting (64-bit mask, one bit per grid cell, default all-on = unchanged behaviour) lets the user exclude cells from motion detection — mask off a swaying branch, a footpath, or busy sky so they never trigger captures. `motion.c` now tallies changed pixels per grid cell and only counts in-zone cells toward the trigger (the overall threshold is normalised over in-zone pixels; an all-off zone simply means "no detection anywhere"). Background rolling still runs over the whole frame on quiet frames, so masked-out motion is absorbed and never lingers. Edited on the **Live tab** via an *Edit detection zone* overlay: the 8×8 grid is painted directly over the live stream, tap cells to include (green) / exclude (red), Save/All-on/All-off/Cancel; persisted through `POST /api/settings` as a 64-char `zone` string (sent this way to dodge JS 64-bit-Number precision loss) and stored in NVS as a `u64`. **(2) Motion-zoomed species ID.** When a bird only fills part of the frame, the classifier used to center-crop the *whole* frame to 224×224 and shrink the bird to a few pixels. Now motion detection reports the **bounding box of the changed in-zone cells** (padded one cell for context; empty when the change is diffuse, e.g. a light shift, which falls back to the old center-crop), threaded from `motion.c` → `capture.c` → `classify_submit_event` → the classifier's `decode_to_input`, which crops to that ROI (in source-frame axes, before the 90/270 rotation permute, so it's orientation-independent) so the bird fills the model input. Gated by a `detect_zoom` setting (default on, toggle in Settings → Motion & Capture). **Saved photos are unaffected — full-frame as before; only the classifier's input is zoomed.** The ROI is snapshotted at trigger time (not overwritten by the follow-up frames' detection) and applied to each best-of-N frame. Manual `POST /api/classify` / gallery identify still use the whole frame.
- v1.31 — **Live-view "Disable detection" maintenance toggle (firmware 0.25.0), extends §5.** The Live tab gains a **Disable / Enable detection** button (below Snapshot) that pauses motion detection on demand — for cleaning the lens, adjusting the mount, or working in front of the camera without generating a stream of junk visit events. While paused, `motion.c`'s task keeps running but skips all frame-grab/differencing, so the **live stream and manual snapshots still work**; when re-enabled it drops its background baseline and re-seeds against the current scene (light/subject may have changed during maintenance). State is **runtime-only, not persisted** — detection always comes up **enabled after a reboot**, so a box left paused can't silently stop recording after a power blip. Driven by a new `POST /api/detect` (`enabled=0|1`, replies `{"enabled":bool}`) plus a `detect` flag added to `/api/status`; the 2 s live tick keeps the button label and an amber **DETECTION OFF** badge over the stream in sync, so two browsers watching the same box agree. The `max_uri_handlers` cap was raised 28→32 for headroom (an exact-fit cap silently drops later route registrations — RemoteStart v1.27).
- v1.30 — **Best-of-N species ID + millisecond capture filenames (firmware 0.24.0), extends §3.2/§3.4.** Species ID no longer classifies only the first frame of a visit — the first frame is often the worst (bird mid-entry, motion-blurred). The classifier now scores up to `CLASSIFY_BEST_OF_N` (**3**) of the event's saved frames and keeps the most confident result, so a cleaner later frame can supply the label. Ranking prefers a *decided real species* (non-empty Latin) over the `no bird`/`Unidentified bird` sentinels, so a confident background frame can't outrank a genuine ID from another frame; among equals, higher confidence wins. Implementation reworks the async pipeline to pass **frame paths** (not a single PSRAM buffer) to the classifier task, which re-reads each frame from SD one at a time — bounding PSRAM to a single decode regardless of N, and taking the run mutex per-frame so a manual `POST /api/classify` isn't blocked for the whole ~N×5 s run. Cost: up to N inferences per event on the classifier task (still fully async — capture never waits; queue depth 2). **Capture filenames now include milliseconds** (`YYYY-MM-DD_HH-MM-SS-mmm.jpg`): an event's frames are ~500 ms apart and second-only names collided (only the `b..z` suffix fallback saved them) — best-of-N relies on distinct per-frame files to re-read. Pre-SNTP `no-date` uptime names are unchanged.
- v1.29 — **Live-view "DETECTING" indicator (firmware 0.23.0), extends §5.** The live tab now shows a pulsing red **DETECTING** badge over the top-left of the stream and a red outline around the frame whenever motion detection is actively capturing an event (driven by the existing `motion` flag in `/api/status`, polled by the 2 s live tick — CSS-only, no new endpoint). The plain-text `motion ACTIVE/idle` in the status line stays. This makes it obvious at a glance when a visit is being captured; the badge is lit during the multi-frame capture window and clears at cool-down. **Note on which frames get classified (unchanged behaviour, documented):** an event saves up to `capture_count` frames to SD, but only the **first** frame is submitted to the species classifier (`capture.c` keeps a PSRAM copy of frame #1 as the event's "best" frame; §3.2 is deliberately async/single-frame so capture never waits). The follow-up frames are stored but not individually classified. A best-of-N option (classify several frames, keep the highest-confidence label) is noted as a §3.2 future enhancement.
- v1.28 — **Fix: classify top-3 ignored the species-name language (firmware 0.22.2), fixes §3.2.** The Gallery "identify" result (and `POST /api/classify`) showed its top-3 breakdown in raw English even with Norwegian selected: `classify_result_t` only splits the *winning* label into `species`/`latin` (which `species_localize` then translates), while `top_label[0..2]` arrive as raw `"Scientific name (Common Name)"` model strings that were emitted verbatim. `classify_send_json()` now parses each raw label's `Latin (Common)` shape and runs it through the same `species_localize`, so the whole result honours the language setting (e.g. `Gråspurv (Passer domesticus)` instead of `Passer domesticus (House Sparrow)`); a label with no parseable binomial (e.g. a bare `background` guard) passes through unchanged. Reply buffer grown 512→768 for the longer localized labels.
- v1.27 — **Fix: motion detection dead at SXGA/HD resolution (firmware 0.22.1), fixes §3.1.** Field report: a bird clearly visible in the live stream produced no detection, and raising sensitivity to 90 % did nothing. Root cause is a regression from v1.21 (which added the HD/SXGA resolutions): `motion.c` decodes each frame at 1/8 scale for differencing, but the detection buffers/limits (`DETECT_MAX_W/H/PX`) were still sized for the old XGA ceiling (128×96 = 12288 px). At SXGA (1280×1024) the 1/8 frame is 160×128 = 20480 px, which overran `DETECT_MAX_PX`, so `detect_once()` hit its `px > DETECT_MAX_PX` guard (and/or the RGB565 decode failed for lack of output buffer) and returned "no motion" on **every** frame — detection was silently disabled at high res, independent of the sensitivity setting (the code bails before sensitivity is ever consulted). HD (1280×720 → 160×90) was broken the same way. Fixed by sizing the detection buffers to the SXGA ceiling (160×128); all lower resolutions and HD now fit. Sensitivity semantics were verified correct in passing (90 % → only 2 % of frame need change — the sensitive end). Also exposed a cumulative `motionTriggers` count in `/api/sysinfo` + a "Motion triggers" Debug-tab row, so a live detection can be confirmed by watching the count rise (there was previously only the transient `motion` active flag).
- v1.26 — **Identify a saved Gallery photo on demand (firmware 0.22.0), extends §3.2/§3.4.** The Gallery gains a per-thumbnail 🔍 button that re-runs species ID on that already-saved JPEG and shows the result (species + confidence, with the top-3 below) inline under the photo — useful for a capture whose event was never classified (e.g. saved before the model was installed, a manual live-view snapshot, or an "Unidentified bird" you want to re-check after a model swap). Rather than re-uploading the ~200 KB image over WiFi, a new `GET /api/classify-file?date=&f=` has the device read the file from its own SD and classify it: the handler validates `date`/`f` against traversal (no `..`, `/`, `\`), reads the JPEG from `/sd/captures/<date>/<f>` into PSRAM, and runs the same synchronous `classify_run_sync` path as `POST /api/classify`, blocking that httpd thread for the ~seconds-long decode + inference. The JSON reply (localized `species` + raw `top3`/timing) is now built by a shared `classify_send_json()` helper used by both classify endpoints. Decision/display only — no model change, no new visit-log row is written (this is a look-up, not a re-log).
- v1.25 — **Drop no-date re-base + fix timezone (firmware 0.21.1).** Removed `storage_rebase_no_date()`: it used FATFS `rename()` to move `no-date` captures into today's folder, but cross-directory `rename()` on this target unlinks the source without creating the destination — a data-loss bug (caught in test, only throwaway snapshots lost). New captures are already correctly dated by the browser-time fallback (v1.24), so the re-base was only cleaning up historical stragglers and isn't worth the risk; stranded `no-date` files now simply stay put. Also fixed the timezone not applying (times/filenames showed UTC): `TZ` is now set + `tzset()` at boot right after settings load (and re-applied after a manual clock set), so `localtime` is correct before any capture path runs.
- v1.24 — **Browser-time fallback + date-time filenames + clock display (firmware 0.21.0), extends §3.4.** Fixes snapshots landing permanently in `/captures/no-date/` on a network with no reachable NTP (the clock never leaves ~1970). New `POST /api/time` lets the web UI set the device clock from the browser when SNTP hasn't synced (page-load + before each snapshot; only while unsynced, sanity-checked, SNTP stays authoritative), and `storage_rebase_no_date()` moves already-stranded no-date captures into today's folder once a clock exists. Capture filenames are now date-time based (`YYYY-MM-DD_HH-MM-SS.jpg`). The current device time + source (`ntp`/`manual`/`none`) is shown in the live-view status line and the Debug tab (`time`/`clockSrc` added to `/api/status` and `/api/sysinfo`).
- v1.23 — **Alternative WiFi network "alt1" + failover (firmware 0.20.0), new §4.7.** The device now stores two networks — primary (`ssid`/`pass`) and an optional alternative (`ssid2`/`pass2`) — each configured from the WiFi tab with its own scan-and-pick. Boot tries primary then alt1; in service it fails over after 5 consecutive reconnect failures and keeps alternating, so a box that can see two APs is never stranded when one is down. The portal retries both. The DHCP/static IP setting is shared across both networks (a single static address requires both APs on the same subnet, else DHCP — the tab says so). WiFi connection logic in `wifi.c` refactored to a slot-indexed network list (`s_nets[]`, contiguous-from-primary usable count) with per-slot NVS keys; `nvs_load_wifi`/`nvs_save_wifi` are now slot-aware and `nvs_erase_wifi` clears both. New `GET /api/wificfg` returns the two configured SSIDs + the currently-connected AP; `/api/sysinfo` gains `apSsid`; the WiFi tab shows connected + both configured names, and the Debug tab's WiFi Link shows the connected network. `POST /wifi-save` gained a `slot` field (blank alt SSID removes alt1).
- v1.22 — **Camera watchdog & auto-recovery (firmware 0.19.0), new §3.5.** Fixes a field failure where the OV2640 stops delivering frames after a while — either `esp_camera_fb_get()` returns `NULL` (SCCB still reads the PID, so `camPresent` misleadingly stays true) or, as the hardware degrades, `fb_get()` **spins CPU-bound** on corrupt frames (`cam_hal: NO-SOI`). All frame grabs route through new `camera_grab()`/`camera_return()` wrappers; a `cam_wd` task (priority **above** the motion task, started **before** it) detects a >5 s stall *while consumers are actively grabbing* — no probe grab, so a spinning camera can't wedge the watchdog — and recovers by draining in-flight grabs, `esp_camera_deinit()`, XCLK-low ~150 ms, re-init. If a grab is stuck in the driver (undrainable) or 3 recoveries fail, it raises `camFault` and backs off rather than pointlessly rebooting (a reboot can't fix the sensor-core latch). **This shipped design is the second cut:** a live test of the first cut exposed that its mutex-across-`fb_get()` + low-priority/late-start let the broken camera starve `app_main` so the watchdog never even launched — so grabs are now lock-free and startup is reordered (watchdog + rollback-validate before `motion_start()`, which goes last). Also adds `socTempC` (ESP32-S3 on-die temp) to `/api/sysinfo` for overheat-vs-connection diagnosis. New Debug-tab rows: watchdog recoveries, SoC temperature, camera-fault. **Known limit / potential HW mod:** the reference board has `CAM_PIN_PWDN`/`CAM_PIN_RESET` unwired, so firmware can't truly power-cycle the sensor — the guaranteed fix needs a HW mod (wire PWDN, or a MOSFET/load-switch on the camera VDD rail); documented in §3.5.
- v1.21 — **Camera resolution + contrast controls (firmware 0.18.0), extends §5.** Settings gains a **resolution** selector (VGA / SVGA / XGA / HD / SXGA, from a `RES[]` table in `camera.c`; index stored in NVS) and a **contrast** control (-2..+2). Resolution is applied at `camera_init` where the frame buffers are sized, so it's **reboot-required** — the UI says so and offers a one-click reboot after a save that changed it. **Memory limits (two real bugs caught in live test):** a high resolution competes with the ~5.5 MB species model + arena for the 8 MB PSRAM. (1) First test at UXGA came up with the camera at 1600×1200 but the *classifier* starved (`clsLabels:0`) because `camera_init` ran first and grabbed the PSRAM — fixed by running `classify_init()` **before** `camera_init()` in `main.c` so the model/arena reserve first, and by having `camera_init` step the frame size **down** through `RES[]` on an init failure (updating `g_settings.resolution` in RAM to what actually loaded) rather than failing boot. (2) With that fix, UXGA's camera *and* the model both loaded — but **inference then failed** (`/api/classify` → 500) because there was no PSRAM left for the runtime JPEG-decode buffer. So **SXGA (1280×1024) is the ceiling** — verified live: at SXGA the camera, model, and a real inference all succeed (~4.7 s, ~775 KB free heap); UXGA is removed from the table/UI, and any stale UXGA value falls back to SVGA. Contrast applies live via `camera_set_contrast()` (OV2640 `set_contrast`). **Note on "sharpness":** the OV2640's `set_sharpness`/`set_denoise` are unimplemented stubs (`return -1`) — there is no sensor sharpness control — so contrast is offered as the supported crispness knob instead of a dead slider. The Debug card's camera resolution string is now dynamic (`camera_framesize_str()`) rather than a fixed `SVGA` macro.
- v1.20 — **"Wipe day" combined cleanup (firmware 0.17.1), rounds out §3.4 Gallery cleanup.** A single confirm-gated Gallery action that deletes a day's photos *and* removes that day's statistics in one step — so cleaning up after debugging can't leave the "stats without images" mismatch behind. `POST /api/captures/delete` gains an optional `stats=1`; when set it also calls a new `storage_reset_stats_day()` that, for a real date, rewrites that month's visit-log CSV keeping the header and every row whose timestamp isn't that day (dropping the empty file if nothing remains), and for `no-date` deletes the unsynced log outright — all under the storage write lock, via a temp-file rewrite so a crash mid-wipe can't corrupt the log. The standalone Delete-all (photos only) and the global Stats → Reset Statistics remain for the narrower cases. Response now reports `deleted` (photos) and `statsRemoved` (rows).
- v1.19 — **Event-aware Gallery + bulk delete implemented (firmware 0.17.0), extends §3.4.** Closes the "stats show a species but the Gallery is just unlabeled files" disconnect (the images were always there — capture saves the JPEG before classification even runs — but the two views weren't linked). `GET /api/events` now joins each day's image files to that month's visit log by frame path: a file that is a logged event's *first frame* comes back annotated with its species + confidence (`sp`/`pct`), localized server-side to the current display language via `species_localize`, so the Gallery badges those thumbnails (follow-up frames stay unlabeled — the log stores one row per event). The join reads the monthly CSV with the same hand-rolled `next_field` scanner `stats.c` uses (avoids the [[csv-parsing-strtok-pitfall]]), capped at 300 events/day, built into a heap map and freed per request. **Cleanup tooling (the ask):** new `POST /api/captures/delete` bulk-deletes photos — `date=` + either `files=a.jpg,b.jpg,...` (multi-select, parsed straight from the body so a long list isn't truncated) or `all=1` (deletes the whole day-folder and rmdir's it), traversal-guarded, under the storage write lock. Gallery UI gains a per-thumbnail checkbox, a species/confidence badge, and Select-all / Delete-selected / Delete-all buttons, each confirm-gated; Delete-all's dialog notes that statistics are separate (Stats → Reset Statistics). Photos only — deleting images never touches the visit log, keeping the two cleanup tools single-purpose. `max_uri_handlers` headroom unchanged (26/28). **Northern-Europe region filter implemented (firmware 0.16.0), realizes the §3.2.1 lightweight path.** New Settings → Species set toggle (Global / Northern Europe only); when on, `classify.cpp` restricts the top-3 ranking to classes whose Latin binomial is in the ~80-species Northern-European set — reusing the existing `species_i18n.c` table as the single source of truth via a new `species_in_region()` (no second list to maintain). The `background` guard and any no-binomial label are always kept; confidence is **not** renormalized, so an out-of-region animal loses its winning class and drops below the confidence threshold to "Unidentified bird" rather than a confident wrong species (kills the "Wild Turkey at a Norwegian feeder" failure mode). Decision-layer only — same model file, same ~4.3 s inference, unchanged in-region accuracy. Robustness: the match count is computed once at label-load and the filter auto-disables for a model with zero in-region labels, so it can't blank out an unrelated model's results; `/api/sysinfo` + the Debug Species-ID card now report the in-region label count. New setting persists in NVS (`s_rfilt`), default off (device ships region-neutral). Verified live on the reference unit (OTA-flashed once WiFi recovered): `/api/sysinfo` reports `clsRegion:83` (all 83 table species present in the loaded 965-class model); on the same non-bird test frame, `/api/classify` returned **Wild Turkey / Steller's Jay / American Crow** with the filter off and **Rock Pigeon / House Sparrow / Eurasian Collared-Dove** (all Northern-European) with it on — both correctly deciding "Unidentified bird" below threshold, proving the candidate set is restricted without renormalizing confidence upward; and `rfilt` round-tripped through NVS across a reboot.
- v1.17 — **Documentation only (no firmware change).** Added §3.2.1 recording the species-model data-source evaluation: a table of public bird-ID sources considered and *why each is not usable as-is* (BirdNET — audio + non-commercial; Merlin — closed; NABirds/CUB — datasets not models, N-American; OSEA — too large for the device; birder — PyTorch + partly CC-BY-NC; Kaggle trainings & astrocoding TFLite — format/licensing/coverage gaps), the rationale for the current Google/Coral iNaturalist-birds default (only freely available model meeting every on-device constraint at once, uniquely Apache-2.0), and a step-by-step sketch of the intended regional-retrain upgrade path (iNat/GBIF data pull → MobileNetV2 transfer-learn → full-int8 quantize / existing converter → SD-installable `<region>.tflite`). No `main/version.h` bump — spec/reference text only.
- v1.16 — **Reset Statistics implemented (firmware 0.15.0), extends §3.4/§5.** New `storage_reset_stats()` deletes every `/sd/log/visits-*.csv` file (the same prefix/suffix match `stats.c` already uses to find them) under the existing write mutex, leaving `/sd/captures` untouched — this clears historic species recognition/visit history without deleting a single photo. New `POST /api/stats/reset` (§6); the Stats tab gets a red "Reset Statistics" button gated by a plain-language `confirm()` dialog that spells out exactly what's deleted (visit history) and what isn't (photos), then reloads the charts to show the now-empty state. Verified live on the reference unit (OTA-flashed over WiFi): before reset `/api/stats/species` showed 4 species rows / 79 visits of accumulated test data across 2 log files; `POST /api/stats/reset` returned `{"ok":true,"deleted":2}`, both species and daily stats read back `[]`, and `/api/days` still listed all 209 of today's saved captures — photos untouched, exactly as specified.
- v1.15 — **Image rotation (mount-correction) implemented (firmware 0.14.0), extends §5.** New `rotation` setting (0&deg;/90&deg;/180&deg;/270&deg;, NVS-persisted) lets the physical mount orientation be corrected without re-gluing the camera module. The OV2640 only rotates 180&deg; in hardware (`camera_set_rotation()` toggles `hmirror`+`vflip` together — free, and since it happens at the sensor before JPEG encoding it corrects the live stream, SD-saved captures, and the species classifier all at once); there's no 90&deg;/270&deg; sensor register, and no JPEG-rotate routine exists in any dependency in this codebase, so real rotation at those two angles would mean a decode&rarr;rotate&rarr;re-encode step per frame — too expensive to run continuously at stream framerate. Scoped instead (user's explicit call) to **classifier-accurate, display-cosmetic** for 90&deg;/270&deg;: `classify.cpp`'s existing crop/resize loop (`decode_to_input`) gets its index mapping permuted for those two angles — same nearest-neighbor sampling, same source pixels, so there's no extra quality cost — while SD-saved JPEGs keep the sensor's native (unrotated) orientation at 90&deg;/270&deg;. The Live View `<img>` gets a pure CSS `rotate()` transform (via a `liveWrap` container sized with the classic centered-absolute + swapped-aspect-ratio trick) so what a person watching the stream sees is corrected too, even though the underlying bytes aren't. Settings tab gains the rotation dropdown (with the 90/270 caveat spelled out in-page); Live View tab gets its own lightweight rotation dropdown for a quick toggle without leaving the stream, both writing to the same `POST /api/settings` (`rot=`) and staying in sync. **Verified live on the reference unit** (over WiFi, no USB — OTA-flashed): a first visual check at 180&deg; showed no change, which turned out to be user/API state getting out of sync mid-test, not a firmware bug — a temporary REG04 register-readback debug field added to `/api/sysinfo` confirmed the hmirror+vflip bits do flip correctly (`0x28`&rarr;`0xF8`&rarr;`0x28`), and a clean re-test with a fresh capture showed the SD-saved JPEG genuinely rotated 180&deg; (roof edge and treeline flipped to the opposite corners); the debug field was removed once confirmed. 90&deg;/270&deg; classifier math was sanity-checked by POSTing a test photo to `/api/classify` at both angles — both returned clean, sane JSON (no crash, no out-of-bounds) — full accuracy verification needs a genuinely mis-mounted camera or a synthetic rotated test image, deferred until one is available. Also hit an unrelated **OTA flakiness at weak WiFi signal**: with RSSI around -88 dBm the ~1.1 MB upload reliably reset mid-transfer or hit the server's receive timeout (408); retrying once RSSI recovered to the -65&ndash;-70 dBm range succeeded immediately — no firmware fix, just a note that OTA over a marginal link may need a retry.
- v1.14 — **Species name language selection (firmware 0.13.0), extends §3.2.** Settings gains **Species name language: English / Norwegian**; the scientific (Latin) binomial is now always shown alongside the common name in whichever language is selected, so a result is never ambiguous even when a translation is missing. New `species_i18n.c`: a curated Latin→Norwegian lookup for ~80 species realistic at a Northern-European nest box or feeder (matching the v1 global model's documented regional deviation) — species outside the table still display correctly (English name + Latin), nothing is ever silently blank. `classify.cpp`'s decision logic now extracts the Latin binomial alongside the English common name (both come from the same Coral label, "Scientific name (Common Name)"); the visit-log CSV gains a trailing `latin` column (old rows without it parse fine — absent means "unknown", not an error). Localization happens **at display time**, not at write time: `/api/status` (ticker), `/api/stats/species`, and `/api/classify` all format the name server-side from the stored English name + Latin binomial + the current language setting, so changing the language setting immediately relabels historical data too, with no re-classification needed. A user-corrected species (Gallery relabeling, still deferred) intentionally drops the Latin name rather than misattributing the original guess's binomial to the correction. **A real bug caught during live verification:** `stats.c`'s original row parser used `strtok_r`, which collapses runs of adjacent delimiters — since the `corrected` column is always empty today (no relabeling UI yet), a row like `...,path,,Erithacus rubecula` made `strtok_r` skip straight past the empty `corrected` field and read the *latin* value into it instead, misattributing a real species' binomial as its own "corrected" label and overwriting the display name entirely (surfaced live as `/api/stats/species` returning a bare `"\n"` for pre-existing "unclassified" rows). Fixed by replacing the tokenizer with an explicit comma-scanning `next_field()` that never skips empty fields. Verified live on the reference device: `/api/classify` on the robin test photo returns `"European Robin (Erithacus rubecula)"` in English and `"Rødstrupe (Erithacus rubecula)"` in Norwegian from the same underlying event; `/api/stats/species` correctly relabels existing rows immediately on a language switch (`unclassified`/`ikke klassifisert`, `Unidentified bird`/`Uidentifisert fugl`) with no re-classification; the `lang` setting round-trips through NVS across a reboot. Firmware 0.13.0.
- v1.13 — **On-device species identification implemented (firmware 0.12.0), §3.2 core now live.** `classify.cpp` (TFLite-Micro via `espressif/esp-tflite-micro`, esp-nn kernels on the S3's vector unit): loads an int8-quantized 224×224 MobileNet-class classifier + index-aligned labels from `/sd/model` at boot (file chosen by the Settings region selector, else first `.tflite` found; ops verified against the reference model — only 6 kernels registered, keeping the op resolver minimal). Everything heavyweight sits in PSRAM: model ~3.9 MB, 3 MB tensor arena, decode buffers. **Asynchrony as specified:** the capture pipeline keeps a PSRAM copy of the event's best (first) frame and returns immediately; a dedicated classifier task (core 1, below motion priority) decodes it (esp_jpeg, auto-scaled → center-crop → nearest-resize to 224²), invokes the model, applies the settings confidence threshold and the "background" guard class ("no bird"), and only then writes the visit-log row — so CSV rows now carry real species + confidence, and the §3.4 stats species table/leaderboard becomes meaningful. Any failure (no model, queue full, decode error) falls back to the pre-§3.2 direct "unclassified" row: no event is ever unlogged. **v1 default model:** Google's iNaturalist-birds (965 species, Apache-2.0, global — a documented deviation from the planned Northern-Europe-only list, see §3.2 + `docs/MODEL.md`). The Coral download is uint8 and modern TFLM is int8-only ("Hybrid models are not supported"), so `tools/convert_model_int8.py` requantizes it offline — activations get a lossless zero-point shift, weights are requantized per-channel symmetric (worst-case error ½ LSB, no calibration data needed), biases recomputed exactly; the firmware feeds the int8 input by XOR 0x80 on the decoded pixels and scales output percentages from the tensor's quantization params. New endpoints (§6): `POST /model/upload?name=` installs model/labels files into `/sd/model` over the network (streamed under the §7 write lock, partial uploads deleted, no card-pulling needed) and `POST /api/classify` classifies a posted JPEG on demand (top-3 + decision JSON) — users can sanity-check their model without waiting for a bird. `/api/status` gains `species` (shown in the Live ticker), `/api/sysinfo` gains `clsModel`/`clsLabels`, and the Debug tab's Species ID card now shows model, label count and real last-inference timing. Settings' region dropdown now lists only `.tflite` files. Deferred to a later milestone: gallery label-correction + favorites (need per-event identity in the Gallery UI; the CSV `corrected` column and stats support for it already exist), and the on-flash model-partition fallback (SD required for now). Verified live on the reference device: clear Wikimedia test photos via `/api/classify` score European Robin 87% and Eurasian Blue Tit 86% (~4.3 s/inference); an obstructed great tit (top-2 Blue Tit 46%/Great Tit 28%) and a distant bird are both correctly guarded as "Unidentified bird"; a real motion event produced a classified visit-log row that surfaced in `/api/stats/species` and the status ticker; heap returns to ~1.1 MB free after inference (no leak, low-water 921 KB during decode). Firmware 0.12.0.
- v1.12 — **Retention pruning implemented (firmware 0.11.0), §3.1/§7 now fully live.** `storage.c` rechecks SD usage against `g_settings.sd_cap_pct` (already exposed in the Settings tab, default 80%) on every successful `storage_save_jpeg()` call — cheap when under cap (one `esp_vfs_fat_info()` call), so it costs nothing on the common path. Once over cap it deletes the oldest capture day-folder whole, one at a time (day-folder names sort lexicographically by date, so "oldest" is just the smallest name), rechecking usage after each deletion, bounded at 64 iterations so a corrupt state can't spin forever. **Today's own folder is never a pruning candidate**, even if it's the only folder left over cap — deleting the day currently being written to would race the capture still filling it; if nothing else is prunable the device logs a warning and stays over cap rather than eating today's captures. The `no-date` folder (pre-SNTP-sync captures) sorts after every real date, so it's the last resort rather than the first — its files' true age is unknown, unlike a dated folder's. **Species-favorite exemption is not yet implemented**: it arrives with §3.4's favorite-flagging (still deferred from the Gallery-tab milestone, v1.7) — there's no favorite flag on any event yet for pruning to check, so nothing is silently being ignored, there's just nothing to exempt. Verified live on the reference unit against real test data (not synthetic): with `2026-07-06` (18 files, prior milestones' test captures) and `no-date` (13 files, pre-sync test captures) sitting alongside today's `2026-07-07`, temporarily lowering the cap below actual usage triggered pruning that removed both old folders in oldest-first order across a single triggering write, correctly stopping once only today's folder remained. **That first test caught a real bug**: right after a reboot, before SNTP resyncs, the clock reads ~1970, so "today" computed as a bogus pre-2020 date that matched no real folder — the "never prune today" guard silently failed to recognize the actual current-day folder as today's, and a reboot-plus-motion-before-sync sequence wiped it. Fixed by having `prune_if_over_cap()` skip entirely (not just skip the today-check) whenever the clock reads pre-2020, mirroring `storage_save_jpeg()`'s own established pre-sync fallback — new captures already land in `/captures/no-date/` during that exact window anyway, so there's nothing dated to protect until the clock catches up. Re-verified after the fix: today's folder survives a low-cap trigger when it's the only folder present (clock synced), and a subsequent motion event correctly pruned a lingering `no-date` folder once the clock had synced, twice confirming the fixed guard. Firmware 0.11.0. **OTA Update tab implemented (firmware 0.10.0), §8 now live — all seven §5 tabs complete.** New `POST /ota/upload` (RemoteStart's proven pattern): raw `.bin` streamed straight to the inactive OTA slot via `esp_ota_write()`, boot partition switched only after a full, verified write (a partial upload just fails — it never leaves a half-written slot as the next boot target), bounded `httpd_req_recv()` timeout retries so one dropped packet doesn't abort a multi-hundred-KB upload (the v1.5 lesson). **The brick-proofing this milestone actually adds is bootloader rollback**, not just the upload plumbing: `CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE` turned on in `sdkconfig.defaults` (both targets — shared file), and `main.c` now calls `esp_ota_mark_app_valid_cancel_rollback()` once every subsystem has initialized without an `ESP_ERROR_CHECK` abort; an image that never reaches that call (crash-loops before finishing boot) gets automatically reverted to the previous slot on the next boot, with no user action needed. OTA tab: file picker + XHR upload with a progress bar, shown running version, and a plain-language explanation of the rollback safety net so users don't panic mid-flash. Verified live end-to-end over LAN: uploaded the running `BirdBox.bin` (1,065,856 bytes) back to the reference unit via `POST /ota/upload` — 200 OK in ~12 s, device rebooted into the alternate OTA slot, came back on the network reporting the same version with a fresh uptime, and a follow-up reset confirmed that slot reaches `boot complete` (and thus `esp_ota_mark_app_valid_cancel_rollback()`) cleanly — the rollback path was never triggered, as expected for a good image. Firmware 0.10.0.
- v1.10 — **Debug tab implemented (firmware 0.9.0), §5 now fully live; new `GET /api/sysinfo`.** Mirrors the RemoteStart Debug-card convention: a `housekeeping_task` in `main.c` samples `esp_get_free_heap_size()` every 10 s and tracks the lifetime low-water mark plus when it was hit (`g_heap_min`/`g_heap_min_ts_us`), so a slow leak shows up as an aging minimum long before the device actually runs low. Debug tab renders five cards from the one endpoint: **System** (free heap, low-water mark + age, uptime, WiFi reconnect count + last-reconnect age from the existing `g_wifi_disconnect_count`/`g_wifi_last_disc_ts_us`), **WiFi Link** (RSSI, channel, own MAC via `esp_wifi_get_mac`), **SD Card** (card CID name, size/free, and a **last-write-ok flag** as the closest thing to a "health" signal FAT/SDMMC exposes — no SMART equivalent exists for SD, so `storage.c` now tracks whether the most recent capture or visit-log write actually succeeded), **Camera** (sensor PID, resolution, current JPEG quality), and **Species ID** — which honestly reports "not available yet" via a new `classify_last_duration_ms()` stub (always -1) rather than fabricating a number ahead of §3.2. Verified live: heap/uptime/reconnect fields sane on the reference unit, SD card fields matched the mounted 64 GB card, camera card showed the correct sensor PID and live-adjustable quality, no-camera/no-SD states render their "bad" (red) status correctly. Firmware 0.9.0.
- v1.9 — **Settings tab implemented (firmware 0.8.0), §5 now live; NTP server made configurable.** `settings.c` moves off the scaffold: every field gets its own NVS key (not a blob) so a future new field falls back to its default instead of invalidating every stored setting on a version bump. Settings tab exposes placement mode, motion sensitivity/frames/interval/cool-down, confidence threshold, SD retention cap, stream/capture JPEG quality (`camera_set_quality()` re-programs the sensor live, no reboot), timezone, IR LED mode, and a **region selector for the future species model (§3.2)** — options are read live from whatever files sit in `/sd/model` (`GET /api/settings` lists them), so the region choice is future-proofed for the model-swap workflow before classification itself lands; an unmatched saved region shows "(missing from SD)" rather than silently reverting. **NTP server is now configurable** (§3.4/§5): a dropdown of common choices (pool.ntp.org default, regional pools, Google/Cloudflare/NIST) plus a Custom text field, applied via a new `wifi_restart_sntp()` that deinits/reinits `esp_netif_sntp` with the new hostname on a live connection — no reboot required, unlike the WiFi-tab's IP settings. All settings apply immediately per FSD §5's "no reflashing" rule; `POST /api/settings` clamps every numeric field server-side and rejects region/ntp values containing path or quote characters. Verified live: defaults round-tripped through `GET /api/settings` after a fresh flash, a full save (`nestbox`/UTC0/custom region+quality) read back correctly and survived a reboot, then reverted to defaults. Firmware 0.8.0.
- v1.8 — **Stats tab and WiFi tab implemented (firmware 0.7.0); §3.4 statistics and §4.5 static IP now live.** New `stats.c`: aggregates the monthly visit-log CSVs into daily/species/hourly buckets on request (newest 12 log files, 62 day / 24 species caps; a user-corrected label wins over the logged species; pre-SNTP "unsynced" rows count toward species totals but not the date/hour buckets, honouring §3.4's no-~1970-dates rule). New endpoints `GET /api/stats/daily|species|hourly` (§6); the ~2.6 kB aggregate lives on the heap, not an httpd stack, and the list responses stream chunked. Stats tab renders bar rows (visits/day, last 30), a 24-column hour-of-day histogram and a species table (visits, first/last seen) — pure inline JS, no CDN (§3.4). WiFi tab replaces the dashboard's link to the portal page: in-tab network scan + credential save, and the §4.5 IP configuration — DHCP (default) or static IPv4 with server-side validation (`400 invalid IPv4 address` — a syntactically bad static IP would associate but be unreachable, indistinguishable from a hang), persisted in NVS, applied to the STA netif before `esp_wifi_start()`; an invalid *saved* config degrades to DHCP rather than bricking the unit, and the §4.6 boot-button reset now erases IP config too — the documented recovery from an unreachable static address. `max_uri_handlers` 16 → 24 for the five new routes. Verified live on the reference unit: stats cross-check exactly against the card's 8 logged visits (6 in the 21 h bucket of 07-06 + 1 in the 6 h bucket of 07-07 + 1 unsynced species-only row); `999.1.2.3` rejected with 400; full static-IP round-trip — saved the current DHCP address as static, device rebooted and came back reachable with `static:true` and matching live values, then reverted to DHCP and confirmed. Firmware 0.7.0.
- v1.7 — **Gallery tab implemented (firmware 0.6.0), first slice of the §5 tabbed UI.** Dashboard rebuilt as the real tab bar (Live | Gallery | WiFi-link; remaining tabs land with their subsystems). Live tab: stream + status ticker + Snapshot-to-SD button; **leaving the Live tab drops the stream `img src` to free one of the two §3.3 stream slots**. Gallery tab: day selector built from new `GET /api/days` (day-folders + file counts), thumbnail grid from new `GET /api/events?date=` (files + sizes; lazy-loaded, newest first), click-through to full size, per-capture delete via new `DELETE /captures/...` (traversal-guarded, confirm dialog). Both listing endpoints stream chunked JSON — no fixed buffer to outgrow on a full card. Favorites and species-label correction (§3.4) deferred to the species-ID milestone, which brings the event metadata they attach to; `GET /api/events` currently lists files rather than the FSD §6 event objects — the API shape converges when events gain IDs/metadata with §3.2. Verified live: day list and file list match the card's real contents (incl. a 4-frame motion burst from §3.1's follow-up logic), throwaway snapshot created → deleted → confirmed 404. Firmware 0.6.0.
- v1.6 — **Motion-triggered capture implemented (firmware 0.5.0), §3.1 live.** `motion.c`: detection frames are the sensor's JPEGs decoded at 1/8 scale (SVGA → 100×75) via `esp_jpeg`, reduced to grayscale (green channel — differencing needs a stable transform, not colorimetry); per-pixel delta > 25 counts as changed; motion when the changed area exceeds a sensitivity-derived threshold (sensitivity 0→11 %, 50→6 %, 100→1 % of frame); rolling background is a 7/8-EMA updated **only on no-motion frames** so a stationary bird isn't absorbed mid-visit, and re-seeded after each event's cool-down. Event pipeline in the motion task: first frame saved immediately, then up to `capture_count`−1 follow-ups at `capture_interval_ms`, **stopping early when motion ends between frames**; then a visit-log CSV row (`storage_append_visit_log()`, monthly `/sd/log/visits-YYYY-MM.csv` with header on creation, species "unclassified" until §3.2) and the `cooldown_s` cool-down. §7's single-writer requirement satisfied by the motion task + write mutex (API snapshots share the mutex) — no separate queue task needed at this producer count. `/api/status` gains `motion`/`events`/`lastEvent`; dashboard ticker shows them with a link to the latest capture (2 s refresh). Verified live: a hand-wave in front of the reference unit triggered event #1 within seconds of boot, one frame saved (early-stop correct — the hand had left), status fields updated, frame fetched back over LAN. Firmware 0.5.0.
- v1.5 — **microSD storage implemented (firmware 0.4.0), §7 partially live.** `storage.c`: SDMMC 1-bit mount at `/sd` (CLK 39 / CMD 38 / D0 40 — the assumed Freenove layout from v1.2, now **verified live**: 64 GB card mounts and round-trips data on the reference unit); never auto-formats a user's card; creates `/captures` `/log` `/model`; missing/unmountable card degrades to a clear no-SD state (§7). `storage_save_jpeg()` writes `/captures/YYYY-MM-DD/HHMMSS.jpg` from the SNTP clock, uptime-named files under `no-date/` before first sync, same-second collisions get a letter suffix. Writes serialized by mutex for now — the §7 queue-based writer task is deferred to the motion-capture pipeline, its first asynchronous producer. New endpoints (§6): `POST /api/capture` (manual snapshot → SD, returns path+size), `GET /captures/...` (static JPEG serving from SD; wildcard route, path-traversal guarded), and `/api/status` gains `sdPresent`/`sdTotalMB`/`sdFreeMB`. Verified end-to-end over LAN: API snapshot saved with correct timestamp path and fetched back byte-identical. Remaining from §7: retention pruning (arrives with capture pipeline), visit-log CSVs (arrives with §3.4).
- v1.4 — **Live MJPEG streaming implemented (firmware 0.3.0), §3.3 now live.** `camera.c`'s temporary auto-probe replaced by real init against the `board_config.h` pin map: JPEG at SVGA 800×600 (the §3.3 stream default), 2 frame buffers in PSRAM, `CAMERA_GRAB_LATEST` so a slow client can't back the sensor up; a missing camera degrades to a clear "no camera" state (503 on `/stream`, `camera_available()` for the UI) instead of aborting boot. New `GET /stream`: `multipart/x-mixed-replace` MJPEG. **Design note: ESP-IDF's httpd is single-threaded, so an endless synchronous stream handler would block every other request; each stream client runs in its own FreeRTOS task via `httpd_req_async_handler_begin()` on port 80** — considered and rejected the esp32-camera example's second-httpd-on-:81 approach, which would break the FSD §6 single-origin API. Max 2 concurrent clients (503 beyond, §3.3). Root dashboard now embeds the live stream with a 5 s status ticker. Verified end-to-end over the LAN on the reference unit: ~17 JPEG frames pulled in 4 s by an external client, sample frame decoded correctly, `/api/status` responsive while streaming. Firmware 0.3.0.
- v1.3 — **WiFi provisioning implemented (firmware 0.2.0), §4 now live.** `wifi.c` + `web_server.c` port the RemoteStart flow: first-boot SoftAP portal (`BirdBox-Config`/`birdbox1234`, setup page at 192.168.4.1 with network scan, URL-decoded credential save to NVS, reboot); boot connect 15 s/5 retries; **on connect-failure with stored credentials the portal opens in APSTA mode and retries the stored network every 60 s in the background, rebooting into normal mode if it succeeds** (§4.4 — implemented as specified, note this *differs* from RemoteStart, which stops STA retries entirely while its portal is open); once ever-connected, in-service disconnects retry indefinitely and never reopen the portal; `WIFI_PS_NONE` + max TX power; boot button (GPIO0) held ≥ 5 s at power-up erases credentials; SNTP (`pool.ntp.org`) + settings timezone started on connect. Web server carries the RemoteStart httpd lessons (LRU purge, handler headroom, loud registration failures) and serves: portal/setup page, placeholder dashboard, `GET /api/scan`, `GET /api/status` (minimal), `POST /api/reboot`, `POST /wifi-save`. Verified live on the reference unit: portal AP + DHCP up, 6 routes registered, camera probe unaffected. Static IP (§4.5) and the full WiFi tab remain for the web-UI milestone.
- v1.2 — **Reference hardware identified and pinned (§2.1).** The project's physical unit (sold as "ESP32-CAM OV2640" on eBay) is actually a generic **ESP32-S3-CAM, N16R8** (ESP32-S3 QFN56 rev v0.2, 16 MB flash, 8 MB embedded octal PSRAM) — identified via esptool chip probe plus a temporary firmware auto-probe in `camera.c` that tried known S3 camera pin maps until SCCB answered: the ESP32-S3-EYE/Freenove map matched (XCLK 15, SIOD/SIOC 4/5, OV2640 at 0x30, PID 0x26), confirmed with a live 800×600 JPEG test frame. `board_config.h` gains a `BOARD_ESP32S3_CAM_GENERIC` block (now the default) with the verified camera pins; LED and SD pins marked TODO-verify. S3 build bumped from assumed 8 MB to real 16 MB flash: OTA slots grown to 2×4 MB and a new 7.9 MB FAT `model` data partition added as the on-flash fallback location for the species model (§3.2). The camera probe stays in `camera.c` until the real capture pipeline replaces it — it doubles as a bring-up diagnostic for other boards.
- v1.1 — Project scaffolded (firmware 0.1.0): ESP-IDF project at repo root with per-target `sdkconfig.defaults.esp32s3`/`.esp32` and partition tables (8 MB S3 with 2×3 MB OTA slots; 4 MB ESP32-CAM with the RemoteStart layout), one stub module per subsystem in `main/` (`wifi` `web_server` `camera` `motion` `capture` `classify` `settings` `storage`, each marked `TODO(FSD §…)`), `esp32-camera` as a managed component, `board_config.h` with pin maps for Seeed XIAO ESP32S3 Sense (default) and AI-Thinker ESP32-CAM, CI workflow (build both targets on every push; `v*` tag → GitHub Release with both `.bin`s), README, MIT LICENSE, docs/ placeholder. No functional requirements changed.
- v1.0 — Initial specification.

---

## 1. System Overview

BirdBox is an open-source, WiFi-connected nest box / bird feeder camera built on an **ESP32-S3 camera module** running **ESP-IDF** (native Espressif SDK — no Arduino framework). It watches a nest box or feeder, detects bird activity, captures photos/short clips, identifies the species with an on-device AI model, and serves everything through its own built-in web UI: live view, capture gallery, and visit statistics.

The device is fully self-contained: no cloud service, no companion app, and no external server is required. Everything — capture storage, species identification, the web dashboard — runs on the device itself, with captures stored on a microSD card. It is designed for public/open-source users: any hobbyist should be able to buy the supported hardware, flash a release binary, join it to their WiFi via the first-boot portal, and mount it on a nest box.

```
 Bird activity ──► Camera + PIR ──► Motion detect ──► Capture (photo/clip) ──► microSD
                                                          │
                                                    Species ID (on-device AI)
                                                          │
 Browser ◄──── Web UI: Live stream | Gallery | Statistics | WiFi | OTA
```

**Development framework:** ESP-IDF v6.x (native Espressif SDK). Project structure, build system (`idf.py`/CMake), WiFi provisioning, web server, OTA and CI conventions follow the proven patterns from the RemoteStart project (`D:\SteinsRootMappe\Claude\RemoteStart`).

---

## 2. Hardware

### 2.1 Target board

| Board | MCU | Camera | PSRAM | Role |
|---|---|---|---|---|
| **Generic "ESP32-S3-CAM"** (N16R8: 16 MB flash, 8 MB octal PSRAM; ESP32-S3-EYE/Freenove-compatible camera pin map) | ESP32-S3 (dual-core LX7, vector instructions) | OV2640 | 8 MB (required) | **Primary target & the project's reference unit** (identified 2026-07-06 by SCCB probe: sensor PID 0x26, camera pin map verified live). Other S3 camera boards (XIAO ESP32S3 Sense, Freenove, S3-EYE) supported via `board_config.h`. |
| AI-Thinker ESP32-CAM | ESP32 classic | OV2640 | 4 MB | Secondary/constrained target: capture, streaming and gallery work; on-device species ID reduced or disabled (§3.2) |

Exact pin maps are board-specific and defined per-board in a `board_config.h`; the FSD does not fix GPIO numbers. Required peripherals:

| Peripheral | Interface | Purpose |
|---|---|---|
| Camera sensor (OV2640 or better) | DVP via `esp32-camera` component | Photo/clip capture, live stream, motion detection |
| microSD card | SDMMC (1-bit or 4-bit) or SPI, FAT32 | Capture storage, visit log database |
| PIR motion sensor (optional) | GPIO input | Low-power wake / first-stage motion trigger |
| IR LED illuminator (optional) | GPIO output | Inside-nest-box viewing in darkness (requires IR-filter-less sensor variant) |
| Status LED | GPIO output | Boot/WiFi/portal state indication (blink patterns, RemoteStart-style) |

### 2.2 Placement variants

The same firmware supports two mounting scenarios, selected in settings (§5):

- **Nest box mode** — camera inside a closed box, close focus, IR illumination, activity is relatively rare and long-duration (nesting).
- **Feeder mode** — camera pointed at a feeder/perch in daylight, activity is frequent and short (visits of seconds), species variety is high.

The mode primarily tunes motion-detection sensitivity, capture cadence and statistics presentation; the feature set is identical.

---

## 3. Functional Requirements

### 3.1 Motion-triggered capture

- Continuous camera-based motion detection: low-resolution grayscale frame differencing against a rolling background, with configurable sensitivity threshold and a minimum-changed-area filter to reject leaves/light changes.
- **Run HD, not SXGA — detection resolution matters (verified on-device 2026-07-13).** Detection reuses the *same* camera stream as capture (the OV2640 exposes one resolution; there is no separate low-res detect feed), differencing each JPEG decoded at 1/8. **SXGA (1280×1024) degrades detection vs HD (1280×720)** two ways: *sensitivity* — larger frames are slower to grab+decode, so the fixed-period detect loop samples less often and misses quick hops (SXGA also sits at the exact 1/8 decode-buffer ceiling, 160×128, with zero headroom and ~300 KB more PSRAM pressure — measured ~547 KB free vs ~854 KB at HD — which correlated with an OOM reboot); *framing* — SXGA's 5:4 field of view is taller/narrower and mis-aligned to a horizontal feeder, where HD's 16:9 keeps birds in-view. There is no clean fix on this sensor (a dual detect/capture stream isn't available; per-frame res-switching is too slow and its FOV change would break the ROI-crop box). **HD is therefore the locked default (§5, firmware 0.61.0).** SXGA's only advantage — more pixels for the classifier — is dissolved by ROI-crop (§3.2.3), which maximizes pixels-on-bird from HD anyway; recover perceived still sharpness with JPEG quality (lower `stream_quality`) instead. Above SXGA is unusable outright: UXGA's 1/8 (200×150) overruns the detect buffer and silently disables motion.
- Optional PIR pre-trigger: when a PIR sensor is fitted, it acts as the first stage (and, in a future battery variant, a deep-sleep wake source); camera-diff confirms before capture.
- On trigger, the device captures a **visit event**:
  - 1 full-resolution JPEG immediately, plus up to N (default 5, configurable) follow-up frames at a configurable interval while motion persists,
  - optionally a short MJPEG clip (default off — SD write bandwidth permitting).
- Debounce/cool-down (default 10 s, configurable) so one continuous visit produces one event, not dozens.
- Each event is written to microSD as `/captures/YYYY-MM-DD/HHMMSS_<seq>.jpg` plus one row in the visit log (§3.4).
- Retention: configurable cap on SD usage (default 80 %); oldest day-folders are pruned first. Events whose species ID is flagged "favorite" by the user are exempt from pruning.

### 3.2 AI species identification

- Every visit event's best frame is classified on-device by a quantized bird-species model (esp-dl / TFLite-Micro int8, running on the S3's vector unit).
- Model: a compact classifier (MobileNet-class, ~1–4 MB, loaded from SD) trained on garden/feeder species. The model file lives on the SD card so users can swap models/regions without reflashing (Settings → Region, or `POST /model/upload`). **v1 default: Google's iNaturalist-birds model** (MobileNetV2, 965 species worldwide incl. a "background" guard class, Apache-2.0; distributed uint8, converted once to int8 with `tools/convert_model_int8.py` — see `docs/MODEL.md`) — global rather than the originally-planned Northern-Europe-only list, since it's a proven, freely downloadable model that covers the Northern-Europe species; purpose-trained regional models remain the intended upgrade path.
- Output per event: top-3 species with confidence scores; results below a configurable confidence threshold (default 60 %) are labeled **"Unidentified bird"** rather than guessed.
- Species names display in a user-selected language (Settings → Species name language: English or Norwegian; see `species_i18n.c`); the scientific (Latin) binomial is always shown alongside it regardless of language, so identification is never ambiguous.
- A "no bird" class guards against false triggers (squirrels, wind); such events are kept but flagged, and can be auto-pruned.
- The user can correct a wrong label from the gallery; corrections are stored with the event (and can optionally be exported to help improve the shared model — see §9).
- **ESP32-classic fallback:** on non-S3 hardware inference is disabled or limited to bird/no-bird; the UI shows captures as "Unclassified". All other features work identically.
- Classification is asynchronous: capture and storage are never delayed waiting for inference; the label attaches to the event when ready (typically < 5 s later).

#### 3.2.1 Species-model data sources — evaluation, current selection, and regional-retrain path

This section records why the v1 model was chosen and what the realistic upgrade paths are, so the choice is auditable and a future maintainer doesn't re-tread the same search. Researched 2026-07-07.

**The hard constraint** is the device, not the availability of bird data. A drop-in model for BirdBox must be: fully **int8**-quantized (input *and* output), input **1×224×224×3** RGB, built only from the ops the firmware registers (`CONV_2D`, `DEPTHWISE_CONV_2D`, `ADD`, `AVERAGE_POOL_2D`, `FULLY_CONNECTED`, `SOFTMAX`, `PAD` — i.e. MobileNet-family; `PAD` was added in firmware 0.49.0 for Keras MobileNetV2's `ZeroPadding2D`, FSD v1.68), **≤ ~4.5 MB** to fit flash/PSRAM budget with a ~3 MB tensor arena, and shipped with an index-aligned label file. Most public "bird recognition" resources fail one of these before accuracy even enters the picture.

**Sources evaluated and *not* usable as-is (and why):**

| Source | What it is | Why it can't be dropped in |
|---|---|---|
| **BirdNET** (Cornell/TU Chemnitz) | The mature open bird-ID model, ~984 NA+EU species | **Audio**, not vision — needs an I2S mic (out of v1 scope, §11); models are **CC-BY-NC-SA** (non-commercial). Different modality entirely, not a variant of this model. |
| **Merlin Bird ID Photo** (Cornell) | Best-in-class consumer photo ID | **Closed source** — no redistributable model or weights. |
| **NABirds** (Cornell/Visipedia), **CUB-200-2011** (Caltech-UCSD) | Annotated image *datasets* (~400 / 200 species, N. America) | Datasets, not models — research-oriented licensing, no license-clean pre-quantized TFLite export. Would require full retraining, and species coverage is North-American. |
| **OSEA** | Offline tool, claims 10,000+ species, pretrained model | Sized for phone/CLI; adapting to an ESP32's ≤4.5 MB / 3 MB-arena budget is a project, not a swap. |
| **birder** (PyPI) | Apache-2.0 wildlife-CV framework, incl. MobileNet + quantized variants | **PyTorch** — needs a PyTorch→ONNX→TF→TFLite conversion, and *some* bundled weights derive from **CC-BY-NC**. Promising but not turnkey. |
| **Kaggle "450/525 bird species"** trainings (e.g. MobileNetV2, ~96 %) | Community training repos | Training repos over Kaggle datasets, not int8 TFLite; species sets skew to showy/global birds, thin on European garden species; per-uploader licensing is inconsistent. |
| **astrocoding/model-ai-bird-classification-tflite** | A MobileNetV2 **TFLite** bird model — closest in format | Small/undocumented species set, likely float32 (would still need `convert_model_int8.py`), license unspecified. Worth a look only if its label set happens to suit. |

**Current selection (v1 default) — Google/Coral iNaturalist-birds, and why it wins:** it is the only freely available model that already satisfies *every* device constraint at once — MobileNetV2 (ops match), 224×224 RGB, ~3.9 MB after int8 conversion, ships with an index-aligned label file, 965 species covering the Northern-European garden/feeder set plus a `background` guard class, and — decisively — **Apache-2.0**, the only commercially-clean license among the credible candidates. Its one documented weakness is that it's *global*, so it can produce geographically absurd top-1 guesses for anything off-threshold (e.g. "Wild Turkey"/"Greater Roadrunner" surfaced during unrelated testing); the confidence threshold + `background` guard contain this, and it never emits a confident wrong label above threshold (acceptance criterion §12.3). Conversion caveat: the Coral download is uint8 and modern TFLM is int8-only, hence the one-time `tools/convert_model_int8.py` requantization (per-channel symmetric weights, ½-LSB worst case) documented in `docs/MODEL.md` and [[tflm-int8-conversion]].

**Intended upgrade path — a regional retrain, not a different database.** The highest accuracy-per-effort improvement is a **Northern-Europe-subset retrain of this same iNat lineage**, not sourcing a new dataset: same Apache-2.0 provenance, same proven on-device format, but a smaller label set → smaller/faster model and far fewer out-of-region false positives. Sketch of the pipeline (offline, on a PC/GPU — none of this runs on the device):
1. **Species list:** define the target Northern-European garden/feeder/nestbox species (align with the `species_i18n.c` Norwegian table already curated for ~80 species).
2. **Data pull:** fetch labeled images per species from **iNaturalist** (research-grade observations, CC-licensed) and/or **GBIF** occurrence media — filter to the target list and to a sane per-species count.
3. **Transfer-learn:** start from a MobileNetV2 224×224 backbone (ImageNet or the iNat-birds weights), retrain the classifier head (and optionally fine-tune upper blocks) on the regional set, keeping the op set within the seven the firmware registers.
4. **Quantize:** post-training **full-int8** quantization (input+output int8) with a small representative image set — or, if a uint8 export is produced, reuse the existing `tools/convert_model_int8.py`.
5. **Package & install:** emit `<region>.tflite` + index-aligned `<region>.txt`, drop onto SD or `POST /model/upload`, select under **Settings → Region / species model**. No firmware change — the model is data on the SD card by design.

This stays consistent with §9's "shared model improvement" idea (user label-corrections could feed such a retrain) and keeps the device's plug-a-model-on-the-card contract intact.

**Concrete next step — a Nordic/boreal retrain (scoped 2026-07-09).** Prompted by a live miss: the reference unit repeatedly captured a Siberian Jay (*Perisoreus infaustus* — "Lavskrike"), correctly logged every time as "Unidentified bird." Investigation found this isn't a framing/blur/exposure bug (some of the captures were sharp) — the v1 model's 965-species label set simply doesn't contain the species at all (confirmed against the actual Coral label list), and it's also absent from the `species_i18n.c` Northern-European table, so the region filter excludes it too. A GitHub-wide search for a ready-made replacement (any TFLite bird model, any license) turned up nothing better: every alternative found is in the same "~1000 common feeder/garden species" tier, so swapping models wouldn't fix this — coverage of boreal specialists is a gap across the ecosystem, not just this one model. This sharpens step 1-2 of the retrain sketch above into a concrete plan:
1. **Species list:** audit the existing `species_i18n.c` ~80-species table against a broader taxonomy reference for gaps like Lavskrike, and extend it to ~100-150 species covering boreal specialists (Siberian Jay, Siberian Tit, Three-toed Woodpecker, etc.) alongside the existing common feeder/garden set. Google's **SpeciesNet** (`google/cameratrapai`, Apache-2.0, a camera-trap-specific classifier covering 2000+ species incl. Siberian Jay) is a useful cross-check for which species have real camera-trap-viable training signal behind them, but is **not** a usable data or model source directly — it's an EfficientNetV2-M two-model ensemble (object detector + classifier) built for desktop/cloud inference, far outside this device's ≤4.5 MB int8 MobileNet budget, and its 65M-image training corpus isn't published/redistributable (only trained weights are). Checked LILA BC (the camera-trap dataset library behind MegaDetector/SpeciesNet) for a usable Nordic image source directly — found none; its public datasets skew North America/Africa/South America.
2. **Data pull (unchanged in source, sharpened in method):** iNaturalist research-grade observations + GBIF occurrence media remain the realistic image source, filtered to the target species list and a Nordic/Fennoscandian region.
3. **Close the domain gap (new):** every real capture examined today was edge-clipped, motion-blurred, extreme-close-range, or some combination — nothing like the clean centered medium-distance photos iNaturalist/GBIF mostly contain. Training on those as-is risks a model that's accurate in benchmarks but not on this device's actual images. Mitigate by augmenting the pulled images with synthetic motion blur, aggressive edge-clipping crops, and close-range perspective distortion before training, deliberately matching this camera's real capture conditions rather than generic bird photography.
4. **Transfer-learn from the current `inat-birds-v1` checkpoint** (not ImageNet from scratch), retrain the head and optionally fine-tune upper blocks on the expanded regional set — same MobileNetV2 224×224 architecture, same op constraint (seven builtins since 0.49.0).
5. **Quantize/package/install unchanged** (steps 4-5 of the sketch above): `tools/convert_model_int8.py`, `<region>.tflite` + `<region>.txt`, SD card or `/model/upload`, no firmware change. The new model's label set would already be Nordic-scoped, so the region filter becomes a no-op for it rather than needing new logic.
6. **Validate** against already-known-hard real captures (today's Lavskrike and sheep events) before any field redeployment — a concrete regression check rather than only benchmark accuracy.

Not yet scoped: exact per-species image-count targets, training-infra setup (a Colab GPU session is sufficient — training itself is cheap; data collection and the augmentation pipeline are the real effort), and how many of the ~100-150 target species end up obtainable at usable quality from iNaturalist/GBIF — those are only knowable once the data-pull step actually runs.

**Shipped as a lighter-weight alternative to a retrain — the region *filter*.** Rather than a new model, **Settings → Species set → Northern Europe only** restricts the *existing* global model's output to the ~80-species Northern-European set (the same `species_i18n.c` list, matched by Latin binomial): at inference the ranking skips any class whose binomial isn't in the set (the `background` guard and any no-binomial label are always kept), so the model physically can't return an out-of-region species. Confidence is deliberately **not** renormalized — an out-of-region animal simply loses its winning class and the best in-region score falls below the threshold, landing as "Unidentified bird" instead of a confident wrong guess. It's a decision-layer restriction, so inference speed and in-region accuracy are unchanged; it only removes out-of-region false positives (the "Wild Turkey at a Norwegian feeder" failure mode). Robustness: the filter auto-disables for a model whose labels don't match the set (0 in-region labels → no filtering), so pairing it with an unrelated model can't blank every result; the Debug tab reports the in-region label count. Default off (global), so the device ships region-neutral. This is the pragmatic first rung of the retrain ladder above — most of the false-positive benefit, none of the training infrastructure.

#### 3.2.2 Closed-loop retraining pipeline (human-in-the-loop) — end-to-end feature and build plan

Scoped 2026-07-11. §3.2.1 records *which* model and *why*, and sketches the offline retrain in the abstract. This section formalizes the **operational loop** that turns everyday use into model improvement: a human confirms species on the device → those confirmations become a labeled dataset on a PC → a fine-tune produces an improved model → it is quantized and pushed back to the SD card. Each pass tightens accuracy on the local species set under this device's real capture conditions — the domain gap stock photography can't close (§3.2.1 step 3). It is the concrete realization of §9's "user label-corrections feed the shared model," kept fully local (no telemetry; nothing leaves the device automatically).

**Four stages, and what already exists.** Three of the four are built; the fine-tune in the middle is the gap.

| Stage | Mechanism | Status |
|---|---|---|
| 1. Human label collection on the device | Gallery ✎relabel / ✓confirm / 🔍identify write the `corrected` column; 5 per-image states; **Near threshold** rescue filter (§3.4/v1.65); double-click = no-bird. Images already on SD as captures. | **Built** (fw 0.47.1) |
| 2. PC-side retrieval | `GET /api/labels/confirmed` → `training-data/export-labels.ps1` → `dataset/<class>/*.jpg` + `labels.csv`; idempotent. `pull-new-captures.ps1` for blind pulls. | **Built** |
| 3. Off-device fine-tune | — | **Not built** (core new work) |
| 4. Quantize + install + close loop | `tools/convert_model_int8.py` (uint8→int8); `POST /model/upload` accepts `<region>.tflite` + index-aligned `.txt` into `/sd/model`; no firmware change. | **Partial** — install path exists; validation, versioning, and label-map regen do not |

**The new work (gaps):**
1. **Training script (stage 3, the core).** Transfer-learn from the `inat-birds-v1` MobileNetV2 224×224 checkpoint (not ImageNet from scratch), retrain the head and optionally upper blocks on the merged set (device `dataset/` + iNat/GBIF stock for pose diversity, §3.2.1 steps 2-3), with the capture-condition augmentation of §3.2.1 step 3 and class-weighting for the inevitable per-species imbalance. Must stay within the seven registered ops (§3.2.1) and the ≤4.5 MB / ~3 MB-arena budget. Runs offline on a Colab GPU.
2. **Label-map / model contract (stage 4).** The device maps output index → name from the index-aligned `.txt` sidecar, and `species_i18n.c` maps those to Norwegian. A retrain — especially a reduced Nordic class set — reorders classes, so the `.txt` **and** the `species_i18n` entries must be regenerated from the *same* class list the training used. This coupling is the sharpest correctness risk: a mismatched label file mislabels every inference silently.
3. **On-device validation harness (stage 4).** Before trusting a candidate, run it over a held-out set of already-labeled real captures via `POST /api/classify` and compare top-1 accuracy against the current model (a concrete regression check per §3.2.1 step 6, not just benchmark accuracy). Gate the install on it.
4. **Model versioning / rollback (stage 4).** Keep the prior `.tflite` so a regressive retrain reverts — mirrors the OTA rollback posture (§8).
5. **Stage 1/2 polish (optional).** Guard human-labeled captures against retention pruning (§7) so training candidates aren't deleted before export; a scheduled `export-labels` refresh instead of manual runs.

**Build plan — prove the loop small first.** The recommended sequence front-loads the end-to-end proof over species breadth:
- **Phase A — data + decision.** Choose the class set; start deliberately tiny (e.g. Dompap ♂/♀, Nøtteskrike, Lavskrike, `no_bird`). Build the stock-image fetcher and the dataset assembler (merge device+stock, train/val split).
- **Phase B — training.** Get a float model that beats stock on the held-out val set.
- **Phase C — quantize + contract.** Int8 convert; regenerate `.txt` + `species_i18n` from the class list; op/size check.
- **Phase D — validate + install.** Run the on-device harness; if better, `/model/upload`; retain the old model.
- **Phase E — ergonomics (optional).** Scheduled pull; pruning protection.

A throwaway ~3-class proof through Phases A-D validates the whole pipeline (and de-risks the label-map coupling and the int8 accuracy drop) before scaling to the ~100-150 species of §3.2.1.

**Effort estimate.** Engineering is small; **data volume is the long pole.**

| Item | Focused-work effort |
|---|---|
| Class-set decision + label-contract design | 0.5 d |
| Stock-image fetcher (iNat/GBIF, dedup, baseline-JPEG) | 1 d |
| Dataset assembler (merge, split, class weights) | 1 d |
| Training script (transfer-learn, augmentation, op-constraint) | 1.5-2 d |
| Quantize + representative-set wiring (converter exists) | 0.5-1 d |
| Label `.txt` + `species_i18n` regeneration from class list | 0.5 d |
| On-device validation harness (`/api/classify` over held-out set) | 1 d |
| Model versioning / rollback + docs | 0.5 d |
| Automation + pruning-protection (optional) | 0.5-1 d |
| **Engineering total** | **≈ 6.5-8.5 days** |

The gating cost is **not** engineering: a usable retrain needs on the order of **150-300+ confirmed images per priority species across varied visits, light, and individuals** (variety over raw count). At the reference unit's current capture/label rate that is **weeks of passive collection plus human labeling time** — realistically 3-8 weeks to a first usable Nordic set, though the 3-class proof can start with far fewer. This matches §3.2.1's "data collection and the augmentation pipeline are the real effort."

**Risks:** (1) label-map/model coupling — regenerate `.txt` + `species_i18n` together or every label is silently wrong; (2) int8 quantization can erode the fine-tune's gain — the validation harness is the guard; (3) label noise (e.g. the fat-fingered `"dom"` typo-class, v1.51-53) needs a cleaning pass in the assembler; (4) a fine-tuned graph must not introduce an op outside the registered seven (`train.py`'s verify step checks this — it's how `PAD` was caught).

#### 3.2.3 Image → model-input contract (how a picture becomes classifier input)

**This is the single most skew-prone part of the pipeline. The rule is absolute: the preprocessing that turns a JPEG into the model's input tensor must be byte-for-byte the *same operation* during training (`train.py`) and on-device inference (`classify.cpp` `decode_to_input`). Any divergence silently mistrains/misclassifies — it cost a whole model generation (v0.2), see below.**

The model input is a **fixed `1×224×224×3` int8 tensor**. A camera frame of any resolution/aspect must be reduced to exactly that. The contract has three stages, identical on both sides:

1. **Decode to RGB.** Device: `esp_jpeg` decodes the JPEG at a power-of-two downscale (1/2…1/8) chosen so the crop region still exceeds 224 px (baseline JPEG only). Training: `tf.io.decode_image(..., channels=3)`. Both yield RGB.
2. **Geometry — crop a square, never stretch.** Take a **square region** of the frame and resize it to 224×224 with **nearest-neighbour** sampling (`sx = x*side/224`). The square is:
   - **Whole-frame mode (`detect_zoom` off, the default):** the largest **centered** square, `side = min(w,h)` — the classic center-crop. This is **aspect/resolution independent** (a centered square is the same shape whether the source is 16:9, 4:3, or 5:4). ***Never* squash the full rectangle into the square** — squashing distorts the bird's proportions differently per source aspect and is the v0.2 skew (see below).
   - **ROI mode (`detect_zoom` on):** a square centered on the **motion ROI** (`motion.c`'s winning cluster, logged in the visit-log `roi` column as `"x0-y0-x1-y1"` fractional), `side = max(roi_w, roi_h)` clamped to the frame, so the bird fills the input regardless of where/how big it was in frame. Exact math: `classify.cpp` `decode_to_input` (`rx0 = roi.x0*w+0.5`, square-expand on ROI center, clamp to frame). Training must crop each image to its logged ROI with the same math.
3. **Quantize to the int8 contract.** Input tensor is **int8, zero_point 0, scale 1/128**. The firmware feeds `pixel ^ 0x80` (== `pixel − 128`) straight in and does **not** apply the tensor's scale/zero-point (`classify.cpp:420-427`). So the model must be trained on `x = (pixel − 128) / 128 ∈ [−1, 1)` and quantized so the input tensor has `zero_point == 0`. Output is int8; the head stays within the **seven registered builtins** (`CONV_2D, DEPTHWISE_CONV_2D, ADD, AVERAGE_POOL_2D, FULLY_CONNECTED, SOFTMAX, PAD`) and the ~4.5 MB / ~3 MB-arena budget. `train.py`'s verify step asserts input dtype/shape/zp, the op set, and output dtype before an artifact is allowed to ship.

**Why this section exists (the v0.2 lesson).** `train.py` originally *stretched* the whole frame to 224² (`tf.image.resize`) while the device *center-cropped* it. The model learned stretched geometry, was served center-cropped geometry, and confidently misclassified a clear off-aspect Dompap as Lavskrike — while validating at a fake 99% because the leaky per-frame split hid it. Fixed in the v0.3 `train.py` by replacing stretch with the device-matching `center_square()`. Corollary rules:
- **Rotation** (§5): 0/180° are baked into the saved JPEG; 90/270° are applied as an index permute in `decode_to_input`. Training crops in raw-frame axes, so a 90/270° *display* rotation would reintroduce a train/serve mismatch — retrain at the deployed rotation, or in raw-frame space.
- **Resolution is a training-data contract, not a quality knob.** Extra pixels never reach the model (everything ends at 224²); what matters is that capture aspect matches what the model was trained on. Center-crop makes the model aspect-independent; ROI-crop additionally makes it position/scale independent. After any NVS wipe, re-verify `res` (and `sens`/`zone`/`conf`) — a reset to a different resolution/aspect silently degrades classification.
- **ROI availability:** the ROI is only useful for training if it was logged — logging was decoupled from `detect_zoom` in fw 0.60.0, so only captures from 0.60.0 onward carry a `roi`; earlier frames are whole-frame-only.

### 3.3 Live streaming / remote viewing

- MJPEG live stream over HTTP (`/stream`), viewable in any browser on the LAN — embedded in the web UI's Live tab and usable as a direct URL (e.g. as a camera source in Home Assistant/VLC).
- Configurable stream resolution/quality, independent of capture resolution; default 800×600 to keep capture quality unaffected.
- At most 2 concurrent stream clients (memory bound); further clients get HTTP 503 with a clear message.
- Streaming and motion detection coexist: while a client streams, motion detection keeps running (detection uses its own low-res frames).
- Remote (outside-LAN) viewing is explicitly **out of scope for the device itself** — no cloud relay, no port-forwarding automation. The documented path is the user's own VPN (WireGuard/Tailscale) to their LAN. This is a deliberate security decision for an open-source device.

### 3.4 History & statistics

- Every visit event is appended to a visit log on SD (`/log/visits.csv`, append-only, one file per month): timestamp, species, confidence, frame count, file paths, user correction.
- **Gallery tab:** browse captures by day; each event's first frame is badged with its species + confidence (joined from the visit log by frame path, localized to the display language); view full-size frames, delete a single capture, multi-select + delete, delete a whole day (photos only), or **wipe a day** (photos + that day's statistics in one action). Favorites (pruning-exempt) and in-gallery label correction remain deferred.
- **Statistics tab:**
  - visits per day/week/month (bar chart),
  - species leaderboard (distinct species, visit counts, first/last seen),
  - activity-by-hour-of-day profile,
  - "new species" flag when a species appears for the first time.
- Charts are rendered client-side in the web UI from JSON APIs (§6); the device only serves data, keeping firmware HTML small (RemoteStart pattern: single-file embedded UI, no external CDN dependencies — the UI must work with no internet access).
- Time base: SNTP, server configurable in Settings (default `pool.ntp.org`), timezone configurable (default `Europe/Oslo`, auto-DST). Capture files are date-time named (`YYYY-MM-DD_HH-MM-SS.jpg`) under a `YYYY-MM-DD/` day-folder.
- **Offline / no-NTP fallback (§3.4.1):** on a network without a reachable NTP server (no internet uplink, or UDP-123 blocked), SNTP never syncs and the clock stays at ~1970 — so captures fall back to `/captures/no-date/`. To avoid this, the web UI posts the **browser's clock** to `POST /api/time` on page load and before every snapshot; if (and only if) SNTP hasn't synced, the device sets its clock from it (`settimeofday`, sanity-checked > 2023) and re-applies the timezone. SNTP remains authoritative — a browser clock never overrides an already-synced time. Any captures already stranded in `no-date/` from before the clock was set stay there (harmless); new captures are correctly dated. The current device time and its source (`ntp`/`manual`/`none`) show in the live-view status line and the Debug tab (`time`/`clockSrc` in `/api/status` + `/api/sysinfo`).
- The timezone (`g_settings.timezone`, POSIX form) is applied at boot right after settings load — before any capture path uses `localtime` — not only on WiFi connect, so date folders/filenames and the displayed time are in local time from the first frame.

### 3.5 Camera watchdog & recovery

**Problem (observed in the field, firmware ≤0.18.0):** after several hours of running, the OV2640 can stop delivering frames — `esp_camera_fb_get()` returns `NULL` on every call while SCCB still reads the sensor PID, so `camPresent`/`camPid` look healthy but the live stream and captures are dead. The stall survives a soft `/api/reboot` (the ESP32 restarts but the sensor stays powered and latched); only removing the sensor's VDD clears it. Confirmed live: a battery disconnect restored video where a reboot did not.

**A worse failure mode, and the design lesson (learned in live test of the first cut).** As the reference unit's camera degraded, a second mode appeared: instead of returning `NULL`, `esp_camera_fb_get()` **spins CPU-bound** on corrupt frames (`cam_hal: NO-SOI` in the log). The first watchdog cut (a) held a camera mutex *across* `fb_get()` and (b) ran at low priority, started *after* `motion_start()`. Both were fatal: the spinning grab (in the prio-4 motion task) starved the prio-1 `app_main` before it ever reached `camera_watchdog_start()`, so the watchdog never launched; and even if it had, the mutex held by the spinning grab would have blocked recovery forever. So the broken camera prevented its own recovery. The redesign below fixes both.

**Detection.** All frame grabs go through `camera_grab()`/`camera_return()` (never `esp_camera_fb_get()` directly). These **never hold a lock across `fb_get()`** — a tiny critical section only guards a `recovering` flag (new grabs bail so none enter `fb_get()` during a deinit), an `inflight` count (grabs inside `fb_get()` or holding an un-returned frame), an `attempts` counter, and the last-good-frame timestamp. The `cam_wd` task checks every 2 s and declares a stall when no good frame has arrived for >5 s **while consumers are actively grabbing** (attempts advancing or a grab in flight). If nobody is grabbing, the heartbeat is stale but the camera is merely *idle* — left alone. Crucially the watchdog does **no probe grab of its own**, so a spinning camera can't wedge the watchdog too.

**Startup ordering & priority (the fix for "watchdog never started").** `camera_watchdog_start()` runs right after `camera_init()` and **before** `wifi`/`web`/`motion` in `main.c`; rollback-validate and "boot complete" also run before `motion_start()`, which goes **last**. So everything critical is up before the one task whose grab loop can spin on a broken camera. The `cam_wd` task runs at **priority 6 — above the motion task (4)** — so it stays schedulable to detect and recover even while a grab spins CPU-bound.

**Recovery.**
- **Soft re-init (implemented).** Set `recovering` (new grabs bail), drain `inflight` (bounded 2 s), `esp_camera_deinit()`, hold **XCLK low ~150 ms** to let the sensor PLL settle, re-init. Clears ESP32-side LCD_CAM/DMA latches, XCLK glitches, and the `fb_get()`-returns-`NULL` latch. Counted + timestamped.
- **Undrainable wedge → fault (implemented).** If `inflight` won't drain (a grab is stuck spinning inside the driver), `deinit()` would crash that task, so recovery **cannot proceed safely** — it returns `ESP_ERR_INVALID_STATE` and the watchdog raises `camFault` and backs off. Auto-reboot is *deliberately not* used: field evidence shows a full reboot doesn't clear the sensor-core latch, so it would only drop recordings for nothing.
- **Repeated failure → fault (implemented).** If three soft re-inits in a row don't bring frames back, set `camFault` and back off. Either way the fault is surfaced loudly (Debug tab + `/api/sysinfo`) so the operator knows a manual power cycle is required.

**Telemetry.** `/api/sysinfo` exposes `camRecoveries` (successful re-inits since boot), `camRecoveryAgo` (seconds since the last, −1 if none), `camFault` (bool), and `socTempC` (ESP32-S3 on-die temperature — a proxy for whether the box is running hot, for the overheat-vs-connection diagnosis; the sensor is the SoC die, not the camera). The Debug tab shows "Watchdog recoveries", a color-coded "SoC temperature", and a red "Camera fault" row when set.

**Tier 2 — hardware power-cycle (NOT implemented; requires a HW mod).** The guaranteed fix for the analog-core latch is to cut and restore the sensor's power, reproducing the battery pull in firmware. The reference board (`BOARD_ESP32S3_CAM_GENERIC`) has **`CAM_PIN_PWDN = -1` and `CAM_PIN_RESET = -1`** — neither line is wired to a GPIO, so firmware has no power lever today. Two possible mods, in increasing reliability:
  1. **Wire PWDN** to a spare GPIO and set `CAM_PIN_PWDN`. On many OV2640 modules PWDN gates the internal regulators; toggling it *may* be enough. Cheapest (one wire), not guaranteed.
  2. **MOSFET / load-switch on the camera VDD rail** driven by a spare GPIO (P-FET high-side switch or a load-switch IC). Literally power-cycles the module — guaranteed to clear the latch.

  Once either lever exists, tier 2 slots into `camera_recover()` between tiers 1 and 3: assert power-off → wait ~300 ms → assert power-on → re-init.

---

## 4. WiFi Configuration (First Startup)

Identical in design to the RemoteStart project's provisioning (its FSD §4 / `main.c` WiFi section), reimplemented in this codebase:

1. On first power-up — or any boot where NVS holds no WiFi credentials — the unit opens a SoftAP config portal:

   | AP Name | Password |
   |---|---|
   | `BirdBox-Config` | `birdbox1234` |

2. The portal page (served at the AP's root, `192.168.4.1`) offers a **WiFi network scan** — the user picks their SSID from the scanned list (or types a hidden one) and enters the password.
3. Credentials are URL-decoded and saved to NVS (`ssid`/`pass` in the `birdbox` namespace); the unit reboots and connects as a STA.
4. Boot-time connect: 15 s timeout / 5 retries. On failure **with stored credentials**, the unit reopens the portal *in APSTA mode* while continuing to retry in the background (a nest box may be at the edge of range; the portal must not permanently strand a temporarily-offline unit). Once connected the first time, in-service disconnects retry indefinitely and never reopen the portal (RemoteStart v1.25 lesson).
5. A **WiFi tab** in the normal web UI (always available, not just first boot) allows changing SSID/password and choosing **DHCP or static IP** (IP/mask/gateway/DNS, server-side IPv4 validation, invalid saved config falls back to DHCP) — the RemoteStart v1.37/v1.38 design.
6. Credentials reset: hold the boot button ≥ 5 s at power-up → NVS WiFi namespace erased → portal reopens.
7. `WIFI_PS_NONE` and max TX power from day one (RemoteStart v1.32 lesson — modem-sleep latency ruins HTTP streaming), and `httpd` with `lru_purge_enable = true` and handler-count headroom (v1.35/v1.27 lessons).

### 4.7 Alternative network (alt1) & failover

The device stores **two** networks: a **primary** (`ssid`/`pass`) and an optional **alternative "alt1"** (`ssid2`/`pass2`), both configured from the WiFi tab (each with its own scan-and-pick). This covers a box that can see two APs (e.g. a house AP and a garden repeater on a different SSID), so it isn't stranded when one is down or out of reach.

- **Boot:** try the primary first (15 s / 5 retries); if unreachable, try alt1 (same budget); if both fail, open the portal — which then keeps retrying **both** networks, alternating each 60 s cycle.
- **In service:** after 5 consecutive reconnect failures on the current AP, fail over to the other stored network, and keep alternating — so the box latches onto whichever AP is actually present as it roams or as APs come and go. Never reopens the portal once ever-connected.
- **Shared IP setting:** the DHCP-or-static IP configuration (§4.5) applies to **whichever** network connects. A single static address is only valid if the primary and alt1 are on the **same subnet**; for APs on different subnets, use DHCP. The WiFi tab states this.
- **Visibility:** the WiFi tab shows the currently-connected AP plus both configured SSIDs (`GET /api/wificfg`, passwords never exposed); the Debug tab's WiFi Link section shows the connected network name (`apSsid` in `/api/sysinfo`).
- **Editing:** saving with `slot=1` and a blank SSID removes alt1. The boot-button reset (§4.6) erases both networks. Passwords are write-only — the tab shows SSIDs but never pre-fills passwords.

---

## 5. Web UI

Single-page UI embedded in firmware (no filesystem-served assets, no CDN), tab bar following the RemoteStart standard:

**Live | Gallery | Stats | Settings | Debug | WiFi | OTA Update**

- **Live** — MJPEG stream, snapshot button, current motion-detection state indicator, quick rotation toggle (mirrors the Settings tab's rotation field).
- **Gallery** — §3.4 browsing/labeling.
- **Stats** — §3.4 charts, plus a confirm-gated Reset Statistics button that clears the visit-log history (saved photos are unaffected).
- **Settings** — placement mode (nest box/feeder), motion sensitivity, capture count/interval, cool-down, confidence threshold, species set (global / Northern-Europe filter, §3.2.1), retention cap, stream quality, camera resolution (HD or SXGA, reboot to apply), contrast, image rotation (0/90/180/270, mount-correction), species-model region (§3.2), timezone, NTP server, IR LED mode (off/auto).
- **Debug** — System card (free heap + low-water mark with age, uptime, WiFi reconnect count + last-reconnect age), WiFi Link card (RSSI/channel/own MAC), SD card status (size/free/health), camera sensor status, last-inference timing.
- **WiFi** — §4 step 5, plus a Reboot Now button.
- **OTA Update** — §8.

All settings persist in NVS and apply without reflashing; settings that require a restart (camera resolution) say so and offer the reboot.

---

## 6. REST API

All UI data flows through JSON endpoints, so the device is scriptable/integrable (Home Assistant etc.) without scraping:

| Endpoint | Method | Purpose |
|---|---|---|
| `/api/status` | GET | Current state: motion, last event, species, SD/heap/WiFi summary |
| `/api/sysinfo` | GET | Debug-card data (heap, heapMin+age, uptime, reconnects) |
| `/api/events?date=&species=&page=` | GET | Paged visit log |
| `/api/events/<id>` | PATCH / DELETE | Correct species label, favorite, delete |
| `/api/stats/daily`, `/api/stats/species`, `/api/stats/hourly` | GET | Chart data |
| `/api/stats/reset` | POST | Delete all visit-log CSVs (clears stats/history; photos untouched) |
| `/api/captures/delete` | POST | Bulk-delete photos: `date=` + `files=a.jpg,b.jpg` (multi-select) or `all=1` (whole day); add `stats=1` to also wipe that day's visit-log rows |
| `/api/capture` | POST | Manual snapshot now |
| `/api/settings` | GET / POST | Read/write settings |
| `/api/ipconfig`, `/api/ipconfig/save` | GET / POST | DHCP/static IP (RemoteStart design) |
| `/api/reboot` | POST | Reboot |
| `/stream` | GET | MJPEG live stream |
| `/captures/...` | GET | Static serving of stored JPEGs from SD |
| `/ota/upload` | POST | Raw `.bin` firmware upload (§8) |
| `/api/classify` | POST | Classify a posted JPEG now — top-3 + decision (§3.2) |
| `/api/classify-file?date=&f=` | GET | Re-run species ID on a saved Gallery photo, read from SD (§3.2) |
| `/model/upload?name=` | POST | Install a model/labels file into `/sd/model` (§3.2) |

No authentication in v1 (LAN-only device, same posture as RemoteStart); an optional basic-auth password is a v2 candidate and noted in §11.

---

## 7. Storage

- **microSD (FAT32)**: captures (`/captures/YYYY-MM-DD/`), visit logs (`/log/visits-YYYY-MM.csv`), optional model file (`/model/`). The device boots and runs without an SD card — live view still works; capture/history features show a clear "no SD card" state instead of failing silently.
- **NVS**: WiFi credentials, IP config, all §5 settings, favorite-species list.
- SD writes are sequenced through a single writer task (camera capture, log append and HTTP static serving must not interleave mid-file).

---

## 8. OTA Updates

- Web-based OTA (OTA Update tab): upload a `.bin` from the browser, RemoteStart-style, with bounded `httpd_req_recv()` retries (v1.5 lesson) and dual OTA partitions with rollback on boot failure.
- Release binaries are produced by CI (§9) so open-source users never need an ESP-IDF toolchain to stay current: download `birdbox-vX.Y.Z.bin` from GitHub Releases → OTA tab → upload.

---

## 9. GitHub, CI & Open-Source Deliverables

- **Repository:** GitHub, public when ready for release. Layout:
  ```
  BirdBox/
  ├── FSD_BirdBox.md          # this document — updated with a changelog entry per change (RemoteStart practice)
  ├── README.md               # user-facing: hardware list, wiring, flash & first-boot walkthrough
  ├── LICENSE                 # MIT
  ├── main/                   # ESP-IDF app (C)
  ├── components/             # esp32-camera, esp-dl model runner
  ├── docs/                   # enclosure/mounting notes, model-swap guide
  └── .github/workflows/      # CI
  ```
- **CI (GitHub Actions):** every push builds with the pinned ESP-IDF version for both targets (esp32s3 primary, esp32 fallback); a `v*` tag creates a GitHub Release with the built `.bin` files and auto-generated per-target version lines (RemoteStart v1.16 pattern).
- **Versioning:** `FIRMWARE_VERSION` in `version.h`, semver; FSD changelog is the change record.
- **Open-source posture:** issues/PRs welcome; docs must be good enough that a stranger with the listed hardware succeeds without asking. Community label corrections (§3.2) may be pooled via voluntary GitHub issue uploads to retrain the regional models — no telemetry, ever, and nothing leaves the device automatically.

---

## 10. Development Environment

- ESP-IDF v6.x on Windows (`idf.py build/flash/monitor`), same toolchain installation as RemoteStart/esp32_clock.
- `idf.py set-target esp32s3` for the primary board.
- `sdkconfig.defaults` per target committed to the repo (PSRAM enabled, camera, FATFS long filenames, httpd tuning per §4.7).

---

## 11. Non-Goals (v1) & Future Candidates

**Out of scope for v1:**
- Cloud services, accounts, or any off-LAN access built into the device (§3.3).
- Audio capture / birdsong identification.
- Battery/solar power management and deep-sleep operation (PIR wake groundwork is laid, §3.1).
- Video with audio; H.264 encoding.
- Web UI authentication.

**Likely v2 candidates:** basic-auth for the web UI, birdsong ID (I2S microphone), battery/deep-sleep mode, MQTT publishing for Home Assistant, multi-camera aggregation page.

**Deferred species-ID options (analysed with DetectEnhance2 / v1.33, deliberately not implemented — revisit with field data from the new `roi`/`top3` visit-log columns):**
- *Score-averaging ensemble across best-of-N frames:* instead of keeping the single most confident frame's result, accumulate the per-class scores across all N zoomed frames and decide on the average (standard ensembling; 965 int sums, trivial memory). More robust to one lucky/noisy softmax, but it changes decision semantics (confidence values shift relative to the user's threshold) and needs validation against real visit data before replacing max-pick.
- *Bilinear resize of the 224×224 model input:* the crop currently uses nearest-neighbor, which is blocky when the ROI upscales. Largely mitigated by the ROI-aware decode scale (v1.33); worth ~a few % accuracy at most, cheap to add if inference quality still lags.
- *Adaptive per-pixel motion threshold:* `PIX_DIFF_THR` is fixed at 25 gray levels; sensor noise rises at dusk/dawn, so a noise floor estimated from quiet frames (e.g. `thr = max(15, 3×mean|cur−bg|)`) would keep trigger behaviour consistent across lighting. No field evidence of a problem yet — implement only if false triggers/misses correlate with light level.

---

## 12. Acceptance Criteria (v1)

**Status: all seven criteria below met and verified live on the reference unit as of firmware 0.18.0 / FSD v1.21 — v1 is functionally complete.** Ongoing work past this point (region filter, resolution/contrast, Gallery cleanup tools, etc.) refines v1 rather than closing a gap in it.

1. Fresh flash → `BirdBox-Config` portal → scanned SSID selected → device on home WiFi in under 3 minutes, no toolchain needed.
2. A bird landing at the feeder produces exactly one visit event with ≥ 1 sharp full-res JPEG on SD and a visit-log row.
3. A common regional species in good light is correctly top-1 identified ≥ 70 % of the time; low-confidence events say "Unidentified bird", never a confident wrong guess above threshold.
4. Live stream viewable in a browser while captures continue to be recorded.
5. Stats tab correctly aggregates a week of real events by day, species and hour.
6. OTA from GitHub Release binary succeeds and survives a mid-upload abort without wedging the web server.
7. Device runs ≥ 7 days unattended with no reboot, no heap-low-water regression, and survives router reboots (indefinite reconnect).
