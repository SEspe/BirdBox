# streamtest partition table — dual OTA slots on a 16 MB flash.
#
# Two app slots exist specifically so PSRAM experiments can be flashed over the
# network: with CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE, an image that hangs in
# cpu_start (which is exactly what a PSRAM-enabled build does on a bad board)
# never reaches esp_ota_mark_app_valid_cancel_rollback(), so the bootloader
# reverts to this known-good streamer on the next boot. No serial needed.
#
# Name,     Type, SubType, Offset,   Size
nvs,        data, nvs,     0x9000,   0x6000
otadata,    data, ota,     0xf000,   0x2000
phy_init,   data, phy,     0x11000,  0x1000
ota_0,      app,  ota_0,   0x20000,  0x300000
ota_1,      app,  ota_1,   0x320000, 0x300000
